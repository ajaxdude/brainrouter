brainrouter is a unified Rust daemon (serve/mcp/install) acting as an LLM failover proxy and review escalation system. Runs on TCP 127.0.0.1:9099 and UDS /run/user/1000/brainrouter.sock. MCP binary is a thin client forwarding to the daemon. Session state is in-memory. Anthropic /v1/messages shim translates to OpenAI with SSE streaming. Review/Escalation API/UI lives under /review/*; root / redirects to /dashboard. Config: brainrouter.yaml. Systemd service active. 38 tests pass. Manifest /v1/models returns 404 by design (Manifest doesn't implement that endpoint); /v1/chat/completions works. All pingpong references removed from codebase and agent config files. ~/.omp/agent/APPEND_SYSTEM.md and LLAMACPP.md updated to reference mcp_brainrouter_request_review and http://127.0.0.1:9099/review.
