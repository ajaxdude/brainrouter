{
  "mcpServers": {
    "brainrouter": {
      "type": "stdio",
      "command": "/path/to/brainrouter",
      "args": ["mcp", "--socket", "/run/user/$UID/brainrouter.sock"],
      "timeout": 300000
    }
  }
}
