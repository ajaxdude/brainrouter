# brainrouter as a native webui for ai-toolbox-cockpit's backends

## Status
`v3 — reworked per user + TOP SESSION feedback, approved, rollout in progress`

- **v1 — 2025-09-16** — initial draft, produced via the Marlin phase (context load + sycophant challenge + first-approach proposal). Not yet reviewed.
- **v2 — 2025-09-16** — revised after 3 independent Dory-phase reviews (comprehension: **FAIL**; readiness: **NOT READY** for PR1+PR2 scope; critic: **5 blocking + 17 important findings**, round 1 of a max-3 bounded loop). This revision resolves or explicitly re-scopes all 5 blocking findings (naming/enum future-proofing, config.json write strategy, routing/provider-integration prerequisite, `toolbox create` engine-args feasibility, and the `--name` bug's real mechanism), folds in the 17 important findings, and directly re-verified (via `grep`/`view` against live source, not sub-agent claims alone) the two most load-bearing critic assertions: the benchmark migration runner's hardcoded version gate (`src/benchmark.rs:1895-1953`) and the daemon's fixed Manifest/llama-swap-only provider construction (`src/daemon.rs:305-320`). Both confirmed true. See Decision log for what changed and why.
- **v3 — 2025-09-16** — approved by the user, then reworked at the user's explicit request ("agreed rework all the ones you mentioned") after a post-approval second-thoughts pass raised 4 concerns about v2's own design: (1) confirm the config.json Phase-1 narrowing is acceptable — **confirmed by user, now settled** (Open question 5 resolved, moved to Decision log); (2) reconsider whether the full §5b routing-integration gate should block Sankey **and** benchmark work for llama_cpp, which is already routable today — **reworked**: Sankey and the benchmark engine dimension now ship early and incrementally per-backend rather than behind one big gate; (3) the R9V `toolbox create` spike (PR5) was blocking all four non-llama_cpp backends, not just R9V — **reworked**: the spike now gates only R9V (and any future backend needing non-default `engine_args`/device passthrough); ds4/halogen/vllm proceed independently; (4) reorder to front-load foundational, hardware/router-independent work and defer the parts that need real traffic or real hardware — **reworked**: Rollout is now organized into explicit waves (see below). This is a scope/sequencing rework, not a re-litigation of the Dory-reviewed technical content in §1-§9, which stands unchanged from v2. **Same day, TOP SESSION (creator session) separately reviewed and resolved 4 of the document's Open questions** (config.json phase-1-vs-bidirectional; cockpit-config-dir-absent behavior; spider-chart categorical-axis semantics; Dory review-rigor sufficiency) and instructed autopilot implementation to proceed starting at PR 0 — see Decision log for each resolution and Open questions for what remains genuinely open.

**Change classification:** `standard`, not `trivial` — this touches public API surface, adds DB schema, introduces a shared cross-tool on-disk config file, and adds new privileged container-mutation endpoints. Full Dory gate set applies; no shortcut available.

**Isolation disclosure (Dory Core rule 2):** all 3 Dory-phase reviews below were run as separate-context `task`-tool sub-agents — fresh model context, no access to this session's conversation history, read-only tools, instructed to read only this document plus the repository/upstream files it cites. This is **not** the same as spinning up fully separate `create_session` app sessions; that tooling was available in this session and was not used for this round. Recorded here as a **partial** isolation certification per the rule's requirement to disclose the actual mechanism rather than assert unearned certification. A future round can use full `create_session`-based isolation if the user wants that stronger guarantee — flagged in Open questions, not decided unilaterally.

Approved by the user and by TOP SESSION on 2025-09-16 (see Human approval). Implementation in progress (Rollout PR 0).

## Problem
brainrouter's dashboard/CLI today can only list and upgrade a single hardcoded podman toolbox (`docker.io/kyuz0/amd-strix-halo-toolboxes:vulkan-radv`, llama.cpp/Vulkan only). The user runs (or wants to run) additional inference backends — DS4 (DeepSeek4 GGUF), Halogen (HGN "Flash" bundles), vLLM (HF-repo serving), R9V (ROCm on AMD R9700) — that today can only be managed through `ai-toolbox-cockpit`, a Python Textual TUI with **no CLI or API mode** (`main.py`/`app.py` always launches the full-screen app; nothing is shell-out-able). The user wants brainrouter itself — its Rust dashboard and CLI — to become the primary, native interactive surface for managing all of these backends' toolboxes/models/servers, sharing cockpit's on-disk config and catalog data rather than re-inventing a divergent brainrouter-only store, and to surface backend/engine as first-class dimensions in brainrouter's existing Sankey diagram and benchmark system, plus a new spider/radar chart for comparing runs across dimensions including a **proposed** "token efficiency to positive result" metric.

## Goals and non-goals

**Goals**
- Native Rust (no Python/cockpit runtime dependency) toolbox lifecycle management (list/create/update/delete/enter-equivalent) for backends: `llama_cpp` (already partial), `ds4`, `halogen`, `vllm`, `r9v`, and future backends added to the catalog without a brainrouter code change for the *listing/catalog* path (server-mode/bespoke launch support for a brand-new backend still needs a follow-up PR — see Open questions #7).
- Explicitly **exclude `comfyui`** (image-gen, not coding-relevant) from every user-facing surface of this work, even though the catalog format itself must still be able to parse it without failing (see Technical plan §2).
- Read (and, per the user's explicit ask, eventually write) cockpit's `~/.config/ai-toolbox-cockpit/config.json` so both tools share one source of truth for backend defaults/active platform/default toolboxes, instead of a second brainrouter-only config. **Revised in v2:** the write half ships in a narrower, safer first form than v1 proposed — see Technical plan §4.
- Consume cockpit's `models.json`/`toolboxes.json` catalog as the data source for what toolboxes/models exist, their images, `runtime_profile`/`engine_args`, and per-model `toolbox_defaults`.
- Extend the Sankey diagram with two new columns: **backend** (which toolbox family served the request) and **engine** (which accelerator/runtime the toolbox used — vulkan/rocm/etc.).
- Extend the benchmark system so runs carry backend + engine as queryable/filterable dimensions.
- Ship a new spider/radar chart in the benchmark UI letting the user pick dimensions among: models, harnesses, token efficiency to positive result, speed, engine, backend, toolbox.
- Fix the stray untracked-container bug in the version-check code path as an early, independent, low-risk fix. **Revised in v2:** the root-cause mechanism is refined (see Technical plan §5a) — the original "missing `--name`" framing was incomplete.

**Non-goals (this round)**
- comfyui support (explicitly excluded by the user) — catalog parsing tolerates it, nothing else does.
- Full 1:1 feature parity with every cockpit TUI micro-interaction (e.g. its `SearchableSelect`/`SelectModal` widgets, ASCII banner, exact keybindings) — parity is scoped to *capabilities* (create/update/delete/list/launch/configure), not TUI widget fidelity, since brainrouter's surface is a web dashboard + CLI, not a TUI.
- Running/validating DS4, Halogen, vLLM, or R9V on hardware brainrouter's current dev/CI environment (Strix Halo, Vulkan/ROCm) can exercise end-to-end — see Risks.
- Rewriting the existing benchmark experiment/run schema (`runtimes`, `experiments`, `runs`, …) wholesale — we extend it (exact mechanism now an explicit Open question, see below), we do not replace it.
- A jointly-supported (brainrouter+cockpit) locking/conflict protocol for `config.json` — out of scope until upstream cockpit adopts one; see Technical plan §4.
- Wiring detached toolbox server-mode containers into brainrouter's actual LLM request router as fully first-class routable providers with health-checking/failover parity to Manifest/llama-swap — v2 adds a **minimal** registration/tagging prerequisite (Technical plan §5b) sufficient to populate Sankey/benchmarks honestly, not a full routing-parity rewrite.

## Current system

### Toolbox/podman management (brainrouter, today)
- `GET /api/toolboxes` → `toolboxes_list()` (`src/server.rs:1753-1809`) runs `podman ps -a --format "{{.Names}}\t{{.Image}}\t{{.Status}}"`, keeps only containers named `llama-*`, and enriches each with `local_created`/`latest_created`/`update_available` via `image_created_date()` (`:1825`) and `hub_toolbox_tag_dates()` (`:1839`, a Docker Hub tags-API poll — this is brainrouter's existing analogue of a "periodic fetch" pattern). Response is ad-hoc `serde_json::Value`; **no dedicated `Toolbox` struct exists**.
- `upgrade_toolbox()` (`src/server.rs:2257-2313`): `podman pull <image>` → `toolbox rm --force <container>` (result ignored) → `toolbox create --image <image> <container>`. Helper `toolbox_container_image()` (`:2244`) reads the current image via `podman inspect --format "{{.Image}}"`.
- Hardcoded default image `docker.io/kyuz0/amd-strix-halo-toolboxes:vulkan-radv` appears at `src/server.rs:527` (upgrade route default), `:1889` (version-check `podman run`), `:1919` (image inspect for version metadata), and the Docker Hub tag URLs at `:1842`/`:2044` hardcode the `kyuz0/amd-strix-halo-toolboxes` repo. There is **no backend/engine concept anywhere** in this code — it is single-image, single-backend by construction.
- **Confirmed bug, mechanism refined in v2 (see Technical plan §5a):** `src/server.rs:1886-1909` runs `podman run --rm --kill-on-drop docker.io/kyuz0/amd-strix-halo-toolboxes:vulkan-radv llama-server --version` with a 15s `tokio::time::timeout` (`PODMAN_VERSION_TIMEOUT_SECS`, `:1886`) and `.kill_on_drop(true)` (`:1890`) but **no `--name`**. v1 of this document asserted the missing `--name` itself was the leak cause; the Dory critic correctly pointed out `--rm` alone reclaims the container on a *normal* exit regardless of name. Re-verified directly: the real leak path is the **timeout branch** — when `tokio::time::timeout` fires, `kill_on_drop` sends the child process a kill signal, which can terminate the `podman run` client without giving it the chance to run its own `--rm` cleanup, potentially orphaning the container underneath. A fixed `--name` doesn't prevent that by itself, but it is still the right complementary fix: without a name, an orphaned container can't even be *found* to clean up (only a random pet-name); with a fixed name, an explicit `podman rm -f <name>` can be issued on the timeout branch as a real, targeted cleanup, and `--replace` prevents "name already in use" errors on concurrent/retried checks.
- CLI: `src/cli.rs:88-104` defines `Toolboxes` and `Upgrade { component }` (clap `PossibleValuesParser` limited to `["llama-swap","manifest","toolbox"]`); handlers at `:527-536` just proxy to `/api/toolboxes` and `/api/upgrade/{component}`. The CLI is a thin client that can target either a local UDS or a **remote** daemon via `--url` (`src/cli.rs:5-6,23-32,339-347`) — relevant to the "enter" design, see Technical plan §5.
- Config: brainrouter's own config is **YAML**, not JSON — `BrainrouterConfig`/`ManifestConfig`/`LlamaSwapConfig`/`ModelsConfig`/etc. in `src/config.rs`, resolved via `default_config_path()` (`~/.config/brainrouter/brainrouter.yaml`). There is no existing brainrouter JSON config and no existing reader for cockpit's `~/.config/ai-toolbox-cockpit/config.json` format. **Relevant risk already partially known to the codebase:** `src/daemon.rs` already warns at startup if `$HOME` is unset ("upgrade and version-check paths will fall back to /root", `src/daemon.rs:284-286`) — i.e. the daemon's process HOME/config resolution diverging from an interactive user's HOME is an already-acknowledged class of bug, directly relevant to config.json sharing (Technical plan §4).
- Existing destructive-route protection: `src/server.rs:224-241` gates a hardcoded path-prefix/exact-path allowlist (`/api/restart/*`, `/api/upgrade/*`, `/api/benchmarks/*`, etc.) to loopback-only callers. **New mutation routes this plan adds are not automatically covered** — each new privileged path must be explicitly added to this list, or it is reachable from non-local callers if the daemon binds non-loopback (`src/daemon.rs:144-153` — the daemon has an optional non-loopback bind mode). Confirmed by direct inspection.
- Terminology note (important, see Alternatives/Decision log): brainrouter's codebase already uses the words "backend" and "engine" informally elsewhere (routing profile backends in `src/routing_profile.rs`/`src/router.rs`, and UI copy). None of those are the toolbox/GPU-accelerator concepts this plan introduces — they are LLM-routing-provider concepts. This plan must not conflate the two; naming is addressed explicitly below.
- **Router/provider construction (new in v2, load-bearing for the Sankey/benchmark promises):** `src/daemon.rs:305-320` constructs the single shared `Router` from exactly `classifier`, `manifest`, `llama_swap`, and a handful of fixed routing knobs (`RouterArgs`) — there is **no generic "list of providers" or dynamic-registration concept** today. A detached, brainrouter-managed toolbox server container (Technical plan §5) is therefore **invisible to the router** unless something explicitly wires it in. This directly gates how much of the Sankey/benchmark "backend/engine reflects real traffic" promise can be true in the near term — see Technical plan §5b and Decision log.

### Sankey diagram (`src/escalation/templates/main_dashboard.html`)
- Fixed **4-column** schema: `SK_COLS = [harness@x16, session@x300, routing@x720, model@x1040]` (`:1314-1319`), total width `SK_TOTAL_W = 1232` (`:1325`).
- `skKeysOf(e)` (`:1411-1414`) returns exactly `{c0,c1,c2,c3}` from `skHarness/skSession/skRouting/skModel`; `skModel()` reads `e.model_key` / `e.effective_provider`, fields that come from `RouteEvent` (`src/routing_events.rs:30-82`, corrected citation — the struct itself spans `:30-82`, not `:23-63` as v1 stated).
- Rendering is **hand-rolled SVG**, not a chart library: `updateSankey()` rebuilds `svg.innerHTML` from computed node/link maps, draws Bézier `<path>` links and `<rect data-col data-key>` node bars.
- Click-to-filter state is `sankeyFilter = {col, key}` (`:1326`-ish); `matchesSankey(t)` compares `skKeysOf(t)['c' + sankeyFilter.col]`. Everything is keyed generically by column index (`c0..c3`, `SK_COLS[i]`), so **the mechanism itself already generalizes to N columns** — the concrete blocker to adding columns is (a) `skKeysOf()` hardcodes exactly 4 fields, (b) `skColorFor()` hardcodes a color map per column index 0–3, (c) fixed pixel `x` positions / `SK_TOTAL_W` assume 4 columns' worth of width, (d) the upstream data source (`RouteEvent`) has no backend/engine fields to key off today, and (e) — new in v2 — even once the fields exist on `RouteEvent`, **only requests actually routed through the Router will ever populate them**; detached toolbox-server containers launched from the new Toolboxes UI are not on that path today (see above).

### Benchmark system (`src/benchmark.rs`, `migrations/0001_benchmark_explorer.sql`, `migrations/0002_benchmark_lab.sql`, `src/escalation/templates/benchmarks.html`)
- This is a **mature, already-multi-dimensional** star schema: `models` → `artifacts` → `experiments` (joins `artifact_id`, `runtime_id`, `hardware_id`, `workload_id`) → `runs` → `performance_metrics`/`speculative_metrics`/`quality_results`/`telemetry_samples`.
- **Key discovery (changes scope of this plan materially):** `runtimes.backend` (`migrations/0001_benchmark_explorer.sql:58`) is already `CHECK(backend IN ('cpu','cuda','rocm','vulkan','metal','sycl','rpc','other'))`. That is *already* the coarse "engine" axis the user is asking for (vulkan/rocm, plus headroom for cuda/metal/sycl). `RuntimeDefinition` (`src/benchmark.rs:388-406`) and `RunSummary` (`:1748-1773`) already surface it end-to-end to the UI. We do not need a new column for this *coarse* axis — but v2 refines this (see Technical plan §1): cockpit's actual `runtime_profile` strings (e.g. `amd-rocm-hipblaslt`, `halogen-strix-halo`) are **finer-grained** than this 8-value enum and do not map onto it 1:1; the existing column remains the right thing to expose as the user-facing "Engine" (that's the granularity the user actually asked for — "vulkan/rocm"), but the finer profile id is tracked as a separate, non-benchmark-exposed field for toolbox deployment purposes only.
- What genuinely does **not** exist yet: a "which toolbox family served this run" dimension (ds4/halogen/vllm/r9v/llama_cpp). Nothing in `runtimes`, `experiments`, or `runs` names the *toolbox/serving stack*; `runtimes` today models a llama.cpp-fork-centric world with **`NOT NULL` fields** `repository`, `fork_name`, `commit_sha`, `compiler`, `compiler_version` (`migrations/0001_benchmark_explorer.sql:50-64`) that don't cleanly fit vLLM (HF-repo based) or DS4/Halogen (bespoke GGUF/HGN loaders) — these backends have no meaningful "compiler"/"commit_sha" in the same sense. This is a real, new dimension, and how to add it without breaking the existing `NOT NULL`/uniqueness contract is a genuine, not-yet-fully-resolved schema-design question (elevated to Open questions in v2 — see below; v1 asserted a specific `ADD COLUMN` approach that the critic showed was incomplete against the actual constraint).
- Ingest is `INSERT OR IGNORE` (`src/benchmark.rs:2248-2273`) keyed on `runtimes`' existing uniqueness — adding new dimensions to that table risks either (a) not being covered by the uniqueness key, so distinct toolbox-family runs collide/dedupe incorrectly, or (b) widening the uniqueness key, which can silently orphan a referencing `experiment_id` under `INSERT OR IGNORE` semantics if a near-duplicate row is ignored instead of inserted. Must be designed explicitly, not assumed safe by analogy to today's llama.cpp-only usage.
- **Migration runner is hardcoded and strict** — re-verified directly in v2: `src/benchmark.rs:1895-1953` only recognizes schema versions 1 and 2, requires an exact row/table-count match (`required_v1_tables != 5`, `required_tables != 6`), and returns `BenchmarkError::Validation` for anything else. A new `migrations/0003_*.sql` file **will not run** just by existing on disk — this function's version-gate logic must be extended to know about version 3 (and later 4), including its own expected-table-count check. This was flagged by the Dory critic as a concrete, load-bearing implementation gap (not a stylistic one) and is now called out explicitly in Detailed implementation.
- Outcome tracking already exists: `runs.status` (`succeeded|failed|oom|timeout|cancelled|skipped|...`) and `quality_results.passed`/`compile_succeeded`/`tests_passed`/`tests_total`. Token counts already exist per-run (`experiments.prompt_tokens`, `experiments.generation_tokens`) and per-quality-check (`quality_results.generated_tokens`). **No existing "token efficiency" or "tokens per success" metric anywhere in the codebase** — confirmed by search. **New in v2:** `experiments.prompt_tokens`/`generation_tokens` are, on inspection, the run's **declared/planned** token budget fields (populated at experiment-setup time), not necessarily an **observed, actually-consumed** count per attempt — this distinction matters a lot for a metric named "token efficiency to *positive result*" and must be resolved during implementation (see Technical plan §8 and Open questions).
- Charting is custom inline SVG (`scatter`, `drawBars`, `drawQuality`, etc. in `benchmarks.html`) — no chart.js/d3, and **no existing radar/spider chart** anywhere in the codebase. The spider chart is wholly new UI, not an extension of an existing chart type.
- Import path: `POST /api/benchmarks/ingest/llama-bench` (`src/benchmark/http.rs`) + `apply_llama_bench_metrics()` (`src/benchmark/imports.rs`) only reads throughput/ttft fields from `llama-bench.json`; backend/engine/toolbox identity must come from the **experiment/runtime template** the import is attached to, never from the llama-bench payload itself.

### ai-toolbox-cockpit (verified against the actual repo, not just the background brief)
- Entry point is `ai_toolbox_cockpit/app.py` (a Textual `App`), with three tabs backed by `views.py`: `ToolboxesView`, `ServersView` ("Server Mode"), `ModelsView` — confirming the three surfaces the user wants brainrouter to match/exceed.
- `catalog/schema.py`: `BACKEND_IDS = {llama_cpp, vllm, comfyui, ds4, halogen, r9v}`; `MODEL_KINDS` maps each backend to its model artifact kind (`gguf`, `gguf_file`, `hf_repository`, `r9v_package`, `hgn_bundle`, `workflow_bundle`). Per-backend model-entry validation is genuinely different per backend (llama_cpp: `inference_profiles`+`toolbox_defaults`+`auxiliary_downloads`; r9v: pinned 40-hex commit SHA + a fixed set of required file `role`s + a separately-hashed PLE file; halogen: `.hgn` checkpoint/overlay + tokenizer dir; vllm/comfyui validated elsewhere in the same file, not yet fully inspected). This confirms the README's claim that "each backend owns its model semantics" — a generic "download+run" abstraction will not fit all five; brainrouter needs a per-backend adapter, not one generic path.
- `assets/toolboxes.json` (`schema_version: 3`): a `runtime_profiles` map (named profiles like `amd-rocm`, `amd-rocm-hipblaslt`, `vulkan`, `intel-level-zero`, `nvidia-gb10`, `halogen-strix-halo`) each holding raw `engine_args` (podman `--device/--group-add/--security-opt/--env` flags) — **this is the actual accelerator/engine concept** in cockpit, distinct from `runtime/engines.py`'s `ContainerEngine` enum (`podman`/`docker`, the *container tooling* choice). A `toolboxes` array lists concrete toolbox entries: `id`, `backend`, `name`, `container_name`, `group`, `image`, `channel` (`stable|development|experimental`), `maturity`, `description`, `runtime_profile` (points into the profiles map above), `supports_load_mode`, `features` (`interactive|server|models`, each `supported|experimental|unavailable`), and optional `backend_config.recommended_use` (fork-specific validated model/quant/server-defaults pairing, e.g. the EngramHalo and HRX-staging entries).
- `settings.py` confirms the exact `~/.config/ai-toolbox-cockpit/config.json` mechanics: `config_path()` honors `$XDG_CONFIG_HOME`; `load_settings()`/`save_settings()` do plain `json.load`/`json.dump`, **atomic write via `.tmp` + `chmod(0o600)` + `Path.replace()`**, silently returning `{}` (falling back to legacy `~/.llama-cockpit.conf`/`~/.ds4-cockpit.conf` migration) on any read error. Structure: top-level `active_platform: str`, `backends: {<backend_id>: {default_toolboxes: {<platform_id>: <toolbox_id>}, ...arbitrary per-backend keys}}`. **There is no lock file and no read-modify-write transaction across processes** — cockpit itself has no protection against two of its own processes racing on this file; brainrouter joining as a second writer inherits that same "last writer wins" property from upstream at best, and can make it materially worse in a specific way v1 did not consider: brainrouter (often run as a long-lived daemon, possibly under a service manager) and an interactively-invoked cockpit TUI can easily have **different effective `$HOME`/`$XDG_CONFIG_HOME`**, meaning "shared config" silently becomes "two different files with the same name," not a shared-write race — see Technical plan §4.
- `runtime/engines.py`: `inspect_containers()` runs `<engine> ps -a --format "{{.Names}}|{{.Image}}|{{.Status}}|{{.CreatedAt}}"` (pipe-delimited, includes `CreatedAt` — brainrouter's current tab-delimited format omits `CreatedAt` and derives dates a different way). `runtime/images.py`: pure command builders (`pull`, `image inspect`, `rm -f`, `image rm`) plus `docker_hub_tag_url()`/`get_remote_image_date()`/`is_remote_image_newer()` — functionally identical in spirit to brainrouter's own `hub_toolbox_tag_dates()`/`image_created_date()`, just written for N images/backends instead of one. `runtime/server_process.py`: server launch is "`<engine> rm -f <container>` (best-effort) → `Popen(command)` in foreground → on exit/Ctrl+C, `<engine> rm -f <container>` again", with a `redact_command()` helper that scrubs `--api-key`/`HF_TOKEN` before ever printing/logging the command. This foreground-with-cleanup pattern does not map 1:1 onto brainrouter's server (which is a long-running HTTP daemon, not a TUI that can "suspend" for a foreground child) — see Architecture and flows.

## Requirements and acceptance criteria
| # | Requirement (from user) | Acceptance criteria |
|---|---|---|
| 1 | Cover ds4, halogen, vllm, r9v (+ future), exclude comfyui | Catalog loader parses all 6 `BACKEND_IDS` (including unknown future ids) losslessly, but the toolbox/model/server UI and API never enumerate or act on `comfyui`/unrecognized entries; adding a 7th backend to upstream's catalog does not break parsing or hide the rest of the catalog, and is at minimum listable (server-mode/bespoke launch support for the 7th backend is an explicit follow-up, not automatic — Open question 7) |
| 2 | brainrouter becomes the native webui for cockpit | Toolboxes/Models/Server-mode-equivalent flows are reachable from brainrouter's dashboard + CLI for every in-scope backend, without cockpit installed or running |
| 3 | Merge config.json | **Revised in v2:** brainrouter reads `~/.config/ai-toolbox-cockpit/config.json` when present and honors `active_platform`/`backends.*.default_toolboxes` (Phase 1, read path first); an explicit, user-triggered "apply to cockpit config" write action exists and round-trips unknown keys untouched; automatic, unconditional background bidirectional read-modify-write is explicitly **not** part of the near-term acceptance bar — see Technical plan §4 and Open questions |
| 4 | Sankey: backend + engine columns | Two new Sankey columns exist, are populated from real request data **for the traffic brainrouter's router actually handles**, and support click-to-filter identically to existing columns; traffic served by a detached toolbox-server container not yet wired into the router is shown under an explicit, distinct bucket (not silently merged with existing routed traffic) until Technical plan §5b lands |
| 5 | Benchmarks: backend + engine dimensions | Runs can be filtered/grouped by toolbox family and by engine (vulkan/rocm/...); existing `runtimes.backend` is confirmed populated and exposed as "Engine"; new toolbox-family dimension is added via a schema approach chosen from the two options in Technical plan §7 (decision needed, see Open questions) |
| 6 | Spider chart with 7 pickable dimensions incl. token-efficiency | User can select 3+ of {models, harnesses, token efficiency to positive result, speed, engine, backend, toolbox} and see a radar chart comparing selected entities across those axes; the token-efficiency metric definition is explicitly presented to the user for sign-off before being treated as fact; categorical axes (engine/backend/toolbox) have an explicit, documented plotting semantics (Open question 4) |
| 7 | `--name` fix | `podman run` version-check no longer creates unnamed/orphaned containers on either the normal-exit or timeout-kill path; verified by a regression test that specifically forces the timeout branch, not just two normal sequential runs (v1's proposed test would not have caught the real bug — see Technical plan §5a) |

## Technical plan

### 1. Naming decisions — three axes, not two (revised in v2)
v1 collapsed "engine" into a single reused column. The Dory critic correctly pointed out that cockpit's `runtime_profile` strings (`amd-rocm-hipblaslt`, `halogen-strix-halo`, `intel-level-zero`, ...) are finer-grained than the existing 8-value `runtimes.backend` enum and don't map onto it 1:1. Resolving this requires **three** distinct, separately-named concepts, not two:
1. **`toolbox_backend` / `serving_backend`** (user-facing "Backend") = *which toolbox/serving product* (`llama_cpp | ds4 | halogen | vllm | r9v`, matching cockpit's `BACKEND_IDS` minus `comfyui`). Chosen name avoids colliding with the pre-existing, semantically different `runtimes.backend` column and with `routing_profile`'s unrelated "backend" concept.
2. **`compute_api`** (user-facing "Engine", the literal `vulkan | rocm` the requirement names) = the existing `runtimes.backend` CHECK column (`cpu/cuda/rocm/vulkan/metal/sycl/rpc/other`). This is the granularity the user's literal ask ("engine: vulkan/rocm") actually matches — reuse it as-is for the benchmark/Sankey-facing "Engine" dimension; do **not** try to make it carry the finer profile strings too.
3. **`runtime_profile_id`** = cockpit's full profile string (`amd-rocm-hipblaslt`, `halogen-strix-halo`, etc.) from `toolboxes.json`'s `runtime_profiles` map. This is **not** exposed as a benchmark/Sankey "Engine" axis — it is an internal field used only for toolbox deployment (looking up `engine_args` to pass to container creation, Technical plan §5). Keeping it separate from `compute_api` avoids conflating "which podman flags to use to launch this toolbox" (deployment-time, high cardinality) with "which coarse accelerator API this run used" (analysis-time, the axis the user actually asked to chart).

Cockpit's own `ContainerEngine` (`podman`/`docker`) is a **fourth, unrelated axis** ("which container tool wraps the process") — brainrouter already only supports podman/toolbox; this plan keeps that constraint and does not add Docker/Distrobox support. Flagged explicitly so nobody conflates any of "engine" (compute API), "engine" (container runtime), or "runtime_profile" (deployment recipe) while implementing.

### 2. Data model (Rust) — revised in v2 for lossless catalog parsing
v1 proposed a single closed 5-variant `ToolboxBackend`/`ModelPayload` enum. Both the Dory readiness and critic reviews independently flagged this as a genuine contradiction with requirement 1 ("future ones" + "exclude comfyui, not fail on it"): a closed enum either hard-fails `serde` on `comfyui`/an unknown 7th backend, or silently drops the whole catalog entry, neither of which satisfies "brainrouter never breaks when upstream adds a backend." v2 splits this into two layers:

```rust
/// Backend identifier exactly as it appears in the catalog JSON. Open/lossless:
/// unknown or excluded ids round-trip instead of failing to parse. NOTE: serde's
/// built-in `#[serde(other)]` fallback discards the original string, which is not
/// good enough here (we need the literal id for display/debugging) — this needs a
/// custom `Deserialize` impl (deserialize to `String` first, then match) rather
/// than a derive; called out explicitly so it isn't "discovered" mid-implementation.
#[derive(Clone, Debug, PartialEq, Eq, Serialize)]
pub enum CatalogBackendId {
    LlamaCpp, Ds4, Halogen, Vllm, R9v,
    Comfyui,          // parsed, never surfaced in any UI/API list per requirement 1
    Other(String),    // any future backend id upstream adds before brainrouter has code for it
}

/// The narrower, closed set of backends brainrouter can actually *execute* actions
/// for (create/update/pull/server-launch). `Comfyui` and `Other` convert to `None`
/// here — they can still be *listed* (via `CatalogBackendId`) but never acted on.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum SupportedServingBackend { LlamaCpp, Ds4, Halogen, Vllm, R9v }
impl TryFrom<&CatalogBackendId> for SupportedServingBackend { /* Comfyui | Other(_) -> Err */ }

pub struct RuntimeProfile { pub id: String, pub engine_args: Vec<String> } // from toolboxes.json.runtime_profiles
pub struct ToolboxDefinition {
    pub id: String, pub backend: CatalogBackendId, pub name: String, pub container_name: String,
    pub group: String, pub image: String, pub channel: Channel, pub maturity: Maturity,
    pub description: String, pub runtime_profile: String, pub supports_load_mode: bool,
    pub features: ToolboxFeatures, pub backend_config: Option<serde_json::Value>, // recommended_use etc, kept loose
}
pub struct ToolboxFeatures { pub interactive: FeatureState, pub server: FeatureState, pub models: FeatureState }

/// Catalog-level, always parses (any backend). `payload` is only `Some` for the
/// five supported backends; comfyui/unknown entries keep their `raw` JSON for
/// display/debugging but are never converted into an actionable ModelPayload.
pub struct CatalogModelEntry {
    pub id: String, pub backend: CatalogBackendId, pub name: String,
    pub raw: serde_json::Value,
    pub payload: Option<ModelPayload>,
}
pub enum ModelPayload { LlamaCpp(LlamaCppModel), Ds4(Ds4Model), Halogen(HalogenModel), Vllm(VllmModel), R9v(R9vModel) }
```
`ModelPayload` stays a per-backend enum (v1's reasoning here held up under review) because `catalog/schema.py` proves the five supported backends' model entries are structurally incompatible (r9v needs pinned commit SHA + file-role manifest + PLE hash; halogen needs `.hgn` checkpoint/overlay/tokenizer_dir; llama_cpp needs `inference_profiles`/`toolbox_defaults`/`auxiliary_downloads`) — directly mirroring why cockpit itself uses `ai_toolbox_cockpit/backends/<backend>/` per-backend modules rather than one generic backend. What changed in v2 is only the *outer* layer (`CatalogBackendId`/`CatalogModelEntry`) becoming open/lossless so the catalog itself never fails to parse.

DB: no changes to `models`/`artifacts`/`runtimes`/`experiments`/`runs` are required for *toolbox management* itself (that's catalog + live podman state, not historical experiment data) — a new **non-benchmark** table set (below) tracks brainrouter's own toolbox/model inventory state; the *benchmark* schema changes are scoped separately (§7).

New tables (own migration, e.g. `migrations/0003_toolbox_catalog.sql`):
```sql
CREATE TABLE toolbox_catalog_snapshot (
  id TEXT PRIMARY KEY,           -- e.g. content hash of vendored catalog files
  models_json TEXT NOT NULL,     -- raw vendored models.json, kept verbatim for audit/debug
  toolboxes_json TEXT NOT NULL,  -- raw vendored toolboxes.json
  source_commit TEXT,            -- upstream ai-toolbox-cockpit commit this snapshot was synced from
  synced_at TEXT NOT NULL
);
```
This table needs an explicit owning store and migration-runner registration (see §7's migration-runner finding — the same hardcoded-version-gate problem applies to any new SQLite file/migration, not just the benchmark one; if this table lives in the *same* `benchmark.db`, it must be added to `src/benchmark.rs`'s version gate; if it lives in its own DB file, that file needs its own (much simpler, since it's new) migration runner. Decision needed — see Open questions.
Live container state (what's installed/running) is **not** persisted to SQLite — mirrors brainrouter's existing pattern of asking podman live (`toolboxes_list()`), which is already proven correct/simple; we extend that function rather than duplicating state into a DB table that could drift from reality.

### 3. Catalog sync mechanism — **vendor**, not periodic runtime fetch (unchanged from v1, reaffirmed)
Rationale (sycophant-challenged in Alternatives): the catalog JSON is schema-validated, versioned (`schema_version`), and used to *build UI forms and validation*, not just to display "is there an update." A runtime fetch means brainrouter's UI shape can change under a user's feet with no review, and it adds a hard runtime dependency on GitHub being reachable from the host, which the current design explicitly does not have (brainrouter works fully offline for everything except image-update checks). Proposal:
- A `scripts/sync-toolbox-catalog.sh` (or `xtask`) fetches `ai_toolbox_cockpit/assets/{models,toolboxes}.json` from a **pinned upstream commit/tag** of `kyuz0/ai-toolbox-cockpit`, validates them against a Rust port of the *shape* of `catalog/schema.py`'s checks (not a full port of the Python — just enough structural validation to fail loudly on a schema change we don't understand yet, **and, per §2, treating an unrecognized `backend` id as a warning, not a hard validation failure**, so the sync script itself doesn't become the thing that breaks lossless parsing), and writes them into `assets/cockpit-catalog/{models,toolboxes}.json` + the pinned commit SHA into a `assets/cockpit-catalog/SOURCE` file, committed to the brainrouter repo.
- A scheduled CI job (weekly, mirroring the cadence implied by cockpit's own CalVer auto-tagging) re-runs the sync script and opens a PR if the content changed, so a human reviews every catalog update before it ships — this is the brainrouter-repo-level analogue of `hub_toolbox_tag_dates()`, but for catalog *content* rather than *image tag freshness*.
- The **existing** `hub_toolbox_tag_dates()`/"update available" runtime check is kept as a true periodic *runtime* fetch (per the existing pattern) and is **extended** to loop over every vendored toolbox's `image` field instead of the one hardcoded string — this part legitimately should stay live/periodic, because "is a newer tag available" is inherently a live fact, not a reviewable content change. **New in v2, from critic finding #12 (important):** this fan-out from 1 image to N images/backends turns a currently-unbounded, no-timeout call (`hub_toolbox_tag_dates()`, `src/server.rs:1839-1858`, confirmed no timeout set today, and the dashboard polls `/api/toolboxes` every 30s per `main_dashboard.html:2602-2608,2832-2835`) into a real scalability/rate-limit/registry-compatibility problem. Proposed fix: a small cached async image-metadata service — per-image TTL cache (e.g. 15–30 min), bounded concurrency, an explicit per-call timeout (reusing the existing `VERSION_CLIENT` pattern at `src/server.rs:71-79`, which supports per-call-site timeouts but isn't itself timeout-defaulted), a real OCI image-reference parser (not a Docker-Hub-only assumption), and a "freshness unavailable" status for registries the parser/adapter doesn't recognize, instead of silently treating every image as Docker-Hub-hosted.

### 4. Config.json merge design — revised in v2 (narrower default, explicit path-mismatch safeguard)
v1 proposed unconditional bidirectional read-modify-write against the shared file as the default. The Dory critic argued (and this document now agrees) that doing continuous, automatic background read-modify-write against a file a second live process (interactively-run cockpit) may be concurrently writing — with **no** lock, version, or conflict signal on either side — is a materially riskier promise than the user's "ideally merging" phrasing requires, especially compounded by the HOME/config-path mismatch risk below. v2 default:
- **Phase 1 (this rollout): read-only import.** brainrouter reads `~/.config/ai-toolbox-cockpit/config.json` (same path resolution as cockpit: `$XDG_CONFIG_HOME` default `~/.config`) at startup/on-demand and honors `active_platform`/`backends.*.default_toolboxes` to seed its own UI defaults. Never writes automatically.
- **Explicit, user-triggered write action** ("Apply to cockpit config" button/CLI flag): when the user explicitly asks brainrouter to persist a change back, brainrouter does a single reload-verify-write (load current file → apply only the specific key(s) the user changed → write back atomically, same `.tmp` + `chmod(0o600)` + `rename()` contract as cockpit's `save_settings()`) — an explicit user action in the loop, not a hidden background sync. brainrouter **only ever touches the same keys cockpit already owns** (`active_platform`, `backends.<id>.default_toolboxes.<platform_id>`) plus, if needed later, a namespaced `backends.<id>.brainrouter` sub-object — never a top-level brainrouter-only key, and all other keys round-trip untouched.
- **True bidirectional live-merge (both tools writing at any time, safely) is explicitly deferred** pending either upstream cockpit adopting a lock/version field, or the user accepting the documented last-writer-wins risk for the automatic case specifically (this is now an Open question, not a v1-style "accepted risk" footnote — the user should decide whether Phase-1-read-only is an acceptable interpretation of "ideally merging," or whether they want the riskier automatic bidirectional behavior anyway).
- **New in v2 — config path/owner mismatch:** the daemon's process `$HOME` (which can silently fall back to `/root` when unset — already partially flagged by an existing startup warning at `src/daemon.rs:284-286`) is not guaranteed to match the interactive user's `$HOME` that actually runs cockpit, especially if brainrouter runs as a system service under a different user/account. If they differ, "shared config" silently becomes two different files with the same name and no error. Proposed mitigation: (a) surface the **effective resolved path** (and its owning UID, if determinable) for the config file prominently in the dashboard/CLI wherever cockpit-config state is shown, so a mismatch is visible rather than silent; (b) add an explicit config-path override setting (env var or brainrouter YAML config key) for operators who know brainrouter and cockpit run under different accounts; (c) refuse to *create* the file autonomously when running as a system-managed service account (heuristic: e.g. UID 0 or a dedicated service UID) — only auto-create it for an interactively-invoked brainrouter process.
- **Resolved in v3 (was Open question 2):** TOP SESSION selected option (a) — if `~/.config/ai-toolbox-cockpit/` (or its resolved equivalent, per the path-mismatch mitigation above) does not exist on a given host, brainrouter **leaves it absent** and operates entirely on its own built-in defaults until cockpit itself initializes that directory (by being run at least once on that host). brainrouter never auto-creates the directory or an empty `config.json`, and never refuses to manage toolboxes because the directory is absent — absence is a normal, first-class state, not an error. This directly answers "what happens if cockpit isn't installed on a given host" from the original requirements: brainrouter is fully functional without it, and the explicit single-write "apply" action (above) is simply unavailable/disabled (not erroring) until the directory exists.

### 4a. PR3 API contract and implementation decisions (added during PR3 implementation, 2025-09-17)
Mirrors §5c's format for PR2. Records the concrete shape PR3 landed in.

**Module and types.** `src/cockpit_config.rs` implements exactly the Phase-1 contract from §4: `config_path()` (same `$XDG_CONFIG_HOME`-default-`~/.config` resolution brainrouter already uses elsewhere, e.g. `src/config.rs::default_config_path()`/`src/daemon.rs:412` — reused verbatim, not reinvented); `CockpitConfig { active_platform: Option<String>, backends: BTreeMap<String, BackendSettings>, extra: Map<String,Value> }` and `BackendSettings { default_toolboxes: BTreeMap<String,String>, extra: Map<String,Value> }`, both using `#[serde(flatten)] extra` so any key cockpit itself understands but brainrouter doesn't model is preserved losslessly on round-trip rather than dropped by a write. `CockpitConfigStatus { path, exists, owner_uid, config: Option<CockpitConfig>, error: Option<String> }` is the read snapshot — `exists: false, error: None` is the normal "cockpit not installed here" state (§4's resolved Open question 2); `error: Some(..)` is reserved for the file existing but failing to parse/read, a materially different, surfaced-as-a-problem state.

**Write path.** `apply_default_toolbox(backend_id, platform_id, toolbox_id)` / `apply_active_platform(platform_id)` both do reload-verify-write: load whatever is on disk *right now* (not a possibly-stale in-memory copy), mutate only the one key the caller asked to change, write back. This narrows, but does not eliminate, the race against a concurrently-running interactive cockpit process (no lock or version field exists on either side to eliminate it fully — consistent with §4's already-documented, user-confirmed acceptance of this residual risk for the explicit-single-write case). Writes use brainrouter's own established atomic-write convention (`src/routing_profile.rs::atomic_write()` pattern: UUID-suffixed temp file in the same directory, `mode(0o600)`, `sync_all`, then `rename()`), not a literal copy of cockpit's own `config.tmp` naming — only the final atomic rename matters for safety, and reusing brainrouter's existing helper pattern is preferable to introducing a second, subtly different one.

**`ApplyError` and its HTTP mapping.** Three variants: `NotAvailable` (the cockpit config directory doesn't exist on this host — maps to **HTTP 404**, distinct from a generic 500, since this is an expected, non-error "not installed" state at the domain level even though the specific write action can't proceed), `Read(String)` (file exists but failed to parse/read before the write — **HTTP 500**), `Write(io::Error)` (the atomic write itself failed — **HTTP 500**). All three implement `Display` and are surfaced verbatim in the JSON error body, matching the existing `ErrorResponse` shape used by every other mutating route in `server.rs`.

**Legacy-config migration is explicitly NOT replicated.** Cockpit's own `settings.py` has a `_legacy_settings()` fallback that migrates old `.llama-cockpit.conf`/`.ds4-cockpit.conf` files if `config.json` itself doesn't exist yet. brainrouter's `load()` does not reach for these files — a missing `config.json` is simply "absent" from brainrouter's perspective, full stop. **Reason:** migrating cockpit's own legacy formats is cockpit's concern to complete (or not) on its own upgrade path; brainrouter re-implementing that migration would create a second, independently-drifting copy of logic that isn't brainrouter's to own, and cockpit already handles it correctly whenever it next runs interactively on that host.

**API routes (all under the existing destructive-route gate for POST; GET is unauthenticated-safe, read-only):**
- `GET /api/cockpit-config` → `CockpitConfigStatus` (the full read snapshot, including the resolved path — always returned even when `exists: false`, satisfying §4's "surface the effective resolved path prominently" mitigation).
- `POST /api/cockpit-config/default-toolbox` — body `{backend_id, platform_id, toolbox_id}` → `{"status":"ok"}` on success, 404 on `NotAvailable`, 500 otherwise.
- `POST /api/cockpit-config/active-platform` — body `{platform_id}` → same response contract.

**Effective default toolbox (dashboard).** The dashboard's "which toolbox is the default for backend X" star/pick affordance combines both layers explicitly, in this precedence: cockpit's own `config.json` override (`backends.<id>.default_toolboxes.<platform_id>`) first; falling back to the catalog's own `Platform.defaults[backend_id]` recommendation (PR2, §2) only when cockpit has no override or its config is absent entirely. Picking a non-default toolbox in the UI calls the `default-toolbox` apply route directly — no separate "confirm" dialog, since this write only ever touches one shallow key and is trivially reversible by picking a different toolbox afterward.

### 5. Native Rust create/update/pull for ds4/halogen/vllm/r9v (mirroring cockpit's `runtime/` patterns, not porting Python)
- Generalize today's `toolboxes_list()`/`upgrade_toolbox()` to operate over the vendored catalog's `toolboxes[]` array instead of one hardcoded image/container name; container listing gains the `CreatedAt` field (adopt cockpit's `ps -a --format "{{.Names}}|{{.Image}}|{{.Status}}|{{.CreatedAt}}"`, pipe-delimited to avoid tab-in-status edge cases) so per-toolbox local-vs-remote freshness works for all of them, not just one.
- **New in v2 — container ownership labeling:** every toolbox container brainrouter creates gets podman labels (`io.brainrouter.managed=true`, `io.brainrouter.catalog_id=<toolbox id>`, `io.brainrouter.catalog_revision=<snapshot id>`) so brainrouter only ever mutates/deletes containers it created. A pre-existing container (e.g. one cockpit created before brainrouter was ever run against this host) is shown as **unmanaged** and requires an explicit "adopt" action before brainrouter will touch it — prevents brainrouter silently deleting/recreating a container the user is actively using via cockpit.
- **New in v2 — operation locking and idempotency:** create/update/delete become **destructive** operations (see §9, Security) that must not run concurrently against the same container name (single in-flight-mutation-per-name lock, e.g. a `tokio::sync::Mutex` keyed by container name, or a DB-backed lease if multiple brainrouter processes can front the same podman socket). Status responses are idempotent: "already running"/"already stopped"/"already deleted" are treated as success, not error, matching how a retried dashboard click should behave.
- **`create`/`update` sequence — flagged as unverified, needs a spike (Dory critic blocking finding #5):** v1 asserted the existing `pull → rm --force → toolbox create --image` sequence (already proven for the one llama.cpp/Vulkan image, which needs no extra flags) generalizes to every backend by parameterizing `--image` and threading `runtime_profile.engine_args` (raw podman flags like `--device`, `--group-add`, `--security-opt`, `--env`) into the same `toolbox create` invocation. **This has not been verified against real `toolbox` CLI behavior.** `toolbox create` (the Fedora/RedHat toolbox wrapper, currently invoked with only `--image <image> <name>` at `src/server.rs:2283-2293`) has its own, more limited flag surface than a bare `podman create`/`podman run`, and it is not confirmed here whether it forwards arbitrary podman flags like `--device`/`--security-opt`/`--group-add` at all. Before claiming parity for any backend that needs non-default `engine_args` (R9V's ROCm device passthrough is the clearest case), this needs a concrete spike: run `toolbox create --help` / test against a real host, and if `toolbox` cannot forward the needed flags, switch the **deployment primitive** for non-default-profile toolboxes from the `toolbox` wrapper to direct named `podman create`/`podman run` (which definitely supports these flags natively), keeping `toolbox` only where its opinionated defaults are sufficient (the current Vulkan/default-profile case). This decision is deliberately left open pending that spike — see Open questions and Alternatives.
- **"Enter" (cockpit's interactive Toolbx/Distrobox shell) has no web-dashboard equivalent** — a browser cannot attach to a live TTY the way a TUI can suspend itself. Proposed: brainrouter's dashboard exposes the exact `toolbox enter <name>` command as copy-to-clipboard text. The CLI (`brainrouter cli toolbox enter <name>`) can genuinely exec it **only when the CLI and the daemon/container engine are on the same host and user context** — **new in v2**, correcting a gap the Dory critic found: since the CLI can target a remote daemon via `--url` (`src/cli.rs:5-6,23-32,339-347`), blindly `exec`ing `toolbox enter` locally when the CLI is talking to a *remote* daemon would enter the wrong (local, likely nonexistent) container, or fail confusingly. The CLI must detect same-host-same-user (e.g. comparing to a UDS-only invocation, or an explicit `--url` pointing at `localhost`/the local UDS) before offering to exec directly; otherwise it prints the exact command as non-executable copy/paste guidance, identical to the dashboard's behavior. Never silently offer a false remote-TTY capability.
- Server-mode launch (per-backend `llama-server`/DS4/vLLM/R9V/Halogen command construction) is **not** cockpit's foreground-Popen-with-Ctrl+C-cleanup model — brainrouter is a long-running daemon, so server-mode toolboxes should be launched as **detached, named, brainrouter-managed containers** (`podman run -d --name <container> ...`) that brainrouter can start/stop/status via the dashboard, analogous to how brainrouter already manages the primary llama-swap-fronted server, rather than a foreground process the dashboard process would have to babysit. This is a genuine architecture divergence from cockpit and is called out as a decision needing sign-off (Open questions) since it changes the operational model (who kills the container if brainrouter restarts, log capture, etc.) — mitigated by the ownership labeling above (brainrouter reconciles/adopts its own previously-created named containers on startup rather than assuming a clean slate).
- Each backend gets its own command-builder module (`src/toolbox_catalog/backends/{ds4,halogen,vllm,r9v}.rs`) mirroring cockpit's `backends/<backend>/` split — **not** shared generic code — because schema.py already proves the five backends need different required fields/validation, and README confirms "the vLLM catalog imports the toolbox's model launch recipe rather than replacing it with generic defaults." Phase order (see Rollout) starts with the backend-agnostic Toolboxes-tab parity (works for all 5 without per-backend server-launch code), then adds server-launch command builders one backend at a time.

### 5a. The `--name` fix, precisely (do first, small, independent — but the mechanism is corrected in v2)
Re-verified directly against `src/server.rs:1886-1909`: the version-check runs `podman run --rm <image> llama-server --version` wrapped in `tokio::time::timeout(Duration::from_secs(PODMAN_VERSION_TIMEOUT_SECS /* 15 */), child)` with `.kill_on_drop(true)` set on the `Command`. On a *normal* exit, `--rm` alone is sufficient — the Dory critic is right that a missing `--name` does not, by itself, explain a leak on the happy path. The credible leak mechanism is the **timeout branch**: when the 15s timeout fires, `kill_on_drop` sends a kill signal to the `podman run` client process; if that signal terminates the client before it completes its own `--rm`-triggered container teardown, the underlying container can be orphaned — and because it was never given a name, it surfaces in `podman ps -a` under a random pet-name with no way to correlate it back to "the version check that timed out." Fix (both parts needed):
1. Add a deterministic `--name brainrouter-version-check` (or per-invocation-unique if concurrent version checks are possible — needs a decision, see Open questions) **and** `--replace` (atomically removes any stale same-named container first, so a previous orphan doesn't cause a "name already in use" error on the next check).
2. On the timeout branch specifically, after `kill_on_drop` fires, issue a best-effort `podman rm -f <name>` before returning the timeout error, so a timeout-orphaned container is actively cleaned up rather than merely "now easier to find manually."
Regression test must **force the timeout branch** (e.g. inject an artificially low timeout against a slow/blocked command, or a test double) and assert no container survives — v1's proposed test ("run the check twice, assert count unchanged") only exercises the normal-exit path, which the critic correctly noted was never the reported failure mode in the first place.

### 5b. Routing/provider integration — new required prerequisite (Dory critic blocking finding #4)
v1 assumed that once a server-mode toolbox container is running, the Sankey diagram and benchmark system could simply be told to reflect its "backend"/"engine" as first-class dimensions of real traffic. This is not true today: `src/daemon.rs:305-320` constructs the single shared `Router` from exactly `classifier`, `manifest`, and `llama_swap` — there is no generic "list of providers" concept, and `RouteEvent` (`src/routing_events.rs:30-82`) only ever carries data about requests that flow through that fixed set. A detached toolbox-server container launched from the new Toolboxes/Server-mode UI is, as of today, **completely invisible to the router** — starting a DS4 server through brainrouter's new UI does not make brainrouter route any request to it.
Proposed minimal fix (deliberately **not** a full routing-parity rewrite, which is out of scope — see Non-goals): introduce a lightweight **"serving identity" registration** — when a toolbox server-mode container starts, it registers a `{toolbox_backend, compute_api, runtime_profile_id, endpoint}` record that either (a) is added as an additional llama-swap-compatible upstream (if the launched server speaks an OpenAI-compatible API, which llama.cpp/vLLM do, and DS4/Halogen need to be checked against), making it genuinely routable and therefore honestly populate `RouteEvent`/Sankey/benchmarks, or (b) for backends that can't be added as a routable upstream yet, is exposed only as **standalone server status** (dashboard shows it running, no Sankey/routing claim is made about it) — explicitly not faking Sankey data for traffic that never went through the router. Which backends land in (a) vs (b) first is a per-backend implementation detail resolved during the Rollout's server-mode phases, not asserted uniformly here. This sub-phase is a **prerequisite** for the Sankey PR (Rollout is reordered accordingly) — shipping the Sankey columns before this exists would either show columns that are always empty/n-a, or (worse) require a stopgap "fake it" data source, neither of which was the ask.

### 5c. PR2 API contract and implementation decisions (added during PR2 implementation, 2025-09-17)
This section resolves the "needs its own API-contract note before PR2 starts" gap flagged in earlier drafts, and records the decisions made while implementing the typed catalog layer + generalized Toolboxes tab.

**Typed catalog layer** landed in `src/toolbox_catalog/{types,models}.rs` exactly as sketched in §2, with one refinement: rather than modeling every optional per-backend field (llama_cpp's `inference_profiles`/`dspark`/`mtp`/`vision_projector`/`auxiliary_downloads`, vllm's `extra_flags`/`env`/`ctx`, etc.), each `ModelPayload` variant strongly types only the fields proven common across **every** vendored entry for that backend (verified by intersecting keys across all entries, not just inspecting one), and captures the rest via `#[serde(flatten)] extra: serde_json::Map<String, Value>`. This keeps `ToolboxDefinition::backend_config`'s "kept loose" philosophy consistent across the whole typed layer instead of applying it to one field and guessing shapes everywhere else. `CatalogModelEntry.raw` still holds the full original JSON regardless, so nothing is lost even where `extra` is used instead of a named field.

**Migration decision (resolves part of §2's "Decision needed"):** `migrations/0003_toolbox_catalog.sql` / `toolbox_catalog_snapshot` is **not created in PR2**. PR2's toolbox/container management needs no new persisted state — the catalog is embedded at compile time (PR1) and container state is read live from podman (unchanged pattern, extended to all 5 backends), matching §2's own observation that "live container state is not persisted to SQLite." A `toolbox_catalog_snapshot` table only becomes useful once something needs to *look back* at which catalog snapshot was in effect for a historical action (the clearest case: §7's benchmark toolbox-family dimension anchoring a run to the snapshot that was current when it happened). That need is revisited at Rollout PR 5, not introduced speculatively here.

**Container ownership labels and the adopt flow.** Podman labels are set at container-creation time and cannot be attached to an already-created container in place — there is no `podman label add`. This has a direct consequence for "adopt": adopting a pre-existing, unmanaged container that happens to match a catalog `container_name` cannot mean "tag it in place." Two options were available: silently treat any name/image match as brainrouter-owned (rejected — this is exactly the risk §5 called out, brainrouter could then delete/recreate a container the user is actively driving via cockpit without ever having asked), or make "adopt" an explicit **recreate-in-place** operation that removes the existing container and immediately recreates it from the *same* image (no forced pull) with brainrouter's ownership labels attached. This PR takes the second option: `POST /api/toolbox-containers/{name}/adopt` is really "recreate this container, unchanged image, now labeled" — it is destructive (briefly stops/removes the container) and is presented to the user as such, not as a free no-op relabel. `update` is the same recreate primitive parameterized to also pull the newest image first; `create` is the same primitive with no prior container to remove.

**`toolbox create --label ...` support is unverified — attempted, fails loud, not silent (narrower cousin of §5's finding #5).** Creation/recreation keeps using `toolbox create --image <image> <name>` (unchanged primitive, still the one proven-working invocation, and needed so containers stay compatible with the future `toolbox enter` "Enter" parity feature from §5 — switching to a bare `podman create` would define away that compatibility without ever deciding to). Whether `toolbox create` forwards a `--label k=v` flag through to the underlying `podman create` the way the ownership labels need is **not verified in this environment** (no `toolbox`/`podman` binary available to test against, same limitation as the R9V spike). This PR's stance: pass `--label io.brainrouter.managed=true --label io.brainrouter.catalog_id=<id> --label io.brainrouter.catalog_revision=<rev>` to every `toolbox create` call and let it fail loudly (a normal `500` with the real `toolbox`/podman stderr, exactly like any other create failure) if the flag isn't supported — this is a visible, immediate, unambiguous failure a real host will surface the first time PR2 runs there, not a silent mis-tagging. If it does turn out to be unsupported, the fallback (switching the creation primitive to `podman create` for brainrouter-managed containers, accepting the `toolbox enter` compatibility question that comes with it) is deferred to whoever hits that failure first, not preemptively built here. `catalog_revision` is a short (12 hex char) SHA-256 prefix over the two vendored catalog files' bytes (`toolbox_catalog::vendored_catalog_revision()`), not a reference to a persisted snapshot row, consistent with the "no new DB table in PR2" decision above.

**Ownership determination without labels on unmanaged containers:** since an *unmanaged* container has no `io.brainrouter.*` labels by definition, "managed" is computed as "container exists AND carries `io.brainrouter.managed=true`" (read via `podman inspect --format '{{ index .Config.Labels "io.brainrouter.managed" }}'`), never inferred from name/image matching alone.

**Per-container locking.** A `tokio::sync::Mutex<()>` keyed by container name, held in `AppState` as `toolbox_container_locks: Mutex<HashMap<String, Arc<tokio::sync::Mutex<()>>>>` (outer std lock only guards inserting a new per-name entry; the actual create/update/delete/adopt work holds the per-name async lock for its duration). A lock is acquired for the container's `container_name` before create/update/delete/adopt run, and released after; a second concurrent request for the same name queues behind it rather than racing `podman`/`toolbox` invocations against each other. Different container names never contend.

**API routes (all under the existing destructive-route gate for POST; GET routes are read-only and ungated, matching `/api/toolboxes` today):**
- `GET /api/toolbox-catalog` → `{ "schema_version": <i64>, "toolboxes": [ToolboxDefinition, backend restricted to the 5 supported ids — comfyui entries are omitted entirely, not merely unsupported-flagged], "platforms": [Platform] }`. Read-only view of the vendored catalog, scoped to what brainrouter can act on.
- `GET /api/toolbox-containers` → `{ "containers": [{ "container_name", "toolbox_id": string|null, "backend": string|null, "image", "running": bool, "status", "created_at", "local_created", "latest_created", "update_available": bool, "managed": bool }] }`. Generalizes today's `/api/toolboxes`: instead of filtering to a `llama-*` name prefix, every podman container (`podman ps -a`) is correlated by exact name against every supported-backend toolbox's `container_name` in the catalog; a podman container matching no catalog `container_name` is omitted (same effective behavior as today's prefix filter, generalized to be catalog-driven instead of hardcoded). `toolbox_id`/`backend` are `null` only in the (should-not-normally-happen) case a name matches but the matching catalog entry was somehow not resolvable; in practice they mirror the matched `ToolboxDefinition`.
- `POST /api/toolbox-containers` body `{ "toolbox_id": "<catalog id>" }` → creates the named container for that catalog entry (must not already exist; use `update`/`adopt` otherwise). Locked per container name.
- `POST /api/toolbox-containers/{name}/update` → pulls the catalog entry's current `image` and recreates. Locked per container name.
- `POST /api/toolbox-containers/{name}/delete` → force-removes the container. Idempotent: removing an already-absent container returns `200 {"status":"ok","message":"already removed"}`, not an error.
- `POST /api/toolbox-containers/{name}/adopt` → recreate-in-place as described above (no pull). Rejects (`409`) if the container is already `managed`.
- `GET /api/toolbox-models` → `{ "schema_version": <i64>, "backends": [ModelBackendCatalog, restricted to the 5 supported backend ids] }`. Read-only catalog view; no download/install action exists yet (that's Rollout PR 6's explicitly-flagged-as-undesigned download lifecycle, Open question 5).
- All five mutating routes are added to the destructive-route allowlist (`src/server.rs`'s `is_destructive` check) via a `path.starts_with("/api/toolbox-containers")` clause (mirrors the existing `/api/upgrade/` pattern) rather than being enumerated one-by-one, since new sub-paths under this prefix are expected as this rollout continues.

**Existing routes kept, not removed, for backward compatibility this PR:** `/api/toolboxes` and `/api/upgrade/toolbox[/…]` continue to work exactly as before (llama_cpp/Vulkan only) — they are not deleted in PR2. The dashboard's Toolboxes tab is switched over to the new catalog-driven routes; the old routes remain callable (e.g. by any external script depending on them today) but are noted as superseded, to be removed in a later cleanup PR once nothing depends on them.

### 6. Sankey diagram extension
- Add fields to `RouteEvent` (`src/routing_events.rs:30-82`, corrected citation) — `toolbox_backend: Option<String>` and `compute_api: Option<String>` (naming per §1) — populated only for requests that actually flow through the router-registered serving identities from §5b; everything else (cloud-routed requests, non-toolbox-served requests, and — until §5b lands for a given backend — server-mode containers not yet wired in) falls back to an explicit bucket.
- **New in v2 — explicit bucket vocabulary, not a single ambiguous "n/a"/"cloud" catch-all** (Dory critic nit, adopted because it materially affects Sankey readability): use distinct, stable values — `cloud` (routed to a cloud/hosted provider), `local_unmanaged` (served by a local process/container brainrouter didn't create or isn't tracking), `external` (a toolbox-like backend the user pointed brainrouter at manually, outside the catalog), `unknown` (genuinely indeterminate) — rather than collapsing all of these into one bucket for UI convenience, which would misrepresent genuinely different situations as the same thing.
- **New bucket value, added while reconciling this section with the v3 Rollout/Decision log (which already referenced it as "§6 vocabulary" without it actually being defined here — fixed as a doc-consistency correction, not a new design decision):** `status_only` — a catalog-recognized backend (ds4/halogen/vllm/r9v) whose own server-mode + routing-integration PR (Rollout PRs 7-11) hasn't landed yet. Distinct from `unknown` (genuinely indeterminate, e.g. malformed/legacy event data) and from `external` (a backend brainrouter was pointed at manually, outside the catalog entirely) — `status_only` specifically means "this backend is a known, catalog-recognized citizen of this integration, brainrouter just doesn't route to it yet."
- `SK_COLS` grows to 6 entries; `skKeysOf()` grows to return `c4`/`c5`; `keys` array, `skColorFor()`'s per-column color map, and `matchesSankey()`'s `'c' + col` lookup already generalize (confirmed: the click-filter mechanism is column-index-generic) — only the hardcoded literal column count/positions and the color table need new entries, not a redesign.
- Column x-positions and `SK_TOTAL_W` need recomputing for 6 columns; propose measuring/laying out columns proportionally from `SK_COLS.length` instead of hardcoded per-column `x` literals, so a 7th future column doesn't require another hand-edit of pixel constants — flagged as a small refactor bundled into this change since we're touching every line that assumes 4 anyway.
- Nodes with no backend/engine get one of the explicit buckets above (never silently dropped), so the diagram's total flow count still balances across all 6 columns.
- **Dependency (superseded in v3 — see Decision log):** ~~this PR is now explicitly ordered after §5b (routing/provider integration) in the Rollout~~. v3 instead ships this PR in Wave 2 (Rollout PR 4), immediately meaningfully populated for the traffic that's *already* routable today (llama_cpp/manifest, which needs no new prerequisite), with every other catalog backend rendering under the new `status_only` bucket above until its own server-mode + routing-integration PR lands — never faked as routed traffic, never blocking this PR's own delivery.

### 6a. PR4 implementation decisions (added during PR4 implementation, 2025-09-17)
Mirrors §4a/§5c's format. Records the concrete shape PR4 landed in.

**`RouteEvent` fields and the derivation function.** `src/routing_events.rs` gains `toolbox_backend: Option<String>` / `compute_api: Option<String>` (both `#[serde(skip_serializing_if = "Option::is_none")]`, so old stored/exported events without these fields still round-trip and the JSON payload doesn't grow for the common cloud/manifest case) plus a single derivation function, `derive_serving_identity(effective_provider: Option<&str>) -> (Option<String>, Option<String>)`, called from all 3 production `RouteEvent` emit sites in `src/router.rs::route()`. Today it is a hardcoded match: `Some("llama-swap") => (Some("llama_cpp"), Some("vulkan"))`, everything else (`Some("manifest")`, `None`) → `(None, None)`. This is a **factually accurate** derivation for 100% of today's real traffic, not a placeholder — `src/daemon.rs:305-320` confirms the router has exactly two providers today (`manifest`, `llama_swap`), and `llama_swap` is unconditionally backed by the one hardcoded Vulkan/llama.cpp toolbox image. **This function must be replaced, not extended, once §5b's real per-request serving-identity lookup lands** — adding further `Some("some-new-provider-string") => ...` arms as each new backend is wired in would reproduce the exact "invisible to the router" gap §5b exists to close, just moved into a string-match table instead of solved properly.

**Dashboard bucket vocabulary, exactly as implemented.** `skServingBucket(e, field)` (`main_dashboard.html`) resolves, in order: the real field value if present (`toolbox_backend`/`compute_api`) → `cloud` if `effective_provider === 'manifest'` → `unknown` if `effective_provider` is absent (the request errored before reaching any provider) → `local_unmanaged` otherwise. This intentionally implements only 3 of §6's 5 documented bucket values from today's 2 real `effective_provider` strings — `external` and `status_only` are **not reachable** by this function as written, and that is expected, not a bug: both only become populated once a later Rollout PR (6-11) registers an additional backend's serving identity or an explicit manual-external-backend concept exists. No stub/dead branch for them was added to the JS; they will be reachable the moment the Rust side can actually produce them, without a dashboard change, since the bucket function's fallback logic already covers "field absent" generically.

**Column layout: computed, not hardcoded.** `SK_COLS` moved from `{name, header, x}` (4 entries, absolute pixel `x`) to `{name, header, width}` (6 entries: harness=284, session=420, routing=320 — the three unchanged columns keep their old effective deltas so nothing shifts visually — plus new backend=190, engine=170, model=60-trailing-margin). A `layoutSankeyColumns()` IIFE cumulative-sums these into real `x` values from a 16px left margin, and `SK_TOTAL_W` is now `SK_COLS[last].x + SK_COLS[last].width` instead of a hardcoded `1232` literal — a 7th future column is now a one-line addition to the array, not a set of hand-recomputed pixel constants. The `<svg>` tag's initial `viewBox`/`width` placeholder attributes were updated to the new computed total (1460) purely to avoid an initial-paint flash before the first `updateSankey()` call overwrites them; they carry no independent meaning.

**Two other hardcoded 4-column sites found and fixed beyond §6's own list.** Confirming §6's claim that "only the literal column count/positions and the color table" were non-generic required actually searching for every hardcoded `[k.c0, k.c1, k.c2, k.c3]`/`c3`-as-last-column assumption, not just the ones §6 named. Two more were found this way: the card-click highlight handler's `const cols = [k.c0, k.c1, k.c2, k.c3]` (path-highlighting a card's full sankey path) and a stale comment referring to "a card's four column keys" — both updated to the 6-column form/generic wording. `matchesSankey()`, `skComputePath()`, and `updateSankey()`'s node/link/`linkCounts` construction were re-confirmed to already be fully `NC`/`SK_COLS.length`-generic, exactly as §6 asserted.

**Color map.** New column-3 (backend) and column-4 (engine) entries in `skColorFor()`'s `map`: real backend ids get stable distinct colors (`llama_cpp`/`ds4`/`halogen`/`vllm`/`r9v`), real engine ids likewise (`vulkan`/`rocm`), and a shared `SK_SERVING_BUCKET_COLORS` object (muted grey for `local_unmanaged`/`unknown`/`status_only`, a distinguishing red for `external` since it represents a deliberate manual override worth visually flagging, purple for `cloud` matching the existing routing-column cloud color) is spread into both column maps so the same bucket value reads consistently regardless of which of the two columns it appears in. Column 5 (model, shifted from its old index 3) keeps the pre-existing empty-map/hash-palette fallback unchanged.

**Verification.** `cargo build --all-targets` clean; `cargo test --lib` → 209/209 (207 + the 2 new `derive_serving_identity` unit tests); `cargo clippy --locked --all-targets` → no new warnings beyond the same pre-existing set (server.rs:329,1216,3111; daemon.rs:132); `scripts/check-html-js.sh` → all 4 templates OK.

### 7. Benchmark schema/UI extension
- **No new column for "Engine"** — reuse `runtimes.backend` (already `cpu/cuda/rocm/vulkan/metal/sycl/rpc/other`), per §1's three-axis resolution; audit and fix any import/ingest path that isn't currently populating it correctly for toolbox-served runs (needs verification during implementation, not assumed here).
- **New dimension needed: "which toolbox family served this run"** — **left as an explicit open design decision in v2, not asserted**, because the Dory critic showed the v1 approach (`ALTER TABLE runtimes ADD COLUMN toolbox_family ...`) runs into `runtimes`' existing `NOT NULL` llama.cpp-specific columns (`repository`, `fork_name`, `commit_sha`, `compiler`, `compiler_version` — `migrations/0001_benchmark_explorer.sql:50-64`) and its existing uniqueness constraint, neither of which was designed with non-llama.cpp-fork backends in mind. Two candidate approaches, presented for a decision rather than picked unilaterally:
  - **(a) Widen `runtimes`:** make the llama.cpp-specific columns nullable, widen the uniqueness constraint to include `toolbox_family`/`toolbox_id`, and add a migration that backfills/validates existing rows. Simpler schema (one runtimes concept), but changes the meaning/constraints of an existing, populated table and requires care around the `run_summary` view and every existing query that assumes those columns are always present.
  - **(b) New `serving_runtimes` table:** keep `runtimes` exactly as-is (llama.cpp/native-binary benchmark identity, unchanged), add a separate table for non-llama.cpp-fork serving stacks (ds4/halogen/vllm/r9v), and give `experiments` a second, nullable FK (`serving_runtime_id`) alongside the existing `runtime_id`. Avoids touching a stable existing table's contract, at the cost of two similar-but-different "what served this run" concepts existing side by side (more surface area, clearer boundaries).
  - Both approaches need `toolbox_id`/`toolbox_family` anchored against a **catalog revision** (`catalog_snapshot_id`/`source_commit`, from §2's `toolbox_catalog_snapshot` table), not just a bare string id — the Dory critic correctly noted that catalog ids/entries can be renamed or removed upstream over time, and a historical benchmark row referencing a since-renamed id becomes ambiguous without an anchor to the exact catalog snapshot that was in effect when the run happened.
- **Migration-runner wiring is a required, concrete implementation step, not a side effect of adding a `.sql` file** — re-verified in v2 directly: `src/benchmark.rs:1895-1953` hardcodes acceptance of schema versions 1–2 only, with exact table-count checks (`required_v1_tables != 5`, `required_tables != 6`) and returns `BenchmarkError::Validation` otherwise. Whichever approach (a)/(b) is chosen, this function's version gate must be extended to recognize the new version (and its own new expected-table/column-count check) — the migration literally will not run without this, regardless of how correct the `.sql` file is.
- `RuntimeDefinition` (`src/benchmark.rs`) and/or a new `ServingRuntimeDefinition` gains the fields chosen above; `RunSummary`/`run_summary` view exposes them; `/api/benchmarks/filters` and `/api/benchmarks/runs` gain corresponding filter params.
- Import paths (`llama-bench` ingest and the generic `/api/benchmarks/ingest`) accept optional `toolbox_family`/`toolbox_id` in the experiment/runtime template payload (not derived from the benchmark tool's own JSON output, which has no concept of it) — matches how `backend`/engine is already supplied via the runtime template today, not extracted from `llama-bench.json`. Ingest must also be re-checked against the widened/second-table uniqueness semantics chosen above so `INSERT OR IGNORE` cannot silently drop a row that should have been distinct (§ current-system finding, `src/benchmark.rs:2248-2273`).

### 7a. PR5 implementation decisions (added during PR5 implementation, 2025-09-17)
- **Resolved §7's open (a)/(b) choice: option (b), new `serving_runtimes` table, refined.** `runtimes.repository`/`fork_name`/`commit_sha`/`compiler`/`compiler_version` are `NOT NULL` and are a *source-build* identity (which fork/commit of llama.cpp was compiled); ds4/halogen/vllm/r9v naturally have a *container-image* identity instead (which toolbox/image served the run) — a genuinely different concept, not just a DB-mechanics inconvenience. Widening `runtimes` would force those non-llama.cpp-fork backends to either fabricate meaningless build-identity values or make five previously-mandatory columns nullable for every future consumer, including llama.cpp rows. A new table keeps both identities' contracts intact.
- **`experiments.runtime_id` stays `NOT NULL` and completely unchanged; `experiments.serving_runtime_id` is a new, nullable column added via plain `ALTER TABLE ... ADD COLUMN`.** SQLite allows adding a nullable FK column without a table rebuild, and `NULL` trivially satisfies a foreign key constraint — so this is purely additive: every existing row gets `NULL` with no backfill, and no existing query or Rust struct's non-nullability assumption about `runtime_id` is disturbed. A hypothetical "no runtimes-shaped identity at all" experiment (an over-engineered case considered and rejected) does not need a CHECK requiring at least one of the two FKs; that question is deferred to whichever future PR (7-11) first needs it.
- **`toolbox_catalog_snapshot` (deferred out of PR2 per §5c's decision log entry) is created in this same migration (`migrations/0003_toolbox_serving_dimension.sql`), inside the benchmark database — not a separate app-wide migration/DB as §2 originally speculated.** Its only real consumer is anchoring `serving_runtimes` rows to the exact vendored catalog content in effect when a run was recorded, so colocating it with its one consumer, under `benchmark.rs`'s existing hand-maintained version-gate mechanism, is more coherent than inventing a second migration runner for one table.
- **`toolbox_catalog::vendored_catalog_snapshot()`** exposes the two previously-private vendored `models.json`/`toolboxes.json` `include_str!` constants plus a `source_commit: Option<String>` parsed from `assets/cockpit-catalog/SOURCE`'s own `- Pinned commit: \`<sha>\`` line (a format `scripts/sync-toolbox-catalog.sh` itself writes, so this reads back an owned convention, not an unowned upstream format) — returns `None`, not a panic, if that line is ever missing or reformatted.
- **Catalog-snapshot auto-ensure, not auto-fabricate:** ingesting a `serving_runtime` calls a new `ensure_catalog_snapshot()` helper that `INSERT OR IGNORE`s a `toolbox_catalog_snapshot` row using the embedded vendored JSON **only when** the bundle's `catalog_snapshot_id` matches the currently-embedded `vendored_catalog_revision()`. For any other (older/different) snapshot id, it does nothing — it does not fabricate unknown snapshot content. If that snapshot row doesn't already exist, the subsequent `serving_runtimes` insert's foreign key constraint surfaces a clear `BenchmarkError::Conflict` (via the existing `ConstraintViolation → Conflict` mapping) instead of silently succeeding with invented data. This mirrors the "fail loud, not silent" discipline already established for `--label` (§5c).
- **Backend-identity reuse, not new vocabulary:** `serving_runtimes.toolbox_backend` reuses `SupportedServingBackend` (§2's closed, 5-variant enum) rather than a bespoke ingest-only type — `comfyui` and any unrecognized future id are rejected at deserialize time with a clear error (hand-written `Serialize`/`Deserialize` impls added to `SupportedServingBackend` for this purpose, mirroring `CatalogBackendId`'s existing hand-written pattern). `serving_runtimes.compute_api` reuses the existing `Backend` string-enum (`cpu/cuda/rocm/vulkan/metal/sycl/rpc/other`) already used by `runtimes.backend` — the same "engine" vocabulary, not a duplicate one; the migration's CHECK constraint was corrected to include `rpc` for exact parity.
- **Cross-bundle validation:** `IngestBundle::validate()` now requires `experiment.serving_runtime_id` to equal `bundle.serving_runtime.id` when either is present, and both to be absent otherwise — the same "referenced id must match the object actually in this bundle" pattern already used for `runtime_id`/`artifact_id`/etc., extended for the first time to an optional (not mandatory) relationship.
- **UI scope for PR5: filter dropdowns only, not new result-table columns.** `benchmarks.html` gained `Toolbox backend`/`Compute API` filter dropdowns (wired through `/api/benchmarks/filters`' new `toolbox_backends`/`compute_apis` lists, matching the existing Backend/Workload dropdown pattern) plus matching `RunQuery` filter params; `toolbox_id` remains filterable via the API/CSV/JSONL export but was not given its own dropdown (a third, more granular dimension was judged unnecessary UI clutter alongside the two headline dimensions). The visible run **table** itself was not given 3 new always-empty columns, because no backend populates non-null `toolbox_backend`/`toolbox_id`/`compute_api` until their own Rollout PRs (7-11) start ingesting real serving_runtime data — adding the columns now would only ever show "Not reported" in every row. This is deferred to whichever of PR7-11 first has real data to show, not skipped outright.
- **Migration-runner gate bug caught and fixed during implementation:** the pre-existing version-2 gate check (`if version != 2 || ...`) would have incorrectly rejected an already-fully-migrated version-3 database, because by the time execution reaches that check a database that started at version 3 has already passed through it once before (the check predates this PR, when version 2 was the final supported version). Corrected to a range check (`!(2..=3).contains(&version)`) so it means "has this database reached at least version 2 with the right shape," not "is this database frozen at exactly version 2" — verified with a new `version_two_registry_is_upgraded_to_serving_dimension_schema` test alongside the corrected `migration_creates_contract_schema_and_enables_pragmas`/`version_one_registry_is_upgraded_to_native_job_schema` assertions (all now expect version 3 and the 2 new tables).

### 8. Spider/radar chart (new) — token-efficiency metric substantially revised in v2; axis semantics resolved in v3
- New custom inline-SVG radar chart (consistent with the codebase's existing "no chart library" convention in `benchmarks.html`) added to `benchmarks.html` (or a new `spider.html` panel included from it), driven by a two-part picker UI (resolved in v3, replacing v2's undifferentiated "7 pickable dimensions" framing):
  - **Facet filters** (multi-select, narrow the underlying run population before comparison): engine, backend, toolbox. These never become their own radar axis.
  - **Compare-by / series selector** (which polygons are drawn): the user picks model and/or harness as the entity dimension(s) to compare — e.g. "compare 2-5 models" (one polygon per selected model, harness held fixed/aggregated by the facets above), "compare 2-5 harnesses" (same, inverted), or "compare model×harness pairs" (one polygon per distinct pair). Model/harness are therefore never radar axes either — they select *what* is being compared, not *how*.
  - **Metric axes** (the actual radar spokes — continuous only): token efficiency to positive result (TEPR) and speed at minimum; designed to be extensible to additional continuous metrics later (e.g. pass rate, cost-per-run) without changing this facet/series/axis structure. User picks ≥2 metric axes to plot (a 2-axis radar is a valid, if minimal, shape — noted here explicitly since it's an unusual radar UX, not a bug).
- **Resolved in v3 (was Open question 7 / "carried over from v1" in v2): categorical axes are never plotted as radar spokes.** TOP SESSION selected option (b) from v2's three presented alternatives: engine/backend/toolbox (and, by the same logic, model/harness) are filter/facet/series-selection keys only, determining which entities appear and which runs feed them, never plotted as a spoke themselves. This resolves what v2 left as a genuine unresolved product decision; option (a) (binary match/no-match expansion) and option (c) are no longer live alternatives for this rollout.
- Continuous axes (TEPR, speed, and any future metric axis) are independently normalized (min–max or percentile) across the currently filtered (faceted) run set — unchanged from v2.
- **"Token efficiency to positive result" (TEPR) — substantially revised in v2.** The Dory critic identified that v1's formula used `experiments.prompt_tokens`/`generation_tokens`, which on closer inspection are **declared/planned** token-budget fields set at experiment-setup time, not confirmed to be an **observed, per-attempt actually-consumed** count — a metric named "efficiency" that silently uses planned-not-actual numbers would be misleading. v2's proposal (still flagged for explicit user sign-off, not shipped as fact):
  > `TEPR(entity, cohort) = SUM(actual_tokens_consumed across every run attempt for entity within cohort) / COUNT(runs in cohort for entity where outcome = positive)`
  - **`actual_tokens_consumed` must be the observed count from the run/telemetry itself** (e.g. `telemetry_samples` or a per-run actual-usage field), **not** `experiments.prompt_tokens`/`generation_tokens` — this document explicitly does **not** assert which existing field (if any) already holds this correctly for every backend; confirming/adding the right field is now called out as a required implementation-time verification step, not assumed solved by picking a column name in this doc (Open question 6, left open per TOP SESSION's instruction to resolve at implementation time).
  - **One authoritative "positive" rule, not left ambiguous per multi-task run:** a run counts as positive only if `runs.status = 'succeeded'` **and**, when the run has any `quality_results` rows, **all** of them have `passed = 1`. A run with zero `quality_results` rows is **excluded from TEPR entirely** (not counted as pass or fail) rather than silently defaulting either way.
  - **Cohort restriction, new in v2:** TEPR is only ever compared within a matched cohort (same workload id at minimum) — mixing workloads in one TEPR number was flagged as making the metric meaningless, since token cost is workload-dependent by construction.
  - **Minimum sample size, new in v2:** TEPR is not displayed (shown as "insufficient data," not a misleadingly precise number) below a minimum combined pass+fail sample count for the entity/cohort pair (proposed default `n ≥ 3`, adjustable) — small-sample TEPR values are flagged as low-confidence even above that floor.
  - Undefined (`COUNT(positive)=0` within the cohort) is rendered as "no passing runs," not 0 or ∞.
  - **Rejected/alternative definitions, unchanged from v1, still offered to the user as alternatives if TEPR-as-now-defined isn't the intent:** (a) generation-tokens-only (excludes prompt cost — undercounts high-context harnesses); (b) per-success-run-only (ignores failed-attempt cost entirely — simpler, but is really "tokens per response," not "efficiency to a positive result"); (c) cost-normalized ($/token) — rejected for now, no local-compute pricing table exists.

### 9. Security boundary for the new mutation surface (new in v2 — Dory critic finding #15)
v1's "Security, privacy, reliability, and operations" section (kept below, largely unchanged) covered secrets/permissions but not the structural fact that this plan adds an entirely new class of privileged endpoints (create/update/delete/adopt for toolbox containers, server-mode start/stop, and — if the write half of §4 ships — config.json mutation). These are **not automatically covered** by the existing destructive-route allowlist: `src/server.rs:224-241` is a hardcoded path-prefix/exact-path match (confirmed by direct inspection) gating destructive APIs to loopback-only callers; a new route like `/api/toolbox-containers/{id}/delete` is reachable from non-local callers unless explicitly added to that list, which matters concretely because the daemon supports an optional non-loopback bind mode (`src/daemon.rs:144-153`). Required, concrete additions (not just "be careful"):
- Every new mutating route added to the same `is_destructive` allowlist mechanism (or its structural successor if that mechanism is refactored) — enumerated explicitly in Detailed implementation, not left to "whoever writes the route remembers."
- Secrets (`HF_TOKEN`, `--api-key`, any backend credential in `backend_config`/`config.json`) never appear in logs, command-line argv visible via `ps`, or API responses — reuse cockpit's `redact_command()` discipline as the baseline, verified with a test that asserts a secret-bearing command never appears unredacted anywhere observable.
- Containers created by this feature default to rootless/no-new-privileges unless a specific `runtime_profile.engine_args` entry requires otherwise (e.g. device passthrough for ROCm) — any such elevated flag is itself catalog-vendored and code-reviewed (§3), not user-supplied free text at request time.
- Config.json's write path (§4) is itself a destructive-route candidate and must be added to the same allowlist mechanism, not treated as "just a config file."

### 10. Models tab: catalog browsing + download lifecycle (pre-PR6 design note, narrows Open question 4)
*(Written before PR6 implementation starts, per the design doc's own Open question requiring this. Grounded directly in upstream source — `ai_toolbox_cockpit/backends/{ds4,halogen,llama_cpp,r9v,vllm}/model_manager.py`, `huggingface.py`, `storage.py` — read live from GitHub during this pass, not guessed.)*

**What upstream actually does (verified, not assumed):**
- Every download-capable backend (ds4/halogen/llama_cpp/r9v) shells out to the `hf` CLI (`huggingface_hub`'s official CLI, falling back to `PATH` if not colocated with the Python interpreter) as a single subprocess: `hf download <repo> [<file>...] [--include <glob>] [--revision <rev>] --local-dir <dir>`. The exact positional/flag shape is backend-specific and load-bearing, not a generic "download files" call:
  - **llama_cpp** (`llama_cpp/model_manager.py::get_download_cmd(repo, quant_pattern)`): `hf download <repo> --local-dir <models_dir>/<repo_basename>`, then either a single positional filename (exact quant file) or `--include <glob>` (sharded/dspark-drafter patterns like `*-of-*.gguf`).
  - **ds4** (`ds4/model_manager.py::get_download_cmd(repo, filename)`): `hf download <repo> <filename> --local-dir <models_dir>` — always exactly one file, no revision pin.
  - **halogen** (`halogen/model_manager.py::get_download_cmd(bundle, directory)`): `hf download <repo> <file1> <file2> ... --revision <revision> --local-dir <directory>` — the bundle's entire `files[]` manifest in one call, pinned to a specific commit revision.
  - **r9v** (`r9v/model_manager.py::get_download_cmd(package, directory)`): identical shape to halogen (multi-file + `--revision`), plus a separate `mount_path()` guard rejecting any destination path containing `:`/control characters (container-mount-path safety, not download-specific).
  - **vllm has no download step at all.** `vllm/models.py`'s own view is explicitly documented upstream as "deliberately non-downloading: it shows maintained launch defaults, local cache state, and Hub search results" — vLLM downloads a repo through its own internal HF integration the first time its server container starts. **Brainrouter's Models tab for vLLM is therefore catalog/Hub-search browse-only, with no download job type at all for this backend** — mirroring upstream exactly, not a brainrouter simplification.
- **Auth:** `huggingface.py::huggingface_environment()` builds the subprocess environment by taking `HF_TOKEN` from the process environment first, falling back to a token cockpit itself saved via `set_setting("hf_token", ...)` — a **top-level** config.json key (via `get_setting`/`set_setting`), not a per-backend one (`get_backend_settings`). It also always sets `HF_XET_HIGH_PERFORMANCE=1`. Brainrouter's `CockpitConfig` (`src/cockpit_config.rs`) currently only round-trips unknown top-level keys through its `extra` catch-all — it has no named `hf_token` accessor yet; PR6 needs to add one (read-only, additive, consistent with Phase 1's existing read scope) to source the same shared token rather than requiring a second, brainrouter-only copy.
- **Execution model is TUI-suspend + foreground-synchronous:** every backend's `_download_model()` calls `self.app.suspend()` then `subprocess.run(command, ..., check=True)` **in the foreground**, letting `hf`'s own terminal progress bar draw directly to the real TTY until the process exits; cockpit itself does no progress parsing at all. **This is not portable to brainrouter**, which is a headless HTTP server with no TTY to suspend into — the same structural reason §5b already gives for why detached server-mode containers need their own registration mechanism instead of cockpit's foreground model.
- **Integrity/completeness checking is deliberately weak for large files, and differs per backend — cockpit's own code, not a corner cut by this document:**
  | Backend | Completeness check | Checksum available? |
  |---|---|---|
  | llama_cpp | none beyond the `hf download` subprocess's own exit code | no |
  | ds4 | `is_model_downloaded()`: destination file exists, by name only (no size check) | no |
  | halogen | `incomplete_files()`: per-file **exact size match** against catalog `size_bytes` — comment in source: *"Size checks catch missing/partial downloads without reading 118 GiB"* | no (`sha256` absent from halogen catalog entries) |
  | r9v | same size-match `incomplete_files()` as halogen, **plus** a separate, explicitly user-triggered `verify_package()`/`verify_file()` action doing a full streaming SHA256 read, only offered because r9v catalog entries are the only ones carrying `sha256` per file | yes, and it's the only backend that does |
  | vllm | n/a (no download step) | n/a |
- **`hf download --local-dir` does not expose partial files at the destination during download** — it downloads to a temp/cache location first and atomically renames into the destination only on success (confirmed via upstream HF maintainer statements), which is exactly why cockpit's own size-match check is a valid completeness signal (a file at the destination path is either absent or fully correct-sized, never partial) but also means **brainrouter cannot compute live byte-level progress by polling the destination directory.**
- **Disk-space preflight is advisory, not blocking:** `storage.py::download_space_note()` compares `shutil.disk_usage(destination).free` against the catalog's declared size and prints a warning if the download is larger than available space, but never refuses to start.

**Proposed native-Rust design for brainrouter (flagged as proposals — the items in "Open items" below need explicit sign-off, not silent adoption):**
1. **Reuse `benchmark_lab.rs`'s existing job-registry shape** (`Arc<Semaphore::new(1)>` single-flight execution slot, a job record with id/status/message polled via `GET`, `POST .../jobs` → `202 Accepted` + job id, `POST .../jobs/{id}/cancel`) as the concurrency/lifecycle model for model downloads, exposed as a new `/api/model-downloads` job registry — **not** a bespoke mechanism, and **not** SSE token-by-token streaming of `hf`'s own tqdm output (its `\r`-based redraws aren't a stable, parseable format to build a real progress bar on). One material difference from `benchmark_lab.rs`'s cancellation: its `watch::channel(false)` model assumes the cancellable work is brainrouter's own async loop checking the flag cooperatively; a `hf download` child process is opaque, so "cancel" here must be actual OS-level process termination (`Child::start_kill()`), not a cooperative flag the child checks itself.
2. **Progress is a coarse state machine, not a byte percentage:** `queued → downloading → verifying → complete | failed | cancelled`. This is a deliberate, evidence-based scope narrowing (not a shortcut): live byte-level progress isn't observably possible against the destination directory (see above), and parsing `hf`'s own stderr for a number is fragile and unverified in this environment (no network + `hf` CLI + real model available in this sandbox to confirm its exact current output format). A bounded raw-stderr ring buffer (mirroring the existing `MAX_CAPTURE_BYTES`/`MAX_SSE_BUFFER_BYTES` caps already used in `benchmark_lab.rs`) is proposed so the dashboard can show a scrolling "raw tool output" tail as reassurance, explicitly labeled as such and never presented as a verified percentage.
3. **Completeness/integrity checks mirror cockpit's own per-backend matrix exactly** (table above) — this is intentionally not "upgraded" to always-checksum, because the catalog itself doesn't carry hashes for 4 of 5 backends, and reading 100+ GB per check is a real, deliberate cost cockpit's own authors avoided. r9v gets an additional explicit, user-triggered "Verify" action (full SHA256, using the already-typed `CatalogModelFile.sha256`/`size_bytes` fields in `src/toolbox_catalog/models.rs`, which already model exactly this data — no catalog-layer change needed for PR6).
4. **Resumability and atomic publish are inherited for free from `hf download` itself** — its own cache-based HTTP-range resume and temp+rename publish mean brainrouter's job wrapper does not need to implement either; a cancelled or failed job is simply retried by re-invoking the identical command.
5. **Disk-space preflight mirrors cockpit's warn-not-block behavior**, comparing the destination filesystem's free space against the catalog's declared size (`Ds4Model.size_gb`, or `sum(CatalogModelFile.size_bytes)` for halogen/r9v) when available — llama_cpp/vllm catalog entries carry no size field at all today, so no preflight is possible for those two backends; this is a real gap inherited from the catalog data, not an oversight.
6. **Command construction is per-backend and mirrors the exact argument shapes above**, not a generic abstraction — e.g. llama_cpp's exact-filename-vs-`--include`-glob branching and halogen/r9v's `--revision` pin are backend-specific and correctness-load-bearing.
7. **New HTTP surface:** `/api/model-downloads` (job registry, mirroring `/api/benchmarks/lab/jobs`'s route shape) for the four download-capable backends, plus a read-only `/api/toolbox-models` (per the existing architecture diagram) for catalog browsing + local-presence status across all 5 backends (vLLM included, browse-only). Both the mutating `/api/model-downloads` routes are added to the `is_destructive` allowlist (§9) — not automatically covered.

**Open items flagged for explicit sign-off, not silently decided:**
- Whether the "coarse state + raw-log tail, no numeric byte progress" UX is an acceptable trade-off, versus wanting a real progress bar (which would require bypassing the `hf` CLI for a lower-level chunked-download implementation against the HF Hub API directly — materially more implementation work and loses the free resume/atomic-publish/auth handling `hf download` provides). Proposed default: ship the coarse version first.
- Global single-in-flight-download (`Semaphore::new(1)`, mirroring `benchmark_lab.rs`) vs. one-per-backend or one-per-destination-directory concurrency. Proposed default: global single-flight (simplest, matches cockpit's own implicitly-serial single-TUI-session model).
- Whether brainrouter should ever gain a **write** path for `hf_token` back into cockpit's shared config.json (cockpit's own `save_hf_token()` does this today). This is a new secret-bearing write action, not covered by Phase 1's existing read-only-plus-narrow-apply scope (§4/Decision log) — proposed to stay **read-only** for `hf_token` specifically unless the user explicitly asks for the write path, given §9's security-boundary discipline around secrets.
- Whether `hf`'s current CLI version has a machine-readable/JSON progress mode that would make byte-level progress reliable after all — not verified in this environment; worth a quick check at PR6 implementation time before committing to the coarse-state-only design above.



```mermaid
flowchart TB
  subgraph Catalog["Build-time / CI"]
    A[kyuz0/ai-toolbox-cockpit\nmodels.json + toolboxes.json] -->|scripts/sync-toolbox-catalog.sh\npinned commit + lossless schema check| B[assets/cockpit-catalog/*.json\ncommitted to brainrouter repo]
  end
  subgraph Runtime["brainrouter server process"]
    B --> C[toolbox_catalog.rs\nCatalogBackendId (open) /\nSupportedServingBackend (closed)]
    C --> D[/api/toolboxes\n(generalized, all backends)/]
    C --> E[/api/toolbox-models\n(new)/]
    D --> F[podman/toolbox command builders\n+ ownership labels + op-lock\n(toolbox create vs raw podman:\nspike pending, sec 5)]
    G[~/.config/ai-toolbox-cockpit/config.json] -->|read-only, Phase 1| H[cockpit_config.rs]
    H -.->|explicit user-triggered\nreload-verify-write only| G
    H --> D
    H --> E
  end
  subgraph Dashboard["Dashboard UI"]
    D --> I[Toolboxes tab]
    E --> J[Models tab]
    F --> K[Server Mode tab\n(detached, named, labeled containers)]
  end
  subgraph RoutingPrereq["NEW prerequisite: sec 5b"]
    K --> R{serving identity\nregistrable as router upstream?}
    R -->|yes| S[Router gains upstream\n(llama-swap-compatible)]
    R -->|no| T[Status-only,\nno routing claim]
  end
  subgraph Requests["Existing request path"]
    S --> L[RouteEvent\n+toolbox_backend, +compute_api]
    L --> M[Sankey: 6 columns\nexplicit cloud/local_unmanaged/\nexternal/unknown buckets]
    T -.->|not represented in Sankey\nas routed traffic| M
  end
  subgraph Benchmarks["Benchmark system"]
    N[runtimes.backend (existing) = compute_api / Engine]
    O["runtimes widened OR new\nserving_runtimes table (decision pending)\n= toolbox_family / toolbox_id"]
    N & O --> P[Spider chart\n7 pickable dimensions,\nTEPR v2 definition]
  end
```

Key change from v1's diagram: the routing-integration decision point (`RoutingPrereq`) is now explicit and sits **between** server-mode launch and the Sankey/benchmark consumers — v1's diagram implicitly assumed every launched container's traffic would just show up in `RouteEvent`, which the critic showed is not currently true.

### 10a. PR6 implementation decisions (added during PR6 implementation, 2025-09-18)
Mirrors §4a/§5c/§6a/§7a's format. Records the concrete shape PR6 landed in, and where it diverges from §10's proposal.

**New module, not folded into `toolbox_catalog.rs`.** `src/model_downloads.rs` (new, ~600 lines) owns the whole job-registry (`ModelDownloadRegistry`: `Semaphore::new(1)`, in-memory `HashMap`+insertion-order `VecDeque` capped at `MAX_JOB_HISTORY = 100`), the per-backend command builders (`build_download`/`build_multifile_download`), the completeness/verification logic, and the local-presence sweep — kept separate from `toolbox_catalog.rs` because that module is pure catalog *data* modeling, while this one is *action* orchestration (subprocess spawning, cancellation, job state), matching the same split `benchmark.rs` (data/schema) vs. `benchmark_lab.rs` (job execution) already establishes.

**`CatalogModelFile` promoted to the `toolbox_catalog` module's public re-export list** (`pub use models::{..., CatalogModelFile, ...}` in `toolbox_catalog.rs`) — it was previously only reachable via `toolbox_catalog::models::CatalogModelFile`; `model_downloads.rs` needed it at the same import depth as `ModelPayload`/`SupportedServingBackend`, so it was added to the curated top-level list rather than importing through the internal `models` submodule path.

**Cancellation is real OS-level `Child::start_kill()`, exactly as §10 point 1 anticipated** — `execute()` runs a `tokio::select!` between `child.wait()` and `cancel_rx.changed()`; the cancel branch kills the child directly rather than checking a cooperative flag (there is nothing in an opaque `hf` subprocess to check).

**HF auth sourcing implemented exactly as §10 described:** `HF_TOKEN` env var first, falling back to `CockpitConfig.hf_token` (new typed field, top-level not per-backend, added to `src/cockpit_config.rs` alongside a new `BackendSettings.models_dir` field and a `CockpitConfig::models_dir(backend_id)` accessor) — both fields are purely additive and round-trip via the pre-existing `#[serde(flatten)] extra` catch-alls, so no existing cockpit config.json content is put at risk. `hf_token` stays **read-only** in this PR, per §10's Open item — no write path was added.

**`quant_pattern` is a required client-supplied string for llama_cpp downloads, not server-derived — and this is a real, upstream-confirmed scope narrowing, not a placeholder.** Re-reading `llama_cpp_model_manager.py` during implementation (beyond what §10's design pass had already checked) showed that upstream itself derives valid quant/pattern values from a **live Hugging Face repo file listing** (`get_hf_quants()`/`get_hf_quants_with_sizes()`), not from any static catalog field — the vendored `models.json`'s `inference_profiles`/`default_inference_profile` fields (which do exist per llama_cpp entry) are about **serving args** (reasoning-effort presets passed to `llama-server`), not download quant selection, and were never a candidate source for this value. Replicating upstream's live HF-API repo-listing lookup was judged out of scope for this PR; the dashboard's llama_cpp download row instead exposes a free-text input where the operator types the exact filename or folder-quant name (the same information they'd read off the model's Hugging Face repo page). Revisit if/when a "browse this repo's available quants" feature is explicitly requested.

**HTTP surface, exactly 6 routes, all under `/api/model-downloads`:**
- `GET /api/model-downloads/status` — read-only local-presence sweep (ds4/halogen/r9v; llama_cpp/vllm excluded — see module docs) for "already downloaded" badges.
- `GET /api/model-downloads` — list known jobs (registry's own history cap).
- `GET /api/model-downloads/{id}` — poll one job.
- `POST /api/model-downloads` — start a job (`{"backend", "model_id", "quant_pattern"?}`).
- `POST /api/model-downloads/{id}/cancel` — cancel a running/queued job.
- `POST /api/model-downloads/verify` — explicit SHA256 verification (r9v-only today, per the catalog's own sha256 coverage).
All `POST /api/model-downloads*` paths are prefix-gated into the `is_destructive` allowlist in `src/server.rs`, mirroring the existing `/api/toolbox-containers` prefix-gate pattern rather than enumerating each sub-path individually.

**`AppState` gains `model_downloads: Arc<ModelDownloadRegistry>`**, constructed once in `src/daemon.rs` alongside the other registries. No other `AppState` construction sites exist in the codebase, so this was a single-site change.

**Dashboard UI: a new top-level nav item ("Downloads" / `#toolbox-models-view`), not a reuse of the existing "Models" nav item.** The existing `#models-view`/`loadModels()` is confirmed (re-verified during PR6, not just assumed from §10) to be an unrelated OMP routing-model picker over llama-swap's already-loaded models — reusing or renaming it would have conflated two unrelated concepts. The new view has a backend tab selector (ds4/halogen/llama_cpp/r9v — vllm excluded per module docs), a catalog-model list per backend showing local-presence status (from the `/status` sweep) and a Download/Cancel/Verify action per row, and a "Downloads" job panel below that polls every 3s only while a job is non-terminal and the view is actually visible (`activeView === 'toolbox-models'`) — mirroring the coarse, non-byte-level progress design from §10 point 2 exactly: the UI shows the job's `status` string and a message, never a percentage.

**In-memory job registry confirmed as a real v1 trade-off, not silently accepted:** a daemon restart loses all download job history and kills any in-flight `hf` child process (same as any other unmanaged subprocess would be on restart) — unlike `benchmark_lab.rs`'s SQLite-backed job persistence. Flagged in §10 already; reconfirmed here as shipped, not revised, since no SQLite-backed job table was added in this PR.

**Verification:** `cargo build --all-targets` clean; `cargo test --lib` → 228/228 (was 209 after PR5; PR6 added 11 `model_downloads` tests + 2 `cockpit_config` tests for the new `hf_token`/`models_dir` fields); `cargo clippy --locked --all-targets` → no new warnings beyond the same pre-existing set noted in §6a, after fixing one new `sort_by`→`sort_by_key` lint in `model_downloads.rs`; dashboard JS extracted and syntax-checked with `node --check` (no dedicated `.cjs` test suite covers `main_dashboard.html` — `scripts/test-benchmark-ui.cjs` covers only `benchmarks.html`, untouched by this PR).

### 11. §5's `toolbox create` engine-args spike — resolved by reading upstream source, no hardware needed (retroactively narrows Open question 2 / Rollout PR 10)
*(Discovered while preparing PR7. Directly reads upstream `ai_toolbox_cockpit/runtime/interactive.py`, `ai_toolbox_cockpit/runtime/toolboxes.py`, and `ai_toolbox_cockpit/backends/{ds4,halogen}/server_runner.py`/`server.py` — fetched live from GitHub during this pass, not guessed.)*

**The premise behind §5's blocking finding #5 was subtly wrong, and upstream's own source shows why, without needing a live R9V host to prove it.** §5 framed the open question as "does `toolbox create` forward arbitrary podman flags (`--device`/`--group-add`/`--security-opt`) for R9V's ROCm passthrough." Reading upstream's actual `build_create_command()` (`runtime/interactive.py`) shows the real answer is simpler and already decided by cockpit itself:

```python
def build_create_command(runtime, name, image, engine_args, ...):
    if runtime.wrapper is InteractiveBackend.TOOLBOX:
        if runtime.engine is not ContainerEngine.PODMAN:
            raise ValueError("Toolbx requires Podman")
        return ["toolbox", "create", "--image", image, name]   # <-- engine_args NEVER passed here
    # ... only the Distrobox branch below actually threads engine_args through --additional-flags
```

**`toolbox create` never receives `engine_args` at all, for *any* backend, in upstream's own code** — not just for the default Vulkan profile. This isn't an oversight: cockpit's Toolboxes-tab containers (interactive dev shells you `toolbox enter`) rely on Toolbx's own broad default host-device/group integration (well-documented upstream Toolbx behavior, distinct from a locked-down bare `podman create`) rather than passing explicit `--device`/`--group-add` flags Toolbx's CLI doesn't even expose. Brainrouter's already-shipped PR2 (`recreate_toolbox_container()` in `src/server.rs`, bare `toolbox create --image <image> --label ... <name>`, no engine_args) independently arrived at **exactly this same shape** — not a gap to fix, a correct match to upstream's real behavior, confirmed now rather than merely assumed.

**Where `engine_args` (ROCm device passthrough, etc.) actually gets applied is a completely different code path: Server Mode, not the Toolboxes tab.** `backends/ds4/server_runner.py::build_server_cmd()` (and confirmed structurally identical in `backends/halogen/server.py` via its own `build_server_cmd`/`run_foreground_server` call) constructs a **plain `podman run`** invocation directly — never `toolbox create` — and threads the toolbox's `runtime_profile.engine_args` straight into it as ordinary podman flags:
```python
docker_args = [engine, "run", "--rm", "-it", "--name", "ds4-cockpit-server"]
docker_args.extend(engine_args)                      # <-- --device /dev/kfd, --group-add render, etc. go here
docker_args.extend(["--ipc=host", "--cap-add=SYS_PTRACE"])   # ROCm HSA memory-mapping requirement
if engine == "podman":
    docker_args.extend(["--security-opt", "label=disable", "--userns=keep-id"])
# ... port/network mapping, -v models:/models:ro, then [image] + [server_binary, ...server_args]
```
`podman run --device ... --group-add ...` accepting these flags is ordinary, well-documented podman CLI behavior — there was never a real "will the flag be forwarded" question for Server Mode; that question only ever applied to `toolbox create`, which (per above) never receives these flags from upstream either. **This closes the flag-forwarding half of Open question 2 without needing real R9V hardware.** What genuinely still needs real hardware (unchanged, not newly discovered) is whether ROCm *itself functions correctly* through this device set on a real R9700 — a hardware/driver question, not a command-construction one; see Open question 1.

**Consequence for the Rollout:** PR 10 (framed as "the R9V-only `toolbox create` vs. raw `podman` spike") is retargeted, not cancelled — there is no `toolbox create`-vs-`podman` decision left to make (Server Mode was always going to be `podman run` directly, per §5's existing "detached, named, brainrouter-managed containers" decision, now additionally confirmed to be upstream's own actual mechanism, not just brainrouter's own choice). PR 10's remaining, real scope is verifying ROCm *functions* on real R9700 hardware once PR 11 is attempted — a hardware-validation checkpoint, not a design spike blocking implementation. PR 11 (R9V server mode) can therefore proceed on the same schedule as PR 7-9, using the exact same `podman run` + `runtime_profile.engine_args` mechanism as ds4/halogen/vllm; only real-hardware *validation* (not command construction) remains gated.

### 12. PR7 pre-implementation design note: ds4 Server Mode (added before PR7 implementation, 2025-09-18)
*(Written before PR7 starts, per this design doc's own established practice of a design-note pass before non-trivial implementation. Grounded in `ai_toolbox_cockpit/backends/ds4/server_runner.py`, `server.py`, `config.py`, and `runtime/toolboxes.py::upgrade_groups_for_podman` — read live from GitHub during this pass.)*

**What upstream's ds4 Server Mode actually does (verified above, §11):** builds one `podman run --rm -it --name ds4-cockpit-server <runtime_profile.engine_args, minus --group-add sudo> --ipc=host --cap-add=SYS_PTRACE [--security-opt label=disable --userns=keep-id] [-p <host:port> | --network=host for multi-node] -v <models_dir>:/models:ro [-v <kv_disk_dir>:/var/cache/ds4-kv] <image> <server_binary> -m /models/<rel_path> --ctx <n> --host 0.0.0.0 --port <port> [...many optional flags for multi-node/dspark/ssd-streaming/mtp/vision]` command, then runs it in the foreground (cockpit's TUI-suspend model) until Ctrl+C, force-removing the fixed-name container both before starting and on stop/interrupt.

**Proposed native-Rust scope for PR7 v1 (flagged as a deliberate narrowing, mirroring PR6's `quant_pattern` precedent — not a silent shortcut):**
1. **Detached, not foreground:** `podman run -d --name brainrouter-ds4-server ...` (replacing `--rm -it` with `-d`), matching §5's already-decided detached/named/labeled-container model, not cockpit's TUI-suspend model — brainrouter has no TTY to suspend into. Ownership labels (`io.brainrouter.managed=true`, `io.brainrouter.catalog_id=<ds4 toolbox id>`, plus new `io.brainrouter.server_backend=ds4`) attached at creation, mirroring PR2's convention exactly.
2. **Single-node ("Standalone") mode only for v1** — the multi-node coordinator/worker distributed-role flags (`--role`, `--layers`, `--listen`/`--coordinator`, `--dist-prefill-*`), DSpark auxiliary-model routing, SSD-streaming cache-expert flags, and MTP/vision-projector auto-wiring are **not** ported in PR7. This is a real scope narrowing, not an oversight: each of those is itself a nontrivial per-family policy (`backends/ds4/config.py`'s `families` table) that changes correctness-critical defaults per model family, and porting all of it in one PR would make PR7 as large as PR6 all over again. Instead, PR7 exposes upstream's own **`custom_args`** escape hatch (a free-text field appended verbatim to the `ds4-server` invocation, exactly mirroring `server_runner.py`'s own `custom_args: str` parameter — not a brainrouter invention) so an operator who needs multi-node/DSpark/SSD-streaming/MTP/vision today can still reach it, just without brainrouter auto-deriving the right flags from the model's family. Revisit if/when full per-family auto-configuration is explicitly requested.
3. **Core v1 parameters, corrected against `build_server_cmd`'s actual signature (a first draft of this note incorrectly assumed `ctx` was optional and that `host` was passed to the server's own `--host` flag — both wrong, corrected here after reading the full function body, not just its call sites):**
   - `model_id` (not a raw path) — references a ds4 catalog model entry (same identifier space as PR6's `StartDownloadRequest.model_id`), resolved server-side to an absolute file path via the catalog + PR6's `effective_models_dir`/local-presence-sweep machinery, rejecting the request if that model isn't already fully downloaded rather than accepting an arbitrary filesystem path from the request body (avoids a path-traversal-shaped API and matches how a user would naturally pick "a model I already downloaded" from the dashboard's Models tab).
   - `ctx` — **required**, not optional: `build_server_cmd(ctx: int, ...)` has no default and always emits `--ctx <n>`; there is no "let ds4-server pick its own default" case to omit it for. brainrouter's dashboard form pre-fills upstream's own observed TUI default (`126000`, `backends/ds4/server.py:255`, the "Standalone" mode value) as a suggestion, not a silent backend-side default — the operator can change it, but the API itself always requires an explicit value.
   - `host`/`port` — **not** what a first read suggests: `host` does **not** become the in-container `--host` flag (`server_args` always hardcodes `"--host", "0.0.0.0"` regardless of the request) — it only selects the **podman port-mapping bind address** (`-p <bind_ip>:<port>:<port>`, with `localhost` translated to `127.0.0.1`; omit the bind-IP prefix entirely and just use `-p <port>:<port>` when `host` is `0.0.0.0`), i.e. whether the server is reachable from the loopback interface only or from the LAN. Upstream's own TUI defaults are `host="localhost"` (loopback-only), `port="8000"` — proposed as brainrouter's own dashboard-form suggested defaults for the same reason as `ctx` above (not a silent backend default).
   - `custom_args` (optional free text, shlex-split before appending, mirroring upstream's own `shlex.split(custom_args)`).
   - `server_binary` is not a user-facing parameter: real vendored ds4 toolbox entries all have no `backend_config.server_binary` override, so it's hardcoded to upstream's own fallback literal `"ds4-server"` for v1 (revisit only if a future vendored entry actually sets an override).

4. **Fixed command shape ported verbatim for the parts that are not model-family-specific:** the engine-args cleaning (`_clean_engine_args` — drop `--group-add sudo`, not needed for server mode), `--ipc=host --cap-add=SYS_PTRACE`, podman's `--security-opt label=disable --userns=keep-id`, the two MXFP4 ROCm env vars (`DS4_ROCM_ENABLE_MXFP4_TILE4=1`, `DS4_ROCM_MXFP4_DOWN_RGROUP=4`, both on by default upstream), the `-v <models_dir>:/models:ro` mount, and `upgrade_groups_for_podman()`'s named-device-group → `keep-groups` rewrite (ported from `runtime/toolboxes.py`, needed because podman's rootless group-add semantics differ from Docker's for named groups like `video`/`render`).
5. **Lifecycle is synchronous request/response, not an async job registry** (unlike PR6's downloads): starting/stopping a container is a sub-second podman operation, not a multi-minute download, so `POST .../start` and `POST .../stop` return their final result directly (mirroring PR2's `create_toolbox_container`'s synchronous shape, not PR6's `202 Accepted` + poll shape). `GET .../status` reads live `podman inspect`/`podman ps` state, matching PR2's "no persisted state beyond live container state" decision — no new DB table.
6. **Sankey/benchmark population: `status_only` bucket, not a routed-traffic claim (§5b, §6a).** PR7 does **not** attempt to make a running ds4 Server Mode container an actual routable upstream for brainrouter's request router (`src/daemon.rs:305-320`'s `Router::new`) — that would require ds4-server to speak an OpenAI-compatible API compatible with the existing `manifest`/`llama_swap` provider abstraction, which is unverified and out of scope for this PR. A running, brainrouter-managed ds4 server renders in the Sankey/benchmark dashboards using the already-shipped `status_only` bucket (§6a) — visible as "running, not yet carrying real routed traffic" — never faked as routed traffic. Full router-proxy integration for ds4 (option (a) in §5b) is deferred to a future PR if/when ds4-server's API compatibility is confirmed.
   **Correction found during implementation (recorded in full in §12a): this item overstated what PR7 actually delivers.** `status_only` is a bucket value that `skServingBucket()`/`derive_serving_identity()` (§6a) can only ever assign to an *actual routed `RouteEvent`* — and per §5b, a ds4 Server Mode container is "completely invisible to the router" until its own `{toolbox_backend, compute_api, runtime_profile_id, endpoint}` serving-identity registration (§5b's proposed mechanism) exists. PR7 does not implement that registration for any backend — it only adds the dashboard's new Server Mode panel (start/stop/status), which is a separate, container-inspect-only surface with no connection to `routing_events.rs`/the Sankey renderer at all. A ds4 server started via PR7 is therefore **not visible in the Sankey diagram in any bucket**, `status_only` included, until a future PR actually builds the §5b registration mechanism. See §12a for the concrete decision to defer that mechanism rather than build it ad hoc inside PR7.
7. **New HTTP surface, scoped to ds4 only in PR7 but shaped to extend per-backend later (mirrors PR6's `/api/model-downloads` prefix convention):** `GET /api/server-mode/ds4/status`, `POST /api/server-mode/ds4/start` (body: `model_id`, `ctx`, `host`, `port`, optional `custom_args` — see corrected item 3 above), `POST /api/server-mode/ds4/stop`. All `POST` routes added to the `is_destructive` allowlist (§9) via a `path.starts_with("/api/server-mode/")` prefix clause, matching the established prefix-gate convention (§5c/PR6). `toolbox_id` is also required on `start` (which of the vendored ds4 toolbox variants — e.g. `strix-halo-ds4-rocm-10-0` vs. `strix-halo-ds4-therock-nightly` — to source the image/`runtime_profile` from), mirroring `create_toolbox_container`'s existing explicit-`toolbox_id` pattern rather than adding a second server-side "effective default toolbox" resolver alongside the dashboard's existing client-side `effectiveDefaultToolbox()` (§4a) — the dashboard already computes this and can pass it through.

**Open items flagged for explicit sign-off, not silently decided:**
- Whether `host`/`port` should have a brainrouter-chosen default (e.g. `0.0.0.0:8090`) or must always be explicitly supplied by the operator. Proposed default: require both explicitly in v1 (no invented port-allocation policy), since a wrong silent default risks port collisions with other locally-running services brainrouter has no visibility into.
- Whether the fixed single-instance container name (`brainrouter-ds4-server`) is the right model, versus allowing multiple concurrently-running ds4 servers (e.g. one per model) under distinct names. Proposed default: single-instance, matching upstream's own single-fixed-name (`ds4-cockpit-server`) design — starting a second model requires stopping the first, exactly like cockpit's own TUI.

### 12a. PR7 implementation decisions (added during PR7 implementation, 2025-09-18)
Mirrors §4a/§5c/§6a/§7a/§10a's format. Records the concrete shape PR7 landed in, and where it diverges from §12's proposal.

**New module, not folded into `server.rs` or `model_downloads.rs`.** `src/server_mode.rs` (new, ~380 lines) owns the command builder (`build_ds4_server_command`), the two ported upstream algorithms (`clean_engine_args_for_server`, `upgrade_groups_for_podman`), toolbox resolution, and the three lifecycle functions (`start_ds4_server`/`stop_ds4_server`/`ds4_server_status`) — kept separate from `model_downloads.rs` for the same reason PR6 kept that module separate from `toolbox_catalog.rs` (§10a): this is *action* orchestration (subprocess construction/spawning against live state), not catalog data modeling.

**`resolve_downloaded_ds4_model()` added to `model_downloads.rs`, not duplicated in `server_mode.rs`.** A small public helper (`pub struct ResolvedDs4Model { models_dir, filename }` + `pub fn resolve_downloaded_ds4_model(model_id) -> Result<ResolvedDs4Model, DownloadError>`) was added right after `local_presence_snapshot()`, reusing that module's existing private `resolve_catalog_entry`/`effective_models_dir`/`build_download`/`check_completeness` helpers rather than re-deriving model-path resolution logic in the new module. It rejects (via `DownloadError::Validation`) any model that isn't `CompletenessResult::Complete` — starting a server against a partially-downloaded file is refused server-side, not left to podman to fail on.

**Two corrections to §12's own draft, found by reading `server_runner.py::build_server_cmd()`'s full 174-line body (not just its call sites) immediately before implementing — recorded here explicitly as corrections, not silent scope changes:** (1) `ctx` has no Python-side default and is always required — §12 already documents this correction inline (item 3); (2) `host` never reaches the in-container `--host` flag, which is always hardcoded to `0.0.0.0` — also already corrected inline in §12 item 3. Both corrections were made to §12's text itself before PR7's code was written, so the implementation matches the (corrected) design note with no further drift.

**`shlex = "1"` added to `Cargo.toml`** — no shlex-equivalent existed in the codebase before PR7; needed to split `custom_args` the same way upstream's own `shlex.split(custom_args)` does (quoted multi-word flag values survive the split correctly, unlike a naive `.split_whitespace()`).

**`ServerModeError` is a new, separate enum from `model_downloads::DownloadError`** (`Validation`/`NotFound`/`Internal`, no `Conflict` — there is no async job to race against, unlike downloads), with a `From<DownloadError> for ServerModeError` impl for `?`-propagation out of `resolve_downloaded_ds4_model()`.

**`LABEL_MANAGED = "io.brainrouter.managed"` is duplicated, not imported, between `src/server.rs` and `src/server_mode.rs`** — the original is a private `const` in `server.rs`; rather than making it `pub(crate)` for one cross-module use, `server_mode.rs` independently declares the same literal with a doc comment flagging the duplication as deliberate.

**Stop is a reasoned departure from upstream, not a silent one:** upstream only ever stops a Server Mode container via foreground Ctrl+C (which relies on the TUI's own attached-process SIGINT propagation) followed by `podman rm -f`. brainrouter's containers are started detached (`-d`, §12 item 1), so there is no foreground process to interrupt. `stop_ds4_server()` instead does a graceful `podman stop --time 10` (SIGTERM, 10s grace, then SIGKILL) followed by `podman rm -f` for guaranteed cleanup either way — both calls are idempotent no-ops if the container is already gone.

**§5b's serving-identity registration is explicitly *not* built in PR7 — a real scope narrowing versus §12 item 6's original framing, corrected in place above.** §12's pre-implementation note assumed a running ds4 server would render in the Sankey/benchmark dashboards via the already-shipped `status_only` bucket "for free." That is incorrect: `status_only` (§6a) is a value `derive_serving_identity()` assigns to an actual `RouteEvent`, and no `RouteEvent` is ever emitted for a ds4 Server Mode container, because PR7 never touches `src/daemon.rs`'s `Router::new`/provider list — a ds4 server started through the dashboard remains exactly as "invisible to the router" as §5b describes, `status_only` included, not just the full routed-traffic case. Building §5b's actual `{toolbox_backend, compute_api, runtime_profile_id, endpoint}` registration mechanism was judged out of scope for PR7 specifically because it is a **cross-backend** concern — the same registry needs to serve ds4/halogen/vllm/r9v alike, and building it against ds4 alone risks a shape that doesn't fit the other three once their own server-mode PRs land. **Decision: defer §5b's registration mechanism to its own dedicated PR** (inserted into Wave 2 after PR7-9 land, so all four new backends' server-mode shapes are known before the shared registry is designed), rather than building a ds4-only version now. Until then, a running server-mode container (any backend, once halogen/vllm/r9v ship) is visible **only** via each backend's own dashboard status panel (e.g. PR7's `#server-mode-view`), never in the Sankey or benchmark dashboards. This is flagged as a new open item below, not silently absorbed into "already done."

**HTTP surface, exactly as §12 item 7 proposed:** `GET /api/server-mode/ds4/status`, `POST /api/server-mode/ds4/start` (body: `toolbox_id`, `model_id`, `ctx`, `host`, `port`, optional `custom_args`), `POST /api/server-mode/ds4/stop`. All `POST /api/server-mode/*` paths are prefix-gated into the `is_destructive` allowlist in `src/server.rs`, mirroring the `/api/model-downloads`/`/api/toolbox-containers` prefix-gate convention (§5c/§10a) rather than enumerating each sub-path.

**Dashboard UI: a new top-level nav item ("Server Mode" / `#server-mode-view`), not folded into the existing "Downloads" view.** Starting a server is a distinct action from downloading a model (§12's own framing: Server Mode containers are structurally different from both the Toolboxes-tab dev-shells and the Downloads view's file-fetch jobs), so it gets its own nav entry and view, consistent with PR6's precedent of giving Downloads its own nav item rather than overloading the pre-existing "Models" (OMP routing-model) view. The new view has: a toolbox dropdown (ds4 entries for the current platform, reusing the existing `effectiveDefaultToolbox('ds4')`/`toolboxCatalogData` machinery to mark the default), a model dropdown restricted to ds4 models the local-presence sweep (`/api/model-downloads/status`) reports as `complete` (starting a server against an incomplete download is refused both client-side, via this filter, and server-side, via `resolve_downloaded_ds4_model`'s rejection), ctx/host/port fields pre-filled with upstream's own observed TUI defaults (`126000`/`localhost`/`8000`) as editable suggestions, a custom-args free-text field, and Start/Stop/Refresh-status buttons. Status is polled on-demand (Refresh button + after Start/Stop), not on an interval timer — unlike the Downloads view's 3s poll loop, there is no long-running job to track; a podman container's live state doesn't change without an explicit user action.

**Verification:** `cargo build --all-targets` clean; `cargo test --lib` → 237/237 (was 228 after PR6; PR7 added 9 `server_mode` tests: group-upgrade algorithm, sudo-arg cleaning, full command-shape assertion against the real vendored `amd-rocm` runtime_profile, host=`0.0.0.0` binding, custom-args shlex-split including an unterminated-quote rejection case, and toolbox-resolution success/failure); `cargo clippy --locked --all-targets` → no new warnings beyond the same pre-existing set noted in §6a/§10a; the dashboard's new `<script>` content was extracted from `main_dashboard.html` and syntax-checked with `new Function(...)` (same technique as §10a, since `scripts/test-benchmark-ui.cjs` only covers `benchmarks.html`).

### 13. PR8 pre-implementation design note: halogen Server Mode (added before PR8 implementation, 2025-09-19)
*(Written before PR8 starts, per this design doc's own established practice of a design-note pass before non-trivial implementation. Grounded in `ai_toolbox_cockpit/backends/halogen/{server,runner}.py` and `halogen_model_manager.py` — read live from GitHub during this pass, in full, not just call sites.)*

**What upstream's halogen Server Mode actually does (structurally different from ds4's in three real ways, not just a relabeling):**
1. **Bundle-based model resolution, not a single flat file.** A halogen "model" is a bundle of `checkpoint` + `overlay` (a checkpoint-override file) + `tokenizer_dir`, plus an optional `vision_tower` for vision-capable bundles — resolved via `halogen_model_manager.get_bundle()`/`incomplete_files()`, not ds4's single `-m /models/<rel_path>`.
2. **Every tunable is an environment variable, not a CLI flag.** `runner.py::build_server_cmd()` emits no in-container CLI args at all after the image — `HALOGEN_CHECKPOINT`, `HALOGEN_CK_OVERLAY`, `HALOGEN_TOKENIZER`, `HALOGEN_API_PORT`, `HALOGEN_CTX`, `HALOGEN_KV_POOL_POSITIONS`, `HALOGEN_KV_SLOTS`, `HALOGEN_PROMPT_CACHE`, and optionally `HALOGEN_VISION_TOWER` are the entire interface. This is a structural difference from ds4's CLI-flag shape, not a cosmetic one — brainrouter's `StartHalogenServerRequest` has no `custom_args` field at all, because upstream's own builder has nothing equivalent to escape-hatch into (unlike ds4, where `custom_args` mirrors `server_runner.py`'s own identically-named parameter).
3. **A `platform_id` parameter is validated inside the builder** (`build_server_cmd(platform_id=...)` rejects anything other than the literal string `"strix-halo"`) — ds4's builder needed no such parameter. Since exactly one platform (`strix-halo`) owns the halogen toolbox in vendored catalog data today, and platform `toolbox_ids` sets are disjoint, this is derived server-side via a new `Catalog::platform_id_for_toolbox()` reverse lookup rather than exposed as a redundant request field the operator would have to also get right.

**Validation bounds (verified against the full function body, mirroring §12's "read the whole function, not just the call site" discipline that already paid off once for ds4):** `context_size` in `[1, 262144]`; `kv_pool_positions >= context_size`; `kv_slots >= 1`; `prompt_cache` in `{"0", "1", "2"}`; `host` must parse as a literal IP address or the string `"localhost"` (unlike ds4, a bare hostname like `"myhost"` is rejected, not silently passed through — IPv6 addresses are bracket-wrapped for the podman port mapping); `models_dir` must not contain `:` (podman volume-mount syntax collision).

**A genuine fidelity detail, not an oversight if omitted:** `runner.py`'s builder calls `upgrade_groups_for_podman()` (already ported in PR7, §12 item 4) but does **not** call `_clean_engine_args()` (the `--group-add sudo` strip ds4's builder applies). Read the full function body to confirm this is deliberate upstream, not a ds4-only helper that happens to also apply elsewhere — it does not, for halogen. Mirrored precisely: `build_halogen_server_command()` applies group-upgrading but not sudo-stripping.

**Config.json persistence — new for PR8, but designed as a reusable primitive, not a halogen-specific one.** Unlike ds4 (confirmed zero `save_backend_settings` calls in its `server.py`), halogen's `_start_confirmed()` persists `host`/`port`/`context`/`pool`/`slots`/`prompt_cache`/`bundle_id`/`engine`/`models_dir` into cockpit's shared config.json on every successful start (all values stored as JSON **strings**, matching upstream's own `Input.value`-is-always-a-string storage shape). vllm and r9v were grep-confirmed to make the same kind of call, so PR8 adds a new generic `cockpit_config::apply_backend_setting_values(backend_id, updates)` helper (merge-into-`BackendSettings::extra`, reusing the existing atomic-write core) rather than a one-off halogen writer — PR9/PR10 (vllm/r9v) are expected to reuse it unchanged. brainrouter's own write deliberately narrows upstream's 9 keys to 7: `models_dir` and `engine` are **not** persisted, because brainrouter has no user-facing "select an engine" concept (podman-only) and `models_dir` is already derived automatically via `effective_models_dir()`, never operator-supplied — flagged explicitly as a scope narrowing, not a silent omission.

**A cross-backend bug found while implementing this note, not merely while implementing PR8's code — corrected here and in PR7's own code retroactively (full account in §13a and a new Decision log entry):** PR7's `resolve_ds4_toolbox()` rejected any toolbox whose `features.server != FeatureState::Supported`. Every vendored vllm/r9v/halogen toolbox is `server: experimental` in `assets/cockpit-catalog/toolboxes.json` — only ds4's happens to be `Supported`, which is why PR7's check appeared to work. Upstream's own TUI (`server.py::set_platform()`) filters with `feature_state("server") != "unavailable"`, i.e. `Experimental` is fully selectable, only UI-badged, never blocked. Left uncorrected, PR8's halogen toolbox (the only one that exists) would be 100% rejected by the very check meant to validate it. **Corrected now, applied to both ds4 and halogen identically** via a shared `resolve_toolbox_for_server()` that rejects only `Unavailable`.

**Proposed native-Rust scope for PR8 v1 (mirrors §12's framing, no new narrowing beyond what §12 already established for detached/single-instance/synchronous-lifecycle server mode):**
1. Detached, named (`brainrouter-halogen-server`, not upstream's own `ai-toolbox-cockpit-halogen-server`), labeled container — same ownership-label convention as ds4 (§5b/PR7), so cockpit itself can also be running on the same host without ambiguity.
2. `StartHalogenServerRequest { toolbox_id, bundle_id, host, port, context_size, kv_pool_positions, kv_slots, prompt_cache }` — no `custom_args` (see structural point 2 above).
3. Single instance, standalone-only (halogen upstream itself has no multi-node concept to defer, unlike ds4 — this is not a narrowing, there is nothing more to port).
4. Synchronous request/response lifecycle, live `podman inspect` status, no persisted DB table — same as §12 item 5.
5. New HTTP surface: `GET /api/server-mode/halogen/status`, `POST /api/server-mode/halogen/start`, `POST /api/server-mode/halogen/stop`, covered by the already-generic `path.starts_with("/api/server-mode/")` destructive-route prefix gate (§9) with no gate change needed.
6. Same Sankey/benchmark deferral as ds4 (§12a): a running halogen server is dashboard-visible only, not yet reachable via `status_only` or any Sankey/benchmark bucket, until PR 11a's serving-identity registration lands.

**Open items flagged for explicit sign-off, not silently decided:**
- Whether the 2 keys brainrouter deliberately does not persist into config.json (`models_dir`, `engine`) should ever be added later if a future UI need for them emerges (e.g. if brainrouter grows a non-podman engine concept). Proposed default: leave unpersisted until such a need is concrete — speculative persistence of unused keys risks a config.json write that looks meaningful but is dead weight.
- Whether halogen's fixed single-instance-per-host model should ever allow more than one bundle to be served concurrently (e.g. a vision-capable bundle alongside a text-only one). Proposed default: no, matching upstream's own single-fixed-container-name design, same rationale as §12's ds4 equivalent decision.

### 13a. PR8 implementation decisions (added during PR8 implementation, 2025-09-19)
Mirrors §12a's format. Records the concrete shape PR8 landed in, and where it diverges from §13's proposal.

**No new module — `server_mode.rs` stays one file across backends, not split into `server_mode/{ds4,halogen}.rs`.** Explicitly considered and rejected splitting: `model_downloads.rs` already established the "one file, per-backend `match` dispatch" convention for this rollout (§10a), and `server_mode.rs` at ~700 lines after adding halogen is still smaller than several other single-purpose modules in this codebase. The module's top doc comment now documents both PR7 and PR8 and states this organizational decision explicitly, so a future PR9/PR10 contributor doesn't have to re-derive it.

**Toolbox-resolution refactor, with the Unavailable-vs-Supported bug fix folded in.** `resolve_ds4_toolbox()`'s body became a thin wrapper around a new shared `load_typed_toolbox_catalog()` + `resolve_toolbox_for_server(catalog, backend, toolbox_id)`, which is where the fix described in §13 actually lives — `resolve_toolbox_for_server` now rejects only `FeatureState::Unavailable`, accepting both `Supported` and `Experimental`. This is a **behavior change to already-shipped PR7 code**, not just new PR8 code — recorded as its own Decision log entry below rather than folded silently into "PR8 additions," per this project's standing rule to document corrections explicitly. It is behavior-neutral for ds4 (all vendored ds4 toolboxes are already `Supported`, so the fix changes nothing observable there) and load-bearing for halogen (its only vendored toolbox is `Experimental` and would otherwise always be rejected). A new regression test, `resolve_toolbox_for_server_accepts_experimental_not_just_supported`, exercises this directly against the real vendored halogen toolbox.

**`platform_id` derivation: `Catalog::platform_id_for_toolbox()`, a new reverse-lookup method on `toolbox_catalog::types::Catalog`.** First-match iteration over `platforms[].toolbox_ids`, documented as relying on the (verified-true-in-vendored-data) assumption that platform `toolbox_ids` sets are disjoint; a unit test (`platform_id_for_toolbox_finds_the_owning_platform`) exercises it against the real vendored halogen toolbox/`strix-halo` pairing. If a future catalog update ever lists the same toolbox under two platforms, first-match would silently pick one — flagged as a latent limitation in the method's own doc comment, not treated as a currently-live risk since today's data doesn't exhibit it.

**Bundle resolution: `ResolvedHalogenBundle` + `resolve_downloaded_halogen_bundle()`, added to `model_downloads.rs` right after `resolve_downloaded_ds4_model()`.** Mirrors that function's shape exactly (`{ models_dir, checkpoint, overlay, tokenizer_dir, vision_tower: Option<String> }`, rejecting any bundle whose `CompletenessResult` isn't `Complete`) — reuses the same private `resolve_catalog_entry`/`effective_models_dir`/completeness-check helpers, no duplicated resolution logic. No dedicated unit test was added for the resolver itself, consistent with the pre-existing gap for `resolve_downloaded_ds4_model()` (hard to unit test without mocking the filesystem and cockpit_config together) — flagged as an existing gap this PR does not close, not a new one it introduces.

**Config-write helper: `cockpit_config::apply_backend_setting_values(backend_id, updates: Vec<(&str, Value)>)`, generic from the start.** Built on the existing `apply()` atomic-write core (same UUID-temp-file/`mode(0o600)`/`sync_all`/`rename()` pattern as §4a/§12a), merging the given key/value pairs into `BackendSettings::extra` (the flattened per-backend JSON map) rather than requiring a typed Rust struct per backend's settings shape — chosen because the 9 upstream keys are backend-specific and not worth modeling as first-class Rust fields when they're written-only, never read back by brainrouter itself. `start_halogen_server()` calls it with brainrouter's narrowed 7-key set (§13) after a successful container start, and the write is best-effort: a failure to persist to config.json never fails the HTTP request (the server is already running at that point; failing the whole request over a config.json write failure would be a worse user experience than a server that starts but isn't recorded). A new unit test, `backend_setting_values_merge_into_extra_and_round_trip`, exercises the merge-and-persist behavior directly against the mutate+write core (the same testing approach `apply_default_toolbox_preserves_unrelated_keys_round_trip` already established, since `apply()`'s `config_path()` isn't overridable in-process).

**HTTP surface, exactly as §13 item 5 proposed:** `GET /api/server-mode/halogen/status`, `POST /api/server-mode/halogen/start` (body: `toolbox_id`, `bundle_id`, `host`, `port`, `context_size`, `kv_pool_positions`, `kv_slots`, `prompt_cache`), `POST /api/server-mode/halogen/stop`. No destructive-route allowlist change was needed — the existing `path.starts_with("/api/server-mode/")` prefix clause (added in PR7, §12a) already covers the new sub-paths.

**Dashboard UI: `#server-mode-view` converted from PR7's single hardcoded ds4 card into a backend-tabs bar + per-backend panel, not a second nav item or a hardcoded second card.** Mirrors the already-established `#toolbox-models-backend-tabs`/`DOWNLOAD_CAPABLE_BACKENDS`/`toolboxModelsActiveBackend` pattern from the Downloads view (§10a): a new `SERVER_MODE_CAPABLE_BACKENDS = ['ds4', 'halogen']` array drives `renderServerModeBackendTabs()`/`setServerModeBackend()`, reusing the existing `TOOLBOX_BACKEND_LABELS` constant for tab text. This was judged more correct than either adding a whole second nav entry (Server Mode is one concept, not two) or silently leaving ds4 hardcoded and bolting halogen on beside it (which would look like two different features, not one tabbed one) — and it mirrors cockpit's own TUI information architecture of one `BackendServerPanel` per backend under a single "Server Mode" screen. The halogen panel adds all 8 request fields (toolbox/bundle dropdowns, host/port/context-size/kv-pool/kv-slots inputs, a prompt-cache `<select>` with upstream's own default `"2"` pre-selected) plus Start/Stop/Refresh buttons, with `renderServerModeHalogenToolboxOptions()`/`renderServerModeHalogenBundleOptions()`/`renderServerModeHalogenStatus()`/`startHalogenServer()`/`stopHalogenServer()` as direct halogen-flavored siblings of PR7's ds4 functions (not a generalized single set of backend-parameterized functions — judged not worth the indirection for 2 backends with genuinely different field sets; revisit if a 3rd backend's server-mode panel would otherwise mean a 3rd near-duplicate function set). `loadServerMode()` now fetches both backends' status on every load/refresh (cheap `podman inspect`-backed calls) regardless of which tab is active, so switching tabs never needs a network round-trip.

**Verification:** `cargo build --lib` clean; `cargo test --lib` → 251/251 (was 237 after PR7; PR8 added 14 tests: `platform_id_for_toolbox_finds_the_owning_platform`, `resolve_toolbox_for_server_accepts_experimental_not_just_supported`, `resolve_halogen_toolbox_finds_the_real_vendored_entry_and_platform`, `resolve_halogen_toolbox_rejects_non_halogen_backend`, `validate_halogen_host_normalizes_localhost_and_rejects_bare_hostnames`, 8 `build_halogen_server_command_*` tests covering the full env-var/label/port/volume shape and every validation rejection path, and `backend_setting_values_merge_into_extra_and_round_trip`); `cargo clippy --locked --all-targets` → no new warnings beyond the same pre-existing set noted in §6a/§10a/§12a (all 8 warnings are in files this PR did not touch); the dashboard's 5 `<script>` blocks were extracted from `main_dashboard.html` and syntax-checked with `new Function(...)` (same technique as §10a/§12a).

### 14. PR9 pre-implementation design note: vllm Server Mode (added before PR9 implementation, 2025-09-19)
*(Written before PR9 starts, per this design doc's established practice. Grounded in `ai_toolbox_cockpit/backends/vllm/{server,runner}.py` — read live from GitHub during this pass, in full — plus `runtime/engines.py` for `detect_container_engines()`/`adapt_nvidia_runtime_args()`, and the real vendored `assets/cockpit-catalog/{models,toolboxes}.json` vllm entries, not just the schema.)*

**What upstream's vllm Server Mode actually does (structurally different from both ds4 and halogen in six real ways):**
1. **Model identity is a raw Hugging Face repo string, not a catalog-file-path or bundle.** `model_id` (e.g. `meta-llama/Meta-Llama-3.1-8B-Instruct`) is passed straight to `vllm serve <model_id>` inside the container — vLLM itself pulls the weights from HF at server-start time. There is no "already downloaded, is it complete" gate the way ds4/halogen have (this is exactly why vllm is already excluded from PR6's Downloads tab — confirmed by `model_downloads.rs`'s existing `resolve_catalog_entry()` early-rejecting `SupportedServingBackend::Vllm` with an explicit comment). Upstream's own TUI additionally accepts a free-form custom HF repo (bypassing the catalog dropdown entirely) alongside the curated model list — both paths are ported (§ item 1 below), since supporting both is essentially free and is not a narrowing of upstream.
2. **A catalog model entry doubles as its own launch "policy."** Verified against the 15 real vendored vllm entries: every entry carries `id`/`name`/`repo`/`trust_remote`/`valid_tp`/`max_num_seqs`/`max_tokens` (already typed on `VllmModel`), plus *optional* `ctx`, `enforce_eager`, `attention_backend` (sometimes explicitly JSON `null`, meaning "do not pass `--attention-backend` at all — the image/model already defaults correctly," distinct from the key being *absent*, which means "pass upstream's own default, `TRITON_ATTN`"), `attention_backend_label` (UI-only, ignored), `env` (a string→string map merged into the container's `-e` list), and `extra_flags` (a literal flag list appended verbatim) — all already losslessly captured by `VllmModel::extra`'s `#[serde(flatten)]` map today, confirmed live against the vendored data, **no `VllmModel` struct changes needed for PR9.** A custom (non-catalog) HF repo gets upstream's own generic fallback policy: `{valid_tp: [1, 2], attention_backend: "TRITON_ATTN", extra_flags: [], env: {}}`.
3. **Per-toolbox policy overrides.** The one vendored non-strix-halo vllm toolbox (`gb10-vllm-cu130`) carries a real `backend_config.policy_overrides` object (`{valid_tp, attention_backend: null, attention_backend_label, env}`) that upstream's `apply_toolbox_policy_overrides()` shallow-merges (`dict.update()` semantics — whole keys replaced, not deep-merged) on top of the model's own policy before launch. `ToolboxDefinition::backend_config: Option<Value>` (already generic, §5c) already carries this losslessly; PR9 adds a small `effective_vllm_policy(model_extra, toolbox_backend_config)` merge helper, not new typed fields.
4. **Podman *and* Docker are both real upstream engines (`detect_container_engines()`), with different flag sets** (`--security-opt label=disable --userns=keep-id` for podman vs. `--user uid:gid` for docker) and Docker gets an additional NVIDIA-GPU flag rewrite (`adapt_nvidia_runtime_args()`, only relevant for the one NVIDIA GB10 toolbox). **brainrouter remains podman-only, same standing decision as ds4/halogen (§12/§13, "no user-facing engine-selection concept")** — `adapt_nvidia_runtime_args()` is therefore a no-op for every real, in-scope brainrouter toolbox today and is not ported, exactly the same reasoning already recorded for ds4's builder. Flagged explicitly as a real (not new) scope narrowing versus upstream, not a silent gap.
5. **Persistent cache-directory mounts, not a read-only models directory.** Four host paths — `hf_cache` (default `~/.cache/huggingface`), `vllm_cache` (`~/.cache/vllm`), `triton_cache` (`~/.cache/triton`), `aiter_cache` (`~/.aiter`) — are mounted read-write and are independently operator-configurable, persisted via upstream's own **separate, explicit** `save_backend_settings("vllm", {...})` call wired to a distinct "Save Cache Paths" button — **not** bundled into server start. Also: an optional `reset_caches` action wipes the vllm/triton/aiter (never huggingface) directories immediately before launch, gated by upstream's own `validate_compiled_cache_roots()` safety check (rejects `/`, the home directory, paths shorter than 3 components, or any path missing an expected marker path component) before allowing any deletion.
6. **`hf_token` is not blocking.** Upstream's own env-var fallback is `f"HF_TOKEN={token}"` if a token is known, else the bare literal `-e HF_TOKEN` (ambient passthrough — podman/docker forward the *host process's own* `HF_TOKEN` env var, if any, when no `=value` is given). Upstream's interactive prompt-if-missing modal has no headless equivalent and is not ported; brainrouter reads `cockpit_config::load().config.hf_token` (already read-only since PR6, §10) and replicates the exact same two-branch fallback — never blocks a start on a missing token.

**A correction to a claim made before PR9's research pass (recorded here so it isn't silently lost): vllm does *not* auto-persist run-time server settings (host/port/tensor-parallel/etc.) into config.json on server start**, unlike halogen (§13). Reading `server.py` in full and grepping confirms the *only* `save_backend_settings("vllm", ...)` call site is inside `save_caches_pressed()` (the explicit "Save Cache Paths" button), never inside `_start_confirmed()`. PR9 therefore does **not** call `apply_backend_setting_values("vllm", ...)` from `start_vllm_server()` the way PR8 does from `start_halogen_server()` — doing so would be a **deliberate brainrouter-side enhancement beyond upstream fidelity**, not a port, so it is not done silently here; see Open items below for whether it should be added anyway for UX parity with halogen. What PR9 *does* reuse `apply_backend_setting_values()` for is a new, separate `POST /api/server-mode/vllm/cache-paths` endpoint — the direct analogue of upstream's own separate "Save Cache Paths" button, preserving the same two-actions-not-one shape upstream has.

**Validated request parameters (mirroring upstream's own validation, verified against the full function body):** `tensor_parallel` must be a member of the effective policy's `valid_tp` list (reject otherwise, mirroring ds4/halogen's validation-heavy style); `gpu_memory_utilization` in `(0.0, 1.0]`; `max_model_len` accepts `"auto"` (resolves to the effective policy's own `ctx` if present, else the literal string `"auto"` — **`--max-model-len` is always emitted, never omitted**; corrected from an earlier draft of this note after re-reading `build_server_cmd()`'s literal `always_flags`, see §14a) or a positive integer string; `attention_backend` may not be operator-overridden when the effective policy's `attention_backend` key is explicitly JSON `null` (mirrors the TUI locking that selector) — supplying one in the request in that case is a validation error, not a silently-ignored value; `dtype` defaults to `"auto"`; `extra_args` is shell-word-split via `shlex` (same escape-hatch pattern as ds4's `custom_args`).

**Proposed native-Rust scope for PR9 v1:**
1. `StartVllmServerRequest { toolbox_id, model_id: Option<String>, custom_repo: Option<String>, host, port, tensor_parallel, max_num_seqs, max_model_len, gpu_memory_utilization, attention_backend: Option<String>, enforce_eager: Option<bool>, dtype, api_key: Option<String>, extra_args: Option<String>, reset_caches: bool }` — exactly one of `model_id`/`custom_repo` required (validation error otherwise, mirroring the TUI's own mutually-exclusive dropdown/free-text fields).
2. `build_vllm_server_command()` resolves the effective policy (item 2/3 above), validates the bounds above, mounts the four cache directories (reading overrides from `cockpit_config`'s `BackendSettings::extra`, falling back to upstream's own defaults, `mkdir -p`-ing each before launch — matching upstream), emits `env`/`extra_flags` from the policy, and appends `extra_args`.
3. Detached, named (`brainrouter-vllm-server`), labeled container — same ownership convention as ds4/halogen.
4. Single instance, synchronous lifecycle, live `podman inspect` status — same as §12 item 5/§13 item 4, no persisted DB table.
5. New HTTP surface: `GET /api/server-mode/vllm/status`, `POST /api/server-mode/vllm/start`, `POST /api/server-mode/vllm/stop`, `POST /api/server-mode/vllm/cache-paths` — all covered by the existing `path.starts_with("/api/server-mode/")` prefix gate (§9), no gate change needed.
6. Same Sankey/benchmark deferral as ds4/halogen (§12a/§13a): dashboard-visible only, until PR 11a's serving-identity registration lands.

**Open items flagged for explicit sign-off, not silently decided:**
- Whether `start_vllm_server()` should *also* persist run-time settings into config.json on start (for UX parity with halogen — e.g. so the dashboard can pre-fill the last-used host/port/tensor-parallel), even though upstream itself does not do this for vllm. Proposed default: **no** — match upstream's actual behavior exactly (settings persistence is cache-paths-only, via its own explicit endpoint), and revisit only if the user explicitly wants brainrouter to diverge from upstream here for convenience.
- Whether Docker-engine support (item 4 above) is ever worth adding for the one NVIDIA GB10 toolbox, given brainrouter's standing podman-only decision. Proposed default: no, until brainrouter has any other reason to run on non-podman hardware — narrowly scoped to "if a user with a GB10 host asks for it."
- Whether `reset_caches` (item 5 above, a real data-deletion action) belongs in v1 at all, or should ship gated behind the same kind of explicit confirmation UX the dashboard already uses for destructive toolbox operations (§9). Proposed default: ship it as an explicit opt-in boolean on the start request (defaults to `false`), reusing upstream's own `validate_compiled_cache_roots()` safety semantics verbatim before any deletion — not a bare "always wipe" toggle.

### 14a. PR9 implementation decisions (added during PR9 implementation, 2025-09-19)
Mirrors §12a/§13a's format. Records the concrete shape PR9 landed in, and where it diverges from or corrects §14's proposal.

**No new module — `server_mode.rs` stays one file across ds4/halogen/vllm, same standing decision as §13a.** The vllm section (~450 lines) was added right after halogen's, before `mod tests`; the module's top doc comment now mentions PR9 too.

**A correction to §14 itself, found while writing `build_vllm_server_command()`: `--max-model-len` is *always* emitted, never omitted for `"auto"`.** §14's original "Validated request parameters" bullet claimed `"auto"` omits the flag entirely; re-reading `runner.py::build_server_cmd()`'s literal `always_flags` list during implementation showed `--max-model-len` is unconditional — it resolves to `policy.get("ctx", "auto")` when the request's own value is `"auto"`, otherwise the request's literal value, but the flag itself is never skipped. Fixed in §14 directly above (rather than left silently wrong) since it's a factual claim about shipped behavior, not a decision under negotiation. `build_vllm_server_command_resolves_max_model_len_from_policy_ctx_when_auto` and `build_vllm_server_command_explicit_max_model_len_overrides_policy_ctx` cover both branches.

**`VllmEffectivePolicy.attention_backend: Option<Option<String>>` — the tri-state carried through explicitly, not collapsed.** This is the one new type-design idea PR9 needed beyond ds4/halogen's flatter request/response structs: preserving upstream's three-way JSON semantic (key absent from the merged policy map → apply `"TRITON_ATTN"`; key present as JSON `null` → model-specific, no flag, no override permitted; key present as a string → that string is the default, operator override permitted) requires an `Option<Option<String>>`, not a single `Option<String>` (which would conflate "absent" and "explicit null"). `resolve_vllm_effective_policy()` builds it directly off the raw `serde_json::Map`'s own `Value` (which already preserves the distinction), and `resolve_vllm_effective_policy_distinguishes_absent_vs_null_vs_string_attention_backend` asserts all three branches directly.

**Deliberate, explicitly-flagged divergence from upstream, confirmed in code as designed in §14's open items: brainrouter *rejects* (HTTP 400) an `attention_backend` override when the model's policy locks it (`Some(None)`), rather than upstream's own silent-ignore.** Upstream's only enforcement is disabling the TUI widget; a headless HTTP API has no equivalent affordance, so accepting-and-ignoring a client-supplied field would be a worse API contract than rejecting it outright. `build_vllm_server_command_rejects_attention_backend_override_when_model_specific` and `build_vllm_server_command_omits_attention_backend_flag_when_model_specific` cover the reject-with-override and silent-no-flag-without-override cases respectively.

**§14's first open item (auto-persist-on-start UX parity) resolved as proposed: no.** `start_vllm_server()` does not call `apply_backend_setting_values()` — only the new, separate `save_vllm_cache_paths()` does, exactly matching upstream's own two-actions-not-one shape. This is vllm's **first reuse** of the generic `apply_backend_setting_values()` helper built in §13a for halogen, confirming it works unmodified for a second backend.

**§14's second open item (Docker engine support) resolved as proposed: not ported.** `build_vllm_server_command()` only emits the podman-specific `--security-opt label=disable --userns=keep-id` branch, same standing podman-only decision as ds4/halogen.

**§14's third open item (`reset_caches` in v1) resolved as proposed: shipped as an explicit opt-in `bool` (default `false`) on `StartVllmServerRequest`, gated by a Rust port of upstream's `validate_compiled_cache_roots()` safety check (`validate_compiled_cache_root()`, one path at a time) before any deletion.** Rejects `/`, the home directory, paths with fewer than 3 components, or any path missing the expected marker substring (`vllm`/`triton`/`aiter`) in one of its own components — run immediately before `reset_vllm_compiled_caches()` wipes only the vllm/triton/aiter directories' *contents* (never the directory itself, never huggingface), matching upstream's own `_clear_compiled_caches()` exactly. Uses `Path::canonicalize()` (requires existence) rather than Python's `resolve(strict=False)`; acceptable because `start_vllm_server()` always `create_dir_all`s all four cache directories before the reset step runs, so they're guaranteed to exist by validation time.

**`model_downloads::expand_tilde()` changed from private to `pub(crate)`** so `server_mode.rs` could reuse it for cache-path expansion (both the request-supplied override in `effective_vllm_cache_dirs()` and the hardcoded upstream defaults) rather than duplicating tilde-expansion logic a second time.

**HTTP surface, exactly as §14 item 5 proposed:** `GET /api/server-mode/vllm/status`, `POST /api/server-mode/vllm/start` (body: `toolbox_id`, `model_id`|`custom_repo`, `host`, `port`, `tensor_parallel`, `max_num_seqs`, `max_model_len`, `gpu_memory_utilization`, `attention_backend`, `enforce_eager`, `dtype`, `api_key`, `extra_args`, `reset_caches`), `POST /api/server-mode/vllm/stop`, `POST /api/server-mode/vllm/cache-paths` (body: `hf_cache`/`vllm_cache`/`triton_cache`/`aiter_cache`, all optional, at least one required). No destructive-route allowlist change was needed, same as §13a.

**Dashboard UI: a third `#sm-panel-vllm` tab added to the existing backend-tabs bar (`SERVER_MODE_CAPABLE_BACKENDS` now `['ds4', 'halogen', 'vllm']`), plus a distinct always-tab-visible `#sm-panel-vllm-caches` card for the separate cache-paths save action — not folded into the same card as start/stop, to keep the "two actions, not one" upstream shape visible in the UI, not just in the API.** The vllm panel's model picker is a dropdown of already-downloaded catalog entries (same `presenceFor()`-gated pattern as ds4/halogen) *plus* an adjacent free-text "custom repo" field, with client-side validation requiring exactly one of the two (mirroring the server-side `resolve_vllm_model_and_base_policy()` validation, not just deferring to the API's 400). `renderServerModeVllmToolboxOptions()`/`renderServerModeVllmModelOptions()`/`renderServerModeVllmStatus()`/`startVllmServer()`/`stopVllmServer()`/`saveVllmCachePaths()` are direct vllm-flavored siblings of the ds4/halogen functions, following §13a's established "no premature backend-parameterized generalization" call. `loadServerMode()` now fetches all three backends' status on every load, same as before.

**Verification:** `cargo build --lib`/`cargo build --all-targets` clean (one pre-existing `private_interfaces` warning surfaced immediately, fixed by making `VllmEffectivePolicy`/`VllmCacheDirs` `pub(crate)` and `build_vllm_server_command` `pub(crate)` rather than `pub`, since nothing outside the crate calls it); `cargo test --lib` → 275/275 (was 251 after PR8; PR9 added 24 new tests: `resolve_vllm_toolbox_finds_the_real_vendored_entry_and_its_runtime_profile`, `resolve_vllm_toolbox_rejects_non_vllm_backend`, `resolve_vllm_model_and_base_policy_{rejects_both_and_neither, resolves_real_catalog_entry_with_explicit_null_attention_backend, rejects_unknown_id, uses_generic_default_for_custom_repo}`, `apply_vllm_toolbox_policy_overrides_shallow_merges_and_replaces_whole_keys`, `resolve_vllm_effective_policy_{distinguishes_absent_vs_null_vs_string_attention_backend, falls_back_to_valid_tp_one_when_absent_or_empty}`, 13 `build_vllm_server_command_*` tests covering the full command shape (env vars, volume mounts, labels, port binding), every validation rejection path, both `max_model_len` resolution branches, both `attention_backend` override/lock branches, and `shlex`-split `extra_args`, `validate_compiled_cache_root_rejects_root_home_and_missing_marker`, `save_vllm_cache_paths_rejects_when_nothing_provided`); one new `cargo clippy --locked --all-targets` warning surfaced from freshly-written code (`clippy::cmp_owned` on a `PathBuf::from("/")` comparison in `validate_compiled_cache_root`) and was fixed immediately (`std::path::Path::new("/")` instead), leaving clippy back at the same 8 pre-existing warnings noted since §6a; the dashboard's 5 `<script>` blocks were syntax-checked with `new Function(...)`, and `scripts/test-benchmark-ui.cjs` stayed at 32/32 (unaffected, confirming no regression to the benchmark UI this PR didn't touch).

## Alternatives considered

**Catalog sync: vendor vs. runtime fetch.** *Rejected:* live runtime fetch of `models.json`/`toolboxes.json` on every dashboard load or on a timer, mirroring `hub_toolbox_tag_dates()`. *Why rejected:* those two files drive validation and form-generation, not just a freshness badge; an unreviewed upstream change could silently alter brainrouter's UI/behavior, and it adds a hard network dependency for core functionality that today works fully offline. *Chosen instead:* vendor + reviewed weekly sync PR (§3), keeping only true "is there a newer image" checks live (now with a caching/TTL/bounded-concurrency design, since the check now fans out to N images — §3).

**Config.json: bidirectional shared file vs. read-only import + brainrouter's own store.** *v1 chose:* full read-modify-write against the same file/format. *Revised in v2, superseded (see Decision log):* the Dory critic argued unconditional automatic RMW against a file a second live, unlocked process may be concurrently writing is riskier than the user's "ideally merging" ask requires, compounded by the HOME/config-path mismatch risk (§4). *Chosen instead:* Phase 1 read-only import + an explicit, user-triggered single-write "apply" action; true automatic bidirectional merge deferred pending either a jointly-supported protocol or explicit user sign-off that they want the riskier behavior anyway (Open questions). This is the most consequential change from v1 to v2 and is flagged for the user's explicit reaction, since it is a narrower interpretation of requirement 3 than v1 shipped.

**Toolbox model data: one generic struct vs. per-backend enum.** *Chosen (unchanged from v1, reaffirmed by review):* `ModelPayload` enum per backend, because `catalog/schema.py`'s validation rules for r9v/halogen/llama_cpp are structurally incompatible, directly mirroring cockpit's own `backends/<backend>/` code-organization decision. **New in v2:** the *outer* catalog-id/listing layer around this enum is now open/lossless (`CatalogBackendId`), not closed — see §2 — to fix the future-backend/comfyui parsing contradiction the closed-enum-only version had.

**Server-mode launch: foreground-suspend-TUI vs. detached daemon container.** *Chosen (unchanged from v1):* detached, named, labeled containers brainrouter can start/stop/inspect asynchronously, because brainrouter's dashboard is a long-running HTTP server with no TTY to suspend into on behalf of a browser tab. **New in v2:** this choice now has an explicit, load-bearing consequence spelled out (§5b) — detached containers are, by default, invisible to the request router, so the "populate Sankey/benchmarks from real traffic" promise requires the new registration sub-phase, not just "launch it and the columns will populate themselves."

**`toolbox create` vs. raw `podman create`/`podman run` as the deployment primitive (new in v2).** *Not yet decided — presented as a live alternative, not resolved unilaterally,* because whether `toolbox create` can forward the podman flags (`--device`, `--security-opt`, `--group-add`) that R9V's ROCm passthrough and similar profiles need is unverified (§5's blocking finding #5). If a spike shows `toolbox create` cannot forward these flags, the plan would switch to using bare `podman create`/`podman run` directly for any toolbox whose `runtime_profile.engine_args` needs flags `toolbox` doesn't support, keeping the `toolbox` wrapper only for the current default/Vulkan profile where it's already proven to work. Recorded here rather than picked so the spike's actual result drives the decision, not this document's guess.

**Benchmark schema: widen `runtimes` vs. new `serving_runtimes` table (new in v2).** *Not yet decided — presented as a live alternative* (§7) because the Dory critic showed both have real costs: widening risks changing an existing, populated, `NOT NULL`-constrained table's contract; a new table avoids that but adds a second "what served this run" concept side by side with the first. Flagged for a decision during PR planning for the benchmark-extension phase, informed by how disruptive each is once actually attempted against the existing `run_summary` view and its consumers.

## Detailed implementation
*(File-by-file plan; every path is either an existing file already inspected above, or explicitly marked **new/proposed**. Rows added or materially changed in v2 are marked.)*

| File | Change type | Rationale |
|---|---|---|
| `src/server.rs:1886-1909` | modify | **v2:** Add `--name`/`--replace` **and** a timeout-branch `podman rm -f` best-effort cleanup (§5a) — the missing-`--name` framing alone was incomplete per Dory critic review. Independent of everything else — do first. |
| `src/server.rs:224-241` (`is_destructive` allowlist) | modify | **New in v2.** Every new mutating route from this plan must be added here explicitly (§9) — not automatically covered. |
| `src/server.rs:526-527, 1889, 1919, 1839-1858, 2044` | modify | Generalize from one hardcoded image/repo to iterating the vendored catalog's toolbox list; extend `hub_toolbox_tag_dates()` to N images with the new caching/TTL/timeout design (§3). |
| `assets/cockpit-catalog/{models,toolboxes}.json` + `SOURCE` | **new** | Vendored, pinned, schema-checked snapshot from upstream. |
| `scripts/sync-toolbox-catalog.sh` | **new** | Fetch + lossless-structural-validate + diff against pinned commit; used by CI + manually. |
| `.github/workflows/sync-toolbox-catalog.yml` (or equivalent CI config) | **new, added in v2** (was described in prose only in v1, not enumerated — Dory readiness finding) | Weekly scheduled run of the sync script; opens a PR on content diff. |
| `src/toolbox_catalog.rs` (+ `backends/{llama_cpp,ds4,halogen,vllm,r9v}.rs`) | **new** | `CatalogBackendId`/`SupportedServingBackend`/`ModelPayload` types (§2) + per-backend command builders (§5). |
| `src/toolbox_catalog/schema_validate.rs` (or a small `catalog-schema` crate module) | **new, added in v2** (was implied, not enumerated — Dory readiness finding) | The Rust structural-validation port of `catalog/schema.py`'s shape, used by both the sync script and (defensively) at runtime load. |
| `src/cockpit_config.rs` | **new** | `~/.config/ai-toolbox-cockpit/config.json` read (Phase 1) + explicit single-write "apply" action (§4). |
| `migrations/0003_toolbox_catalog.sql` | **new** | `toolbox_catalog_snapshot` table (§2) — **must also be registered in whichever migration runner owns it** (see next row). |
| `src/benchmark.rs:1895-1953` (schema version gate) | modify, **added in v2** (Dory critic blocking/important finding — this was missing from v1's file list entirely) | Extend the hardcoded version-1/2 acceptance logic to recognize version 3 (toolbox catalog snapshot table, if co-located in the same DB) and version 4 (benchmark toolbox-family dimension, §7) with their own expected-table/column checks. Without this, the new `.sql` migration files never execute. |
| `migrations/0004_benchmark_toolbox_dims.sql` | **new** | Adds the toolbox-family/toolbox-id dimension per whichever of §7's (a)/(b) options is chosen, plus `catalog_snapshot_id`/`source_commit` anchoring. |
| `src/benchmark.rs` (`RuntimeDefinition`/new `ServingRuntimeDefinition`, `RunSummary`, `RunQuery`, ingest at `:2248-2273`) | modify | Add fields per §7's chosen option + filters; re-audit `INSERT OR IGNORE` uniqueness semantics against the new dimension. |
| `src/benchmark/http.rs`, `.../imports.rs` | modify | Accept `toolbox_family`/`toolbox_id` in ingest/prepare payloads; expose in `/api/benchmarks/filters`. |
| `src/routing_events.rs:30-82` (`RouteEvent`) | modify | Add `toolbox_backend`/`compute_api` optional fields with the explicit bucket vocabulary (§6), corrected citation from v1's `:23-63`. |
| `src/daemon.rs:305-320` (`Router::new`/`RouterArgs`) | modify, **new in v2** (Dory critic blocking finding #4 — not in v1's file list at all) | Add the serving-identity registration mechanism (§5b) so router-eligible detached toolbox servers can actually become routable upstreams; this is the file that currently proves the gap exists. |
| **PR2 API routes (new in v2 — Dory readiness finding: v1 described these only in prose)**: `GET /api/toolbox-catalog`, `GET /api/toolbox-containers`, `POST /api/toolbox-containers` (create), `POST /api/toolbox-containers/{id}/update`, `POST /api/toolbox-containers/{id}/delete`, `POST /api/toolbox-containers/{id}/adopt`, `GET /api/toolbox-models` | **new** | Exact request/response JSON shapes to be finalized as part of PR2 implementation (not fully specified in this design doc — flagged as needing a follow-up API-contract note before PR2 starts, per readiness finding). |
| `src/escalation/templates/main_dashboard.html` (Sankey block, ~1300-1610) | modify | 6-column `SK_COLS`, `skKeysOf`, color map, proportional layout, explicit bucket values (§6). |
| `src/escalation/templates/benchmarks.html` | modify | New spider-chart panel + dimension picker; new filter controls for toolbox family/engine; TEPR v2 display incl. "insufficient data"/"no passing runs" states. |
| `src/cli.rs` (`Toolboxes`, `Upgrade`) | modify | Extend to multi-backend; add `toolbox enter <name>` exec path gated to same-host-same-user (§5), remote `--url` case prints copy/paste guidance only. |
| `tests/` (new integration tests) + `scripts/test-benchmark-ui.cjs` (extend) | new/modify | Cover new endpoints, Sankey columns, spider chart rendering, TEPR fixture cases (incl. "no passing runs" and "insufficient sample size"). |

## Testing and evaluation
- Unit tests for `toolbox_catalog.rs` parsing against the actual vendored JSON (not synthetic fixtures) so a real upstream schema change fails a real test, **including a fixture with an unrecognized backend id to prove lossless parsing (`CatalogBackendId::Other`) actually works** (new in v2 — directly tests the fix for blocking finding #2).
- Unit tests for `cockpit_config.rs` covering: file absent, file present with unknown extra keys (must round-trip untouched), and the explicit single-write "apply" action (load → mutate one key → write → reload and diff) — **no longer a "concurrent-write race simulation ... last one wins, documented not fixed" test as v1 proposed**, since v2 no longer ships unconditional automatic bidirectional writes; if/when true bidirectional merge is built later, real concurrent-write fault-injection tests are required before it ships (Open questions).
- **Regression test for the `--name`/timeout fix must force the timeout branch** (e.g. a test double or artificially-lowered timeout against a deliberately slow command), not just run the check twice normally — v1's proposed test would have passed even without the fix (Dory critic finding #1); assert no orphaned container survives specifically the timeout path.
- Test that the migration-runner version gate (`src/benchmark.rs:1895-1953`) actually accepts and correctly executes the new migration version(s) — a smoke test that a fresh DB migrates from version 2 → 3/4 and an existing populated version-2 DB migrates in place without data loss.
- Authorization test: every new mutating route is present in the destructive-route allowlist and is rejected from a non-loopback simulated peer (new in v2, directly tests §9).
- Extend `scripts/test-benchmark-ui.cjs` to assert the spider chart renders for a fixture dataset with all 7 axes selectable and TEPR computing to the v2 formula on a known fixture (including "no passing runs" and "insufficient sample size" cases, and asserting the categorical-axis behavior once Open question 4 is resolved).
- Explicitly **cannot** integration-test ds4/halogen/r9v/vllm container creation against real hardware in this environment — see Risks; propose these land behind a feature flag / "unverified on this host" UI badge until hardware-validated. Explicitly **cannot** verify whether `toolbox create` forwards arbitrary podman flags in this environment either — the §5 spike needs a real host with `toolbox`/podman installed, flagged as a blocking prerequisite for any backend needing non-default `engine_args`, not just a nice-to-have.

## Security, privacy, reliability, and operations
*(High-level operational summary; §9 above has the concrete, load-bearing detail added in v2.)*
- `cockpit_config.rs` must preserve cockpit's `chmod 0600` on write (the file may contain `HF_TOKEN`/API keys per backend settings) — never relax permissions, never log the full file contents.
- Command builders must apply the same secret-redaction discipline as cockpit's `redact_command()` (`--api-key`, `HF_TOKEN`, etc.) before any command is logged or displayed in the dashboard/CLI.
- Detached server-mode containers need an operational answer for "what happens to a container if brainrouter itself restarts/crashes" — proposed: containers are named deterministically and labeled (§5), and brainrouter reconciles/adopts already-running named-and-labeled containers on startup rather than assuming a clean slate.
- Catalog vendoring must not vendor any auxiliary download credentials or model weights — only the catalog *metadata* JSON files.
- **New in v2:** every new privileged endpoint is explicitly enumerated in the destructive-route allowlist (§9); this is now a required implementation checklist item per route, not a general statement of intent.

## Rollout, migration, and rollback
Proposed phased PR sequence (via the pr-stack pattern), each independently reviewable/revertable. **Reworked in v3** (superseding v2's single-file linear ordering, per the Decision log) into three explicit waves so that (a) foundational, hardware/router-independent work lands first, (b) Sankey/benchmark population is incremental per-backend rather than gated behind one big "all backends routable" milestone, and (c) the R9V hardware spike blocks only R9V, not the other three new backends.

**Wave 1 — foundational, no hardware or router dependency.**
1. **PR 0 — this design doc + the `--name`/timeout fix.** Design doc committed to `docs/design/ai-toolbox-cockpit-integration.md` (per Dory) as the durable source of truth; the corrected §5a fix (name + replace + timeout-branch cleanup) lands as a small, independent, safe-to-land-alone change with its timeout-forcing regression test.
2. **PR 1 — catalog vendoring + sync script + schema validator + CI workflow.** No behavior change yet.
3. **PR 2 — `toolbox_catalog.rs` types (open `CatalogBackendId`/closed `SupportedServingBackend`) + generalized Toolboxes tab** (list/create/update/delete-equivalent for all 5 backends via the catalog, no bespoke server-launch yet), plus container ownership labeling/adopt-flow and per-container operation locking. This alone already satisfies most of "webui for cockpit's Toolboxes tab."
4. **PR 3 — `cockpit_config.rs`, Phase 1 only** (config.json read path + the explicit single-write "apply" action — automatic bidirectional RMW is **not** in scope for this rollout at all, per the user's confirmation; see Decision log), wired into the Toolboxes tab's "default toolbox per platform" behavior, including the config-path/HOME-mismatch surfacing (§4).

**Wave 2 — incremental Sankey/benchmark population + per-backend server mode.** **Reworked in v3:** v2 treated "serving-identity registration for every backend" as one hard prerequisite gating both the Sankey and benchmark PRs. That's replaced with per-backend incrementality: each new column/dimension is added once (nullable/additive), then *populated* backend-by-backend as each backend's own server-mode PR lands, rather than waiting for all of them.
5. **PR 4 — Sankey extension** (`RouteEvent` gains nullable `toolbox_backend`/`compute_api` fields; dashboard renders the 6-column layout, §6). Ships now, immediately meaningfully populated for the traffic that's *already* routable today (llama_cpp/manifest) — no new prerequisite needed for that slice. Every other backend renders in the explicit `status_only` bucket (§6 vocabulary) until its own server-mode + routing-integration PR (below) lands; never faked as routed traffic.
6. **PR 5 — Benchmark toolbox-family dimension** (§7: widen `runtimes` vs. new `serving_runtimes` table decision made here, plus the migration-runner version-gate wiring in `src/benchmark.rs:1895-1953`). The *engine* half of this (`runtimes.backend` = rocm/vulkan) needs no new prerequisite at all — it already exists and is already populated today; only the new toolbox-family/toolbox-id column(s) start as `NULL`/absent until ingest for a given backend is wired up in that backend's own PR.
7. **PR 6 — Models tab equivalent** (browse catalog models per backend, trigger downloads via each backend's own mechanism) — independent of server-mode/routing, can land any time after PR2/PR3. **Flagged (unchanged from v2):** this PR's download-lifecycle design (resumability, hashing/integrity verification, atomic publish, cancel/concurrency) needs its own short design note before implementation.
8. **PR 7 — ds4 server mode** (create/update/pull + detached launch). **Corrected during PR7 implementation (§12a): does *not* include §5b's routing-integration/serving-identity-registration slice** — that turned out to be a cross-backend concern (the same registry must serve ds4/halogen/vllm/r9v alike) rather than something to build ds4-only, so it's deferred to new PR 11a below. PR7 ships dashboard-only visibility (start/stop/status panel) for ds4 servers; they do not appear in the Sankey or benchmark dashboards in any bucket, `status_only` included, until PR 11a lands. Does not depend on R9V's spike (PR below).
9. **PR 8 — halogen server mode** (same shape as PR 7, for halogen — dashboard-only visibility, same Sankey/benchmark deferral). **Complete (§13/§13a).** Also generalized `resolve_ds4_toolbox()`'s (PR7) feature-state check from `Supported`-only to "anything but `Unavailable`" — a retroactive bug fix to already-shipped PR7 code, found while implementing PR8 (see Decision log).
10. **PR 9 — vllm server mode** (same shape as PR 7, for vllm — dashboard-only visibility, same Sankey/benchmark deferral). **Complete (§14/§14a).** First reuse of `apply_backend_setting_values()` (built for halogen in §13a) by a second backend, via a new, separate `POST /api/server-mode/vllm/cache-paths` endpoint (not bundled into server start, unlike halogen — a genuine upstream behavioral difference, not an oversight).
11. **PR 10 — R9V real-hardware ROCm functional validation (retargeted in §11: no longer a `toolbox create` flag-forwarding spike).** Reading upstream source (§11) resolved the flag-forwarding question by inspection: `toolbox create` never receives `engine_args` for any backend, and Server Mode's `podman run` unambiguously accepts arbitrary flags — neither needs live hardware to confirm. PR 10's real remaining scope is narrower and purely functional: confirm ROCm (HSA/`--device /dev/kfd`+`/dev/dri`, `--group-add render`/`video`, `--ipc=host`) actually works end-to-end on a real R9700 host once PR 11's command builder exists to test against — a hardware-validation checkpoint gating only R9V, not a design spike blocking implementation. If real R9V hardware access is never obtained, R9V ships behind the same "unverified" badge as the rest of the risk table, not blocked indefinitely.
12. **PR 11 — R9V server mode**, implemented on the same schedule as PRs 7-9 (no longer gated on a pre-implementation spike outcome, since §11 resolved the mechanism question by source inspection); PR 10's hardware validation happens against PR 11's own command builder once both exist, and is tracked as a functional-verification checkpoint rather than a blocking prerequisite. Same dashboard-only visibility / Sankey deferral as PRs 7-9.
13. **PR 11a — §5b serving-identity registration (new, added during PR7 implementation — §12a).** Builds the actual `{toolbox_backend, compute_api, runtime_profile_id, endpoint}` registration mechanism §5b originally proposed, once all four new backends' server-mode command shapes are known (from PRs 7-9/11) rather than designing it against ds4 alone. For each backend, resolves option (a) (register as a real routable llama-swap-compatible upstream, if the server speaks an OpenAI-compatible API) vs. option (b) (`status_only` bucket, dashboard-visibility-only) per §5b's own branching — which backends land in (a) vs (b) is not asserted here, resolved per-backend during this PR. This is what actually makes `status_only` reachable for the first time; until this PR, `status_only` remains unreachable exactly as §6a already documented for `external`.

**Wave 3 — spider chart.** Depends on Wave 2's benchmark dimension data existing for at least a couple of backends to be meaningfully testable; TEPR v2 metric and the categorical-axis behavior (Open question 7) resolved before implementation. **New dependency found during PR7 implementation:** since PRs 7-11 no longer populate any Sankey/benchmark dimension for ds4/halogen/vllm/r9v (§12a), Wave 3's "a couple of backends" precondition now realistically requires PR 11a to have landed first (llama_cpp alone is not "a couple of backends"), not just PRs 7-11 — Wave 3 is retargeted to depend on PR 11a, not just Wave 2's original PR list.
14. **PR 12 — Spider chart** (7-dimension picker, TEPR v2 metric, dimension-picker UI).

Rollback: every PR after #1 is additive (new tables/columns are nullable, new UI panels are new, not replacing existing ones) except the Sankey column-count change, the `runtimes`-widening option if chosen over the new-table option, and the hardcoded-image generalization in `server.rs`, which touch existing code paths — those get explicit "does the existing single-toolbox flow still work unmodified" regression checks before merge. The wave reordering does not change any individual PR's own additive/rollback properties, only their sequencing and mutual dependencies.

## Risks and mitigations
| Risk | Mitigation |
|---|---|
| ds4/halogen/r9v/vllm command builders are written against documentation/catalog data only, never run against real hardware in this environment | Ship behind an "unverified" badge; do not claim parity until a host with that hardware confirms; solicit upstream/user validation explicitly (see Open questions) |
| **New in v2, resolved in §11:** `toolbox create` may not forward the podman flags (`--device`/`--security-opt`/`--group-add`) that non-default `runtime_profile.engine_args` need | **Resolved by reading upstream source (§11), no spike needed:** `toolbox create` never receives `engine_args` for any backend upstream — the Toolboxes tab relies on Toolbx's own default device/group access instead; Server Mode applies `engine_args` via plain `podman run`, which natively supports arbitrary flags. Rollout PR 10 is retargeted to real-hardware ROCm *functional* validation, not a flag-forwarding spike |
| **New in v2, reworked in v3:** detached server-mode containers are invisible to the router by default, so Sankey/benchmark "backend/engine reflects real traffic" could be promised but not actually true | **v3:** no single hard gate; Sankey/benchmark ship in Wave 2 populated immediately for already-routable traffic (llama_cpp/manifest), with each additional backend's own server-mode PR (7-11) bringing its own routing-integration slice and population increment; backends not yet integrated render as explicit `status_only`, never faked |
| **New in v2:** brainrouter's daemon HOME and the interactive user's HOME running cockpit can differ, silently breaking the "shared config" premise | Surface effective resolved config path/owner; explicit override setting; refuse auto-create under service-account heuristics (§4) |
| **New in v2:** widening `runtimes` (if chosen over a new table) changes an existing, populated table's contract | Decision deferred to implementation time with both options scoped (§7); explicit backward-compat regression check either way |
| **New in v2:** generalizing the "is a newer image available" check to N images can hit registry rate limits / stall dashboard refresh, and today's version has no timeout at all | Cached async image-metadata service with TTL, bounded concurrency, per-call timeout, real OCI reference parsing, "freshness unavailable" fallback (§3) |
| config.json race between brainrouter and a running cockpit instance | **Confirmed in v3:** narrowed to an explicit single-write user action rather than continuous background RMW; automatic bidirectional live-merge is out of scope for this rollout entirely (user-confirmed, Decision log), not merely deferred pending sign-off |
| Vendored catalog drifts from upstream between syncs | Weekly CI sync PR + pinned-commit `SOURCE` file makes drift visible and reviewable, not silent |
| Sankey/benchmark schema changes break existing dashboards for users who never touch toolboxes | All new columns/fields nullable and additive; existing 4-column Sankey behavior is a strict subset of the 6-column version (explicit bucket for non-toolbox events) |
| "Token efficiency to positive result" is subjective and could mislead if the user disagrees with the chosen formula, or if the "actual tokens consumed" field turns out not to exist as assumed | Explicitly flagged as a proposal (not shipped as fact) pending sign-off; alternatives documented alongside it; field existence is an explicit implementation-time verification step, not assumed (§8) |
| Scope is very large for one contributor/session | Phased PR sequence (above) with independently mergeable increments; no single PR requires all backends done |
| **New in v2:** this document's own Dory review used sub-agent isolation, not full separate sessions | Disclosed explicitly in Status; a stronger isolation round is available on request (Open questions) |

## Open questions
*(Only unresolved, material items remain here; resolved questions are removed, not left stale — see Decision log for what was decided and why. TOP SESSION resolved 4 of v3's questions on 2025-09-16, explicitly instructing that the remaining ones below are correctly left for implementation-time investigation via their proposed spikes/verification steps, with outcomes documented in the Decision log as they're resolved.)*

1. **R9V hardware detection on non-R9700 hosts.** README validates on 2× AMD R9700 32GB; brainrouter has no ROCm/AMD-GPU detection logic today. Does brainrouter attempt to auto-detect ROCm-capable GPUs (and if so, how — `rocminfo`? sysfs? a vendored device-ID list?), or does it just expose R9V as a selectable option and let creation fail loudly with a clear error on unsupported hardware? Affects §5 and Rollout PR 11. To be resolved at PR 11 implementation time.
2. **`toolbox create` engine-args forwarding for R9V — resolved by reading upstream source (§11), no live hardware needed.** Upstream's own `build_create_command()` never passes `engine_args` to `toolbox create` for any backend; `engine_args` are only ever applied via plain `podman run` in Server Mode, which trivially supports arbitrary flags. What genuinely remains open (Rollout PR 10, retargeted) is purely functional: does ROCm actually work end-to-end on real R9700 hardware — a hardware/driver question, not a command-construction one. To be resolved at PR 10/11 against real hardware if/when available; ships behind an "unverified" badge otherwise.
3. **TEPR's "actual tokens consumed" field — does it already exist in ingested benchmark data, or does the ingest format need extending first?** §8's metric assumes an actual/observed token count is available (not just a declared/requested one); this needs to be checked against real `examples/benchmarks/*.json` shapes and the ingest code before Rollout PR 12 starts. If it doesn't exist, either the ingest format needs a new field (upstream harness cooperation required) or TEPR must fall back to a documented-as-weaker proxy. **Not resolved opportunistically during PR5** — PR5 only added the `toolbox_backend`/`compute_api`/`toolbox_id` dimensions (§7a); it did not touch `experiments.prompt_tokens`/`generation_tokens` vs. an actual-consumed-tokens field, so this remains fully open. To be resolved at PR 12.
4. **Models-tab download lifecycle (Rollout PR 6) — implemented in PR6 (§10a); items (a)-(c) still require explicit sign-off, item (d) is now resolved.** §10/§10a's mechanism (job-registry pattern mirroring `benchmark_lab.rs`, coarse `queued/downloading/verifying/complete/failed/cancelled` state machine, per-backend integrity matrix mirroring cockpit exactly, OS-level cancel) has shipped as designed. What remains genuinely open, requiring explicit sign-off: (a) whether the "no byte-level progress, raw-log tail only" UX trade-off is acceptable versus a real progress bar; (b) global vs. per-backend/per-directory download concurrency (shipped as global `Semaphore::new(1)`, per §10's proposed default); (c) whether brainrouter should ever gain a write path for `hf_token` back into cockpit's config.json (shipped read-only in PR6, per §10's proposed default). **(d) resolved (§10a Decision log, 2025-09-18): confirmed against the official `hf` CLI reference that `hf download` has no `--format`/`--json`/machine-readable progress flag at all** — the coarse state-machine design is not a stopgap, it is the only design the current CLI surface supports.
5. **`toolbox create --label ...` support, for ownership-label attachment specifically (§5c, narrower cousin of question 2).** PR2 passes `--label` flags to `toolbox create` and treats any resulting failure as loud/visible (a normal create-failure error), not silent — but this environment has no `toolbox`/podman binary to confirm the flag is actually forwarded on a real host. If a real host shows it isn't, the fallback (switch the creation primitive to plain `podman create` for brainrouter-managed containers) needs an explicit decision about whether that breaks `toolbox enter` compatibility for those containers, which is not decided here. To be resolved the first time PR2 runs against a real `toolbox`/podman host.
6. **§5b serving-identity registration shape, for new Rollout PR 11a (added during PR7 implementation, §12/§12a).** §5b sketched a `{toolbox_backend, compute_api, runtime_profile_id, endpoint}` record and an (a)/(b) branch (real routable upstream vs. `status_only` dashboard-visibility) but did not specify: where this record lives (new DB table? in-memory registry keyed by container name, similar to `model_downloads.rs`'s job registry? derived on-demand from `podman inspect` the way `ds4_server_status()` does today?), whether it's checked at request-routing time (adding runtime overhead to every request) or only at container-start time (cheaper, but stale if the container dies without brainrouter noticing), and which of ds4/halogen/vllm/r9v actually speak an OpenAI-compatible API (option (a) eligibility) — this needs checking against each backend's real server binary, not assumed uniformly. Deliberately left unresolved until PR 11a, once all four backends' own server-mode command builders exist to design the shared registry against.

## Decision log
*(Never erased; superseded entries are marked, not deleted.)*

- **Decision:** Toolbox model data uses a per-backend enum (`ModelPayload`) rather than one generic struct. **Status:** active (v1, reaffirmed in v2). **Reason:** `catalog/schema.py` validation rules differ structurally per backend; mirrors cockpit's own per-backend code organization. **Date:** 2025-09-16.
- **Decision:** Server-mode launches use detached, named, labeled containers rather than a foreground-suspend-the-TUI model. **Status:** active (v1, reaffirmed in v2, with a new load-bearing consequence spelled out in §5b). **Reason:** brainrouter's dashboard is a long-running headless HTTP server with no TTY to suspend into. **Date:** 2025-09-16.
- **Decision (superseded):** ~~Config.json integration is a full, continuous, automatic bidirectional read-modify-write against the same shared file cockpit uses.~~ **Status:** superseded by the entry below. **Reason it was superseded:** Dory critic round 1 showed this doesn't solve the actual concurrent-write race (atomic *write* ≠ atomic *read-modify-write*), and a daemon-vs-interactive-user HOME mismatch could silently mean "shared" config is actually two different files. **Date superseded:** 2025-09-16 (v2).
- **Decision:** Config.json integration is Phase 1 read-only import plus an explicit, user-triggered single-write "apply" action; automatic continuous bidirectional merge is deferred pending explicit user sign-off on the accepted risk (Open question 5) or a jointly-supported merge protocol. **Status:** active (v2). **Reason:** narrows the exposure window from "always racing" to "one explicit, reviewable write," while still satisfying "ideally merging" in a way the user can react to before the riskier version is built. **Date:** 2025-09-16.
- **Decision:** Backend identity is modeled on two layers — `CatalogBackendId` (open, lossless, includes an `Other(String)` fallback with a custom Deserialize impl so the original string is preserved) for anything read from the catalog, and `SupportedServingBackend` (closed, 5 variants: llama_cpp/ds4/halogen/vllm/r9v) for anything that triggers an executable action. **Status:** active (v2, replaces an implicit single-closed-enum assumption in v1 that was never written down as a formal decision but was corrected after the Dory critic's blocking finding #2). **Reason:** a single closed enum would hard-fail on comfyui or any future backend appearing in the catalog feed, contradicting the explicit "future ones" requirement. **Date:** 2025-09-16.
- **Decision:** The existing `runtimes.backend` column (`rocm`/`vulkan`/etc., `migrations/0001_benchmark_explorer.sql:58`) *is* the "engine"/compute-API dimension the user asked for — it is renamed/reused conceptually, not duplicated with a new column of the same meaning. **Status:** active (v1, reaffirmed in v2). **Reason:** avoids two columns meaning the same thing; naming clarified in v2 §1 as `compute_api` in prose to disambiguate from the new toolbox-family dimension, without necessarily renaming the underlying SQL column (a cosmetic rename is optional, deferred to implementation). **Date:** 2025-09-16.
- **Decision:** Whether to widen `runtimes` or add a new `serving_runtimes` table for the toolbox-family dimension is explicitly left open (§7), not decided in this document. **Status:** open (v2, new). **Reason:** both options have real, only-discoverable-at-implementation-time costs per the Dory critic; premature commitment here risks locking in the wrong one. **Date:** 2025-09-16.
- **Decision:** `toolbox create`'s ability to forward arbitrary podman flags is unverified and treated as a blocking spike (PR5), not assumed to work. **Status:** open/spike-pending (v2, new). **Reason:** Dory critic blocking finding #5; today's only observed usage (`server.rs:2283-2293`) passes just `--image` and a name, no evidence either way for flag-forwarding. **Date:** 2025-09-16.
- **Decision (superseded):** ~~A new "serving-identity registration" mechanism (§5b) is a hard prerequisite for *both* the Sankey and benchmark-extension PRs, inserted into the Rollout order before those PRs.~~ **Status:** superseded by the entry below. **Reason it was superseded:** on a post-approval second-thoughts pass, the user (via TOP SESSION) agreed this over-gated the design — llama_cpp is already routable today, so Sankey/benchmark support for it doesn't need to wait on a router-integration mechanism for the other four backends. **Date superseded:** 2025-09-16 (v3).
- **Decision:** The Sankey and benchmark-toolbox-dimension PRs ship early (Rollout Wave 2, PRs 4-5) and are populated **incrementally per backend**: immediately meaningful for already-routable traffic (llama_cpp/manifest) with no new prerequisite, then extended one backend at a time as each backend's own server-mode PR (7-11) brings its own routing-integration slice. Backends not yet integrated render as an explicit `status_only` bucket, never faked as routed traffic. **Status:** active (v3). **Reason:** user-requested rework to avoid one large all-or-nothing gate; matches how the rest of the rollout is already incremental per-backend. **Date:** 2025-09-16.
- **Decision (superseded):** ~~The R9V `toolbox create` engine-args spike (PR5) is a prerequisite before *any* backend needing non-default `engine_args` is claimed to work, implicitly gating ds4/halogen/vllm alongside R9V.~~ **Status:** superseded by the entry below. **Reason it was superseded:** user-requested rework — none of ds4/halogen/vllm are known today to need non-default `engine_args`/device passthrough; only R9V's ROCm passthrough does. Blocking all four behind one spike was an unnecessary scope coupling. **Date superseded:** 2025-09-16 (v3).
- **Decision (superseded):** ~~The `toolbox create` engine-args spike (Rollout PR 10) gates only R9V (Rollout PR 11); ds4/halogen/vllm (PRs 7-9) proceed independently of its outcome. If a future backend turns out to need non-default engine_args too, it moves behind this same spike at that time.~~ **Status:** superseded by the entry below. **Reason it was superseded:** the premise (an unresolved "does `toolbox create` forward engine_args" question) turned out to be answered by upstream's own source, not by a live-hardware spike — see §11. **Date superseded:** 2025-09-18.
- **Decision:** `toolbox create` never receives `engine_args` for any backend upstream (confirmed by reading `runtime/interactive.py::build_create_command()` directly — the Toolbx-wrapper branch is exactly `["toolbox", "create", "--image", image, name]`); `engine_args` are applied only in Server Mode via plain `podman run` (`backends/{ds4,halogen}/server_runner.py::build_server_cmd()`), which natively accepts arbitrary flags. Rollout PR 10 is retargeted from a flag-forwarding spike to a real-hardware ROCm functional-validation checkpoint (does ROCm actually work on an R9700, not "can a flag be passed"); PR 11 (R9V server mode) is no longer gated on PR 10's outcome and can proceed on the same schedule as PRs 7-9. **Status:** active (v3, resolves Open question 2's mechanism half). **Reason:** direct source inspection is stronger evidence than an unverified assumption, and removes an unnecessary implementation-blocking dependency for PR 11 — only real hardware functional validation remains genuinely gated. **Date:** 2025-09-18.
- **Decision:** PR7 (ds4 Server Mode) v1 scope is single-node/"Standalone" only — multi-node coordinator/worker roles, DSpark, SSD-streaming, and MTP/vision-projector auto-configuration are not natively ported; a `custom_args` free-text field (mirroring upstream's own identically-named `server_runner.py` parameter, not a brainrouter invention) is the escape hatch for anything beyond core single-node params (model path, ctx, host, port). **Status:** active (v3 addendum, pre-PR7). **Reason:** each deferred feature is itself a nontrivial per-model-family policy (`backends/ds4/config.py`'s `families` table); porting all of it in PR7 would repeat PR6's scope growth. Mirrors the already-accepted `quant_pattern` free-text narrowing precedent (§10a). **Date:** 2025-09-18.
- **Decision:** Server-mode start/stop/status (PR7 onward) is synchronous request/response (mirroring PR2's `create_toolbox_container`), not an async job registry (unlike PR6's downloads) — starting/stopping a container is a sub-second operation, and `status` reads live `podman inspect`/`ps` state with no new persisted DB table. **Status:** active (v3 addendum, pre-PR7). **Reason:** matches the actual latency/statefulness profile of the operation; introducing async job polling for a sub-second action would be needless complexity PR6's download use case doesn't share. **Date:** 2025-09-18.
- **Decision:** Config.json integration ships as Phase 1 read-only import plus an explicit, user-triggered single-write "apply" action; automatic continuous bidirectional merge is **out of scope for this rollout**, not merely deferred. **Status:** active, confirmed (v3; supersedes v2's framing of this as still needing sign-off). **Reason:** TOP SESSION explicitly confirmed this is the accepted default in response to Open question 5 being raised ("Accept the safer Phase 1 default... Do NOT build automatic continuous bidirectional merge"). **Date:** 2025-09-16.
- **Decision:** If `~/.config/ai-toolbox-cockpit/` is absent on a host, brainrouter leaves it absent and uses its own built-in defaults until cockpit itself initializes the directory; brainrouter never auto-creates it and never refuses to operate because of its absence. **Status:** active, confirmed (v3; resolves what was Open question 2). **Reason:** TOP SESSION explicitly selected this option ("if it doesn't exist... leave it absent and use brainrouter's own defaults... Do not auto-create, do not refuse to operate"). **Date:** 2025-09-16.
- **Decision:** In the spider/radar chart, engine/backend/toolbox (and, by the same logic, model/harness) are facet/filter/series-selection keys only and are never plotted as their own radar axis; the radar's spokes are continuous metrics only (TEPR, speed, extensible to more later). **Status:** active, confirmed (v3; resolves what was Open question 7 / v2's option (b)). **Reason:** TOP SESSION explicitly selected option (b) ("engine/backend/toolbox are filters/facets only... Radar only plots continuous metrics... per selected facet combination"). **Date:** 2025-09-16.
- **Decision:** The current isolated-sub-agent Dory review round (comprehension/readiness/critic, disclosed as partial isolation in Status) is sufficient; no additional full-`create_session`-isolated re-review round is required before implementation begins. **Status:** active, confirmed (v3; resolves what was Open question 8). **Reason:** TOP SESSION explicitly confirmed ("current isolated-sub-agent Dory round is sufficient; no additional full-session re-review round needed before implementation"). **Date:** 2025-09-16.

- **Decision:** `migrations/0003_toolbox_catalog.sql` / `toolbox_catalog_snapshot` is deferred, not created in PR2. **Status:** active (v3 addendum, PR2). **Reason:** PR2's container list/create/update/delete/adopt needs no persisted state beyond the compile-time-embedded catalog (PR1) and live podman state (existing pattern, extended); a snapshot-anchor table is only needed once something references a historical catalog snapshot, which first happens at Rollout PR 5. **Date:** 2025-09-17.
- **Decision:** "Adopt" for a pre-existing unmanaged toolbox container is implemented as an explicit recreate-in-place (remove + recreate from the same image, now with brainrouter's ownership labels attached), not a label-only no-op. **Status:** active (v3 addendum, PR2). **Reason:** podman has no `label add` for an existing container — labels are set at creation time only — so genuinely "adopting without touching the container" is not possible; silently treating a name/image match as owned without any recreation was rejected as reintroducing the exact unmanaged-container risk §5 called out. **Date:** 2025-09-17.
- **Decision:** Per-container-name operation locking uses an in-process `tokio::sync::Mutex` keyed by container name (`AppState.toolbox_container_locks`), not a DB-backed lease. **Status:** active (v3 addendum, PR2). **Reason:** brainrouter runs as a single process per host today (no evidence of multiple processes fronting the same podman socket); an in-process lock is sufficient and avoids adding cross-process lease machinery speculatively. Revisit if/when brainrouter ever runs multi-process against a shared container engine. **Date:** 2025-09-17.
- **Decision:** PR2's new mutating routes live under a single `/api/toolbox-containers` prefix (create/update/delete/adopt as sub-paths), gated in the destructive-route allowlist by a `path.starts_with("/api/toolbox-containers")` clause rather than one allowlist entry per route. **Status:** active (v3 addendum, PR2). **Reason:** matches the existing `/api/upgrade/` prefix-based gating pattern; avoids the allowlist silently missing a future sub-path added under the same prefix. **Date:** 2025-09-17.
- **Decision:** `/api/toolboxes` and `/api/upgrade/toolbox[/…]` (the pre-PR2, llama_cpp-only routes) are kept working, not deleted, in PR2; the dashboard switches to the new catalog-driven routes, and the old ones are marked superseded for removal in a later cleanup PR. **Status:** active (v3 addendum, PR2). **Reason:** avoids a breaking change for anything external already calling the old routes, while still moving the dashboard itself fully onto the generalized surface. **Date:** 2025-09-17.
- **Decision:** The generalized dashboard Toolboxes list is client-side filtered to the catalog's `strix-halo` platform's `toolbox_ids` before rendering (via `/api/toolbox-catalog`'s already-unfiltered `platforms[]`), rather than showing all 25 supported-backend catalog entries across all 4 hardware platforms. **Status:** active (v3 addendum, PR2). **Reason:** without this, the sidebar would list r9700/gb10/intel-b70-only toolboxes as "+ create" affordances on a strix-halo host, which is misleading clutter, not a real option. `strix-halo` was chosen because it's the platform brainrouter's existing hardcoded default image already assumes — this is a continuation of an existing assumption, not a new guess. The server API itself stays unfiltered (all platforms, all supported toolboxes) so a real host-detection mechanism (Open question 1) can later drive this from the server side instead. **Date:** 2025-09-17.

- **Decision:** `ApplyError::NotAvailable` maps to HTTP 404, not 500, on both PR3 apply routes. **Status:** active (v3 addendum, PR3). **Reason:** "the cockpit config directory doesn't exist on this host" is an expected, first-class, non-error domain state (§4's resolved Open question 2), not a server fault — 404 communicates "this specific write action isn't available right now" distinctly from a generic failure. **Date:** 2025-09-17.
- **Decision:** brainrouter's `cockpit_config::load()` does not replicate cockpit's own `_legacy_settings()` migration fallback (old `.llama-cockpit.conf`/`.ds4-cockpit.conf` files) — a missing `config.json` is simply "absent," full stop. **Status:** active (v3 addendum, PR3). **Reason:** that migration is cockpit's own upgrade-path concern to own; brainrouter re-implementing it would create a second, independently-drifting copy of logic that isn't brainrouter's responsibility, and cockpit already performs it correctly the next time it runs interactively on that host. **Date:** 2025-09-17.
- **Decision:** PR3's write path (`apply_default_toolbox`/`apply_active_platform`) does reload-verify-write (load current on-disk state immediately before mutating), using brainrouter's own established atomic-write helper pattern (UUID-suffixed temp file, `mode(0o600)`, `sync_all`, `rename()`) rather than literally mimicking cockpit's `config.tmp` naming convention. **Status:** active (v3 addendum, PR3). **Reason:** narrows (without eliminating) the concurrent-write race already documented and accepted in §4; reusing brainrouter's own existing helper conventions (`src/routing_profile.rs::atomic_write()`) is preferable to introducing a second, subtly different one — only the final atomic rename matters for safety, not the literal temp-file name. **Date:** 2025-09-17.
- **Decision:** The dashboard's "effective default toolbox" for a backend combines cockpit's `config.json` override first, falling back to the catalog's own `Platform.defaults[backend]` recommendation only when no cockpit override exists (absent config, absent backend entry, or absent platform key within it). Picking a toolbox in this UI writes immediately via the apply route, with no separate confirmation step. **Status:** active (v3 addendum, PR3). **Reason:** matches §4's precedence intent (cockpit's own explicit user choice should always win over a catalog-side recommendation); no confirmation dialog because the write only ever touches one shallow, freely-reversible key. **Date:** 2025-09-17.

- **Decision:** `derive_serving_identity()` is a hardcoded, exhaustive match on today's exact 2 `effective_provider` strings, explicitly documented to be **replaced**, not extended, once §5b's real per-request serving-identity lookup lands. **Status:** active (v3 addendum, PR4). **Reason:** re-verified `src/daemon.rs:305-320` shows the router has exactly `manifest`/`llama_swap` today, and `llama_swap` is unconditionally the one hardcoded Vulkan/llama.cpp image — so `llama-swap → llama_cpp/vulkan` is accurate for all of today's traffic, not a guess; growing this match arm-by-arm as future backends are wired in would silently reproduce the "invisible to the router" problem §5b exists to solve properly. **Date:** 2025-09-17.
- **Decision:** The dashboard's Sankey bucket vocabulary (`skServingBucket()`) implements only 3 of §6's 5 documented buckets (real value / `cloud` / `unknown` / `local_unmanaged`) from today's 2 real `effective_provider` values; `external` and `status_only` are left unreachable by design rather than stubbed. **Status:** active (v3 addendum, PR4). **Reason:** those two buckets only become meaningful once a later Rollout PR registers an additional backend's serving identity or an explicit manual-external-backend concept exists; the fallback chain already generalizes, so no JS change will be needed to reach them later, and adding dead/untestable branches now would be speculative. **Date:** 2025-09-17.
- **Decision:** `SK_COLS` moved from hardcoded absolute `x` positions to declared `width`s cumulative-summed by a `layoutSankeyColumns()` IIFE into computed `x`/`SK_TOTAL_W`. **Status:** active (v3 addendum, PR4). **Reason:** §6 itself flagged this as a small bundled refactor since PR4 already touches every line assuming 4 columns; computed layout means a 7th future column is a one-line array addition instead of a set of hand-recomputed pixel constants. **Date:** 2025-09-17.

- **Decision (doc-consistency fix, not a new design choice):** §6's dependency line ("ordered after §5b") and bucket vocabulary are updated to match what the v3 Rollout/Decision log already decided but §6 itself never caught up to: PR4 ships in Wave 2 without waiting for §5b to land for every backend, and a new `status_only` bucket value (a catalog-recognized backend whose own server-mode/routing PR hasn't landed yet, distinct from `unknown`/`external`) is added to §6's vocabulary — the Rollout text at PR4 already referenced `status_only` as "§6 vocabulary" even though §6 never actually defined it until now. **Status:** active (v3, PR4 pre-work). **Reason:** the document had drifted internally inconsistent between §6 (still v2-era "ordered after §5b," 4 buckets) and the later Rollout/Decision-log entries (v3, no such ordering, referencing a 5th bucket that didn't exist) — caught and fixed while starting PR4 implementation rather than left to silently diverge further. **Date:** 2025-09-17.

- **Decision:** Resolves Open question 2 (`runtimes` widen vs. new `serving_runtimes` table): a new `serving_runtimes` table, not a widened `runtimes`. **Status:** active (v3 addendum, PR5). **Reason:** `runtimes`' `NOT NULL` source-build columns (`repository`/`fork_name`/`commit_sha`/`compiler`/`compiler_version`) encode a build identity that has no natural analog for container-image-served backends; a new table keeps both identities' contracts intact rather than forcing one to fit the other. See §7a for the full comparison. **Date:** 2025-09-17.
- **Decision:** `experiments.serving_runtime_id` is added as a nullable `ALTER TABLE ... ADD COLUMN`, with `experiments.runtime_id` left `NOT NULL` and completely unchanged. **Status:** active (v3 addendum, PR5). **Reason:** purely additive — every pre-existing row gets `NULL` with no backfill required, and no existing query's non-nullability assumption about `runtime_id` is disturbed. **Date:** 2025-09-17.
- **Decision:** `toolbox_catalog_snapshot` (deferred out of PR2 per the earlier decision log entry) is created in PR5's migration, inside the benchmark database, not as a separate app-wide migration/DB. **Status:** active (v3 addendum, PR5). **Reason:** its only consumer is anchoring `serving_runtimes` rows to the exact catalog content in effect at run time, so colocating it under `benchmark.rs`'s existing version-gated migration runner is simpler than a second migration mechanism for one table. **Date:** 2025-09-17.
- **Decision:** Ingesting a `serving_runtime` auto-inserts its `toolbox_catalog_snapshot` anchor row only when the bundle's `catalog_snapshot_id` matches the binary's currently-embedded `vendored_catalog_revision()`; any other snapshot id is left to the foreign key constraint to reject (surfacing `BenchmarkError::Conflict`), never fabricated. **Status:** active (v3 addendum, PR5). **Reason:** matches the "fail loud, don't invent data" discipline already established for `--label` (§5c); auto-fabricating unknown historical snapshot content would silently misrepresent what catalog was actually in effect for that run. **Date:** 2025-09-17.
- **Decision:** `serving_runtimes.toolbox_backend` reuses `SupportedServingBackend` (§2) and `serving_runtimes.compute_api` reuses the existing `Backend` enum (§1/§7), rather than either field getting a new bespoke type. **Status:** active (v3 addendum, PR5). **Reason:** keeps backend/engine identity as one vocabulary across the whole codebase instead of a second parallel one scoped only to benchmarks; required adding hand-written `Serialize`/`Deserialize` impls to `SupportedServingBackend` (previously only used internally, not serialized) so it rejects `comfyui`/unrecognized ids at deserialize time. **Date:** 2025-09-17.
- **Decision:** PR5's dashboard changes add `Toolbox backend`/`Compute API` filter dropdowns to `benchmarks.html` but do not add corresponding columns to the visible run table, and do not add a `toolbox_id` dropdown. **Status:** active (v3 addendum, PR5). **Reason:** no backend populates non-null `serving_runtime` data until its own Rollout PR (7-11) lands, so table columns would show "Not reported" in every row today; `toolbox_id` remains available via the API/CSV/JSONL export without a dedicated dropdown, avoiding a third UI control alongside the two headline dimensions. Revisit once PR7-11 begin producing real data. **Date:** 2025-09-17.
- **Decision (bug fix, not a new design choice):** the pre-existing migration-runner's intermediate version-2 gate check (`if version != 2`) is corrected to a range check (`!(2..=3).contains(&version)`) so it means "has reached at least version 2 with the right shape," not "is frozen at exactly version 2" — the exact-equality form would have incorrectly rejected an already-fully-migrated version-3 database once version 3 was introduced. **Status:** active (v3, PR5 pre-work). **Reason:** caught during PR5's first real compile/test of the new migration, before it could ship as a regression; `schema_migrations` is append-only, so a v3 database still carries its v1/v2 rows and must pass every earlier range check, not just the final one. **Date:** 2025-09-17.

- **Decision:** PR6's download mechanism shells out to the `hf` CLI with per-backend argument shapes mirrored exactly from upstream (`hf download <repo> [<file>...] [--include <glob>] [--revision <rev>] --local-dir <dir>`), rather than a generic "download files" abstraction. **Status:** active (v3, §10 pre-PR6 note). **Reason:** verified live against upstream `{ds4,halogen,llama_cpp,r9v}/model_manager.py`; llama_cpp's exact-filename-vs-`--include`-glob branching and halogen/r9v's `--revision` pin are backend-specific and correctness-load-bearing, not incidental. **Date:** 2025-09-18.
- **Decision:** vLLM gets no download job type in PR6 at all — its Models tab is catalog/Hub-search browse-only. **Status:** active (v3, §10 pre-PR6 note). **Reason:** verified upstream `vllm/models.py` is explicitly documented as "deliberately non-downloading"; vLLM's server pulls from HF on demand at container start, so brainrouter mirrors that division of responsibility exactly rather than inventing a download step cockpit itself doesn't have. **Date:** 2025-09-18.
- **Decision:** Model-download orchestration reuses `benchmark_lab.rs`'s job-registry shape (single-flight `Semaphore`, `POST .../jobs` → 202 + id, poll `GET .../jobs/{id}`, `POST .../jobs/{id}/cancel`) as a new `/api/model-downloads` registry, with cancellation implemented as real OS-level child-process termination rather than `benchmark_lab.rs`'s cooperative `watch::channel` flag. **Status:** active (v3, §10 pre-PR6 note). **Reason:** avoids inventing a second concurrency/lifecycle mechanism when an already-reviewed one exists in the codebase; the cancellation mechanism must differ because `hf download` is an opaque external process that cannot itself observe a cooperative flag. **Date:** 2025-09-18.
- **Decision:** Download progress is modeled as a coarse state machine (`queued → downloading → verifying → complete | failed | cancelled`) plus an optional bounded raw-stderr tail, not a byte-level percentage. **Status:** active, proposal flagged for sign-off (v3, §10 pre-PR6 note). **Reason:** confirmed via web search that `hf download --local-dir` downloads to a temp/cache location and atomically renames into the destination only on success, so no partial-byte progress is observable at the destination path; parsing `hf`'s own `\r`-based tqdm stderr for a number is fragile and unverified in this sandbox (no network/`hf` CLI available here). **Date:** 2025-09-18.
- **Decision:** Model-file completeness/integrity checks mirror cockpit's own per-backend matrix exactly (llama_cpp: none; ds4: file-exists; halogen: per-file size match; r9v: size match plus a separate explicit user-triggered full SHA256 verify) rather than a uniform stronger check. **Status:** active (v3, §10 pre-PR6 note). **Reason:** the catalog itself carries `sha256` only for r9v's `CatalogModelFile` entries (already typed in `src/toolbox_catalog/models.rs`); inventing checksums brainrouter's own catalog data doesn't have isn't possible, and always-checksumming would re-introduce the "reading 100+ GiB per check" cost cockpit's own authors deliberately avoided (per their own source comment). **Date:** 2025-09-18.
- **Decision:** Resumability and atomic publish for model downloads are not separately implemented by brainrouter — both are inherited for free from `hf download`'s own cache-based resume and temp+rename publish behavior. **Status:** active (v3, §10 pre-PR6 note). **Reason:** confirmed via web search this is `hf download`'s actual behavior; a cancelled/failed job is retried simply by re-invoking the identical command, with no bespoke resume-tracking needed. **Date:** 2025-09-18.
- **Decision:** Resolves Open question 4(d): `hf download` has **no** machine-readable/JSON progress mode. **Status:** active, confirmed (v3 addendum, PR6). **Reason:** verified directly against the official `hf` CLI reference (`huggingface.co/docs/huggingface_hub/en/package_reference/cli`) — `hf download`'s enumerated option list is exactly `--type/--repo-type`, `--revision`, `--include`, `--exclude`, `--cache-dir`, `--local-dir`, `--force-download`, `--dry-run`, `--token`, `--max-workers`, `--help`; no `--format`/`--json`/`--quiet` flag exists for this subcommand (unlike e.g. `hf cache ls --format json` or `hf auth whoami --format json`, which are metadata-listing commands, not downloads). This closes the last open item under Open question 4 and confirms the coarse `queued/downloading/verifying/complete/failed/cancelled` state machine (§10 point 2) is not a stopgap pending a future check — it is the only reasonable design given today's actual CLI surface. **Date:** 2025-09-18.
- **Decision:** `model_downloads.rs` is a new top-level module, kept separate from `toolbox_catalog.rs`. **Status:** active (v3 addendum, PR6). **Reason:** `toolbox_catalog.rs` is pure catalog data-modeling; download job orchestration (subprocess spawning, cancellation, job registry state) is action/execution logic, mirroring the existing `benchmark.rs` (data/schema) vs. `benchmark_lab.rs` (job execution) split already established in this codebase. **Date:** 2025-09-18.
- **Decision:** `CatalogModelFile` is added to `toolbox_catalog.rs`'s curated top-level `pub use models::{...}` re-export list. **Status:** active (v3 addendum, PR6). **Reason:** `model_downloads.rs` needed it at the same import depth as `ModelPayload`/`SupportedServingBackend`; it had been omitted from the original re-export list even though sibling model types were present — a small oversight fixed opportunistically while wiring PR6, not a design change. **Date:** 2025-09-18.
- **Decision:** `hf_token` (`CockpitConfig`, top-level) and `models_dir` (`BackendSettings`, per-backend) plus a `CockpitConfig::models_dir(backend_id)` accessor are added to `src/cockpit_config.rs` as new, purely additive typed fields; `hf_token` remains read-only in PR6, with no write-back path added. **Status:** active (v3 addendum, PR6). **Reason:** sources the same shared HF auth token cockpit itself uses (`get_setting("hf_token", ...)`) rather than requiring a second, brainrouter-only copy, per §10's design; both new fields round-trip losslessly through the pre-existing `#[serde(flatten)] extra` catch-all, so no existing config.json content is put at risk. A write path for `hf_token` remains an explicit open item (§10 Open items, still unresolved) since it is a new secret-bearing write action outside Phase 1's read-only-plus-narrow-apply scope (§4). **Date:** 2025-09-18.
- **Decision:** The dashboard's model-download UI is a new top-level nav item (`#nav-toolbox-models`/`#toolbox-models-view`, "Downloads"), not a reuse or repurposing of the existing `#models-view`/`loadModels()` nav item. **Status:** active (v3 addendum, PR6). **Reason:** re-verified (not just assumed) that the existing "Models" view is an unrelated OMP routing-model picker over llama-swap's already-loaded models with a "Copy for OMP" action; conflating it with catalog browsing/downloads would have been confusing, not a simplification. **Date:** 2025-09-18.
- **Decision:** llama_cpp's download row exposes a free-text `quant_pattern` input (the operator types the exact filename or `--include` glob, sourced by reading the model's HF repo page themselves) rather than brainrouter attempting to enumerate valid quants server-side. **Status:** active, confirmed (v3 addendum, PR6; narrows what §10 had already flagged as a design point). **Reason:** re-confirmed directly against `llama_cpp_model_manager.py` during PR6 implementation that upstream itself derives this value from a **live** Hugging Face API repo-file-listing call (`get_hf_quants`/`get_hf_quants_with_sizes`), not from any static catalog field; replicating that live-lookup UX was judged out of scope for PR6 v1. Revisit only if a "browse this repo's available quants" feature is explicitly requested. **Date:** 2025-09-18.

- **Decision:** `src/server_mode.rs` is a new top-level module, kept separate from both `model_downloads.rs` and `server.rs`. **Status:** active (v3 addendum, PR7). **Reason:** same data-vs-action split rationale as `model_downloads.rs`/`toolbox_catalog.rs` (§10a) — this module is subprocess-construction/lifecycle action logic against live podman state, not catalog data modeling, and is a distinct action space from the download-job orchestration in `model_downloads.rs`. **Date:** 2025-09-18.
- **Decision:** `resolve_downloaded_ds4_model()` is added to `model_downloads.rs` (reusing its existing private catalog/path-resolution helpers) rather than duplicated in `server_mode.rs`. **Status:** active (v3 addendum, PR7). **Reason:** avoids a second, possibly-drifting implementation of "resolve a catalog model id to its on-disk path and verify completeness" — `server_mode.rs` depends on `model_downloads.rs` for this one function instead. **Date:** 2025-09-18.
- **Decision:** PR7's original design note (§12 item 6) claimed a running ds4 Server Mode container would populate the Sankey/benchmark dashboards via the already-shipped `status_only` bucket. **This is corrected: it does not, and this is a real, newly-identified scope narrowing, not a factual correction to upstream behavior like the ctx/host fixes.** §5b's serving-identity registration mechanism (the actual prerequisite for any Sankey/benchmark visibility of a server-mode container) is not built in PR7; it's deferred to new Rollout PR 11a, once all four new backends' server-mode shapes exist to design the shared registry against rather than building a ds4-only version now. **Status:** active (v3 addendum, PR7). **Reason:** discovered while implementing PR7's dashboard panel — confirmed `derive_serving_identity()`/`skServingBucket()` (§6a) can only ever act on an actual `RouteEvent`, and PR7 never touches `src/daemon.rs`'s `Router::new`/provider list, so no `RouteEvent` is ever emitted for a ds4 Server Mode container; `status_only` is therefore unreachable for it, exactly as §6a already documented for `external`. **Date:** 2025-09-18.
- **Decision:** ds4 Server Mode's dashboard panel is a new top-level nav item (`#nav-server-mode`/`#server-mode-view`, "Server Mode"), not folded into the existing "Downloads" (`#toolbox-models-view`) nav item. **Status:** active (v3 addendum, PR7). **Reason:** starting/stopping a server is a structurally distinct action from downloading a model file (§12's own framing distinguishes Toolboxes-tab dev-shells, Downloads' file-fetch jobs, and Server Mode's `podman run` containers as three different things) — mirrors PR6's own precedent of giving Downloads its own nav item rather than overloading the pre-existing "Models" (OMP routing-model picker) view. **Date:** 2025-09-18.
- **Decision:** `stop_ds4_server()` performs a graceful `podman stop --time 10` before `podman rm -f`, rather than mirroring upstream's `rm -f`-only stop path. **Status:** active (v3 addendum, PR7). **Reason:** upstream's `rm -f`-only stop relies on its own foreground/TTY-attached process having already received Ctrl+C's SIGINT before that `rm -f` runs; brainrouter's containers are started detached (`-d`, §12 item 1 — a decision already made in the design note, not new here), so there is no attached foreground process for a signal to reach, making a real graceful-stop step a necessary (not optional) corollary of the detached-lifecycle choice, not an arbitrary embellishment. **Date:** 2025-09-18.

- **Decision (retroactive correction to already-shipped PR7 code, not just a new PR8 choice):** `resolve_ds4_toolbox()`'s toolbox-eligibility check is generalized from "reject unless `features.server == Supported`" to "reject only if `features.server == Unavailable`" (both `Supported` and `Experimental` now accepted), via a new shared `resolve_toolbox_for_server()` helper both ds4 and halogen resolution now call. **Status:** active (v3 addendum, PR8). **Reason:** found while implementing PR8 — every vendored vllm/r9v/halogen toolbox is `server: experimental` in `assets/cockpit-catalog/toolboxes.json`, only ds4's happens to be `Supported`; upstream's own TUI (`server.py::set_platform()`) filters with `feature_state("server") != "unavailable"`, treating `Experimental` as fully selectable. The original PR7 check was too strict and, left uncorrected, would have rejected halogen's only vendored toolbox 100% of the time. Behavior-neutral for ds4 (already `Supported`); load-bearing for halogen. A new regression test (`resolve_toolbox_for_server_accepts_experimental_not_just_supported`) pins the corrected behavior against the real vendored data for both backends. **Date:** 2025-09-19.
- **Decision:** halogen's `platform_id` (required by `build_server_cmd`, rejecting anything but the literal `"strix-halo"`) is derived server-side via a new `Catalog::platform_id_for_toolbox()` reverse lookup, not accepted as a request field. **Status:** active (v3 addendum, PR8). **Reason:** exactly one platform owns the halogen toolbox in vendored data today, and platform `toolbox_ids` sets are disjoint, so deriving it removes a redundant value the operator would otherwise have to also supply correctly; the method's own doc comment flags first-match-wins as a latent limitation if a future catalog ever lists one toolbox under two platforms (not true of today's data). **Date:** 2025-09-19.
- **Decision:** `StartHalogenServerRequest` has no `custom_args` field, unlike ds4's request type. **Status:** active (v3 addendum, PR8). **Reason:** upstream's own halogen `build_server_cmd()` has no free-text/passthrough parameter to mirror — every tunable is already its own named env var, fully exposed as its own form field; inventing a brainrouter-only escape hatch with no upstream analog was rejected as scope creep beyond what §13's fidelity goal calls for. **Date:** 2025-09-19.
- **Decision:** A new generic `cockpit_config::apply_backend_setting_values(backend_id, updates)` helper is added in PR8 (not halogen-specific), used by `start_halogen_server()` and intended for reuse by PR9 (vllm) and PR10 (r9v) without modification. **Status:** active (v3 addendum, PR8). **Reason:** grepped `server.py` for all four backends and confirmed halogen/vllm/r9v all call `save_backend_settings()` on server start (ds4 does not) — building this generically now, during the first backend that actually needs it, avoids three near-duplicate per-backend writers landing across PR8-10. Brainrouter's write narrows upstream's 9 persisted keys to 7, omitting `models_dir` (auto-derived, never operator-supplied) and `engine` (no user-facing engine-selection concept — podman-only) — flagged explicitly as a narrowing in §13, not a silent gap. **Date:** 2025-09-19.
- **Decision:** `#server-mode-view` is converted from PR7's single hardcoded ds4 card into a backend-tabs bar + per-backend panel (`SERVER_MODE_CAPABLE_BACKENDS`), mirroring the Downloads view's `#toolbox-models-backend-tabs` pattern, rather than adding a second top-level nav item or a second hardcoded card beside the first. **Status:** active (v3 addendum, PR8). **Reason:** Server Mode is one product concept across backends (mirrors cockpit's own one-screen-per-backend `BackendServerPanel` TUI architecture), not two separate features; this also makes adding PR9/PR10's own panels a tab-array addition rather than a new nav item each time. **Date:** 2025-09-19.

- **Decision:** `VllmEffectivePolicy.attention_backend` is typed `Option<Option<String>>`, and brainrouter rejects (HTTP 400) any request that supplies an `attention_backend` override when the resolved policy has it explicitly locked (`Some(None)`), rather than upstream's own silent-ignore-the-override behavior. **Status:** active (v3 addendum, PR9). **Reason:** upstream enforces this only by disabling a TUI form widget, which has no headless-HTTP-API equivalent; silently accepting-and-discarding a client-supplied field is a worse API contract than an explicit validation error, and the divergence is fully documented in §14/§14a rather than left implicit. **Date:** 2025-09-19.
- **Decision (correction to §14's own pre-implementation note, not a new design choice):** `--max-model-len` is unconditionally emitted by `build_vllm_server_command()`, never omitted for `"auto"` — it resolves to the effective policy's `ctx` if present, else the literal string `"auto"`. **Status:** active (v3 addendum, PR9). **Reason:** §14's original "Validated request parameters" bullet incorrectly claimed the flag was omitted for `"auto"`; re-reading `runner.py::build_server_cmd()`'s `always_flags` list during implementation showed it is unconditional. Fixed directly in §14's text (a factual claim about shipped behavior, not a decision under negotiation) rather than left silently wrong. **Date:** 2025-09-19.
- **Decision:** `start_vllm_server()` does **not** call `apply_backend_setting_values()` on a successful start, unlike `start_halogen_server()` (PR8). Only the new, separate `save_vllm_cache_paths()` (backing `POST /api/server-mode/vllm/cache-paths`) persists settings, matching upstream's own two-actions-not-one shape for vllm exactly. **Status:** active, confirmed (v3 addendum, PR9; resolves §14's first open item). **Reason:** upstream's own `_start_confirmed()` has no `save_backend_settings()` call — only `save_caches_pressed()` does; auto-persisting on start would be a deliberate brainrouter-side enhancement beyond upstream fidelity, and the proposed default (match upstream, don't add convenience-divergence silently) was accepted as-is. **Date:** 2025-09-19.
- **Decision:** `reset_caches` ships in PR9 v1 as an explicit opt-in `bool` (default `false`) on `StartVllmServerRequest`, gated by a Rust port of upstream's `validate_compiled_cache_roots()` safety check (new `validate_compiled_cache_root()`, checked per-directory before any deletion). **Status:** active, confirmed (v3 addendum, PR9; resolves §14's third open item). **Reason:** the proposed default (ship as an explicit opt-in, not a bare always-wipe toggle, reusing upstream's own safety semantics verbatim) was accepted as-is; deletion only ever touches the vllm/triton/aiter directories' contents, never huggingface, matching `_clear_compiled_caches()` exactly. **Date:** 2025-09-19.
- **Decision:** Docker-engine support for vllm (relevant only to the one NVIDIA GB10 toolbox) is not ported in PR9; brainrouter remains podman-only for vllm, same as ds4/halogen. **Status:** active, confirmed (v3 addendum, PR9; resolves §14's second open item). **Reason:** the proposed default (no, until a user with GB10 hardware actually asks) was accepted as-is; `adapt_nvidia_runtime_args()` is a no-op for every real, in-scope brainrouter toolbox today regardless. **Date:** 2025-09-19.
- **Decision:** `model_downloads::expand_tilde()` is changed from private to `pub(crate)` so `server_mode.rs` can reuse it for vllm's cache-directory path resolution, rather than duplicating tilde-expansion logic. **Status:** active (v3 addendum, PR9). **Reason:** the exact same expansion semantics (leading `~` or `~/` only) were already implemented and tested for the Downloads/models-dir code path; a second copy would risk silent behavioral drift between the two. **Date:** 2025-09-19.

## Referenced files
*(Every file a fresh Dory phase needs to understand and implement the plan.)*

- `src/server.rs:224-241` — the destructive-route allowlist mechanism; every new mutating route must be added here (§9).
- `src/server.rs:526-527, 1839-1858, 1886-1909, 1919, 2044, 2244-2313` — hardcoded default toolbox image/repo across multiple call sites; the version-check command with its 15s timeout + `kill_on_drop(true)` (the real mechanism behind the `--name` bug fix, §5a); and the existing `toolbox create`/upgrade command construction that §5's spike must extend.
- `src/daemon.rs:144-153, 284-286, 305-320` — non-loopback bind mode (relevant to §9's exposure surface); HOME-fallback warning (relevant to §4's path-mismatch risk); `Router::new`/`RouterArgs` construction proving today's router only knows about Manifest/llama-swap providers (the basis for §5b).
- `src/routing_events.rs:30-82` — `RouteEvent` struct; target for the new `toolbox_backend`/`compute_api` fields (§6).
- `src/escalation/templates/main_dashboard.html:1300-1610` — existing Sankey `SK_COLS`/`skKeysOf`/color-map/click-filter implementation; extension target for §6.
- `src/benchmark.rs:388-406, 1748-1773, 1895-1953, 2248-2273` — `RuntimeDefinition`, `RunSummary`, the hardcoded schema-version gate (must be extended for any new migration to actually run), and the ingest `INSERT OR IGNORE` path whose uniqueness semantics must be re-audited against the new dimension (§7).
- `src/benchmark/http.rs`, `src/benchmark/http_tests.rs`, `src/benchmark/imports.rs` — benchmark HTTP API and ingest/import surface, extension targets for §7.
- `src/benchmark_lab.rs` — related benchmark-lab functionality; confirm no additional hardcoded assumptions before extending schema.
- `src/escalation/templates/benchmarks.html`, `src/escalation/templates/model_observability.html` — dashboard templates; extension targets for §7/§8.
- `migrations/0001_benchmark_explorer.sql:50-64` (`runtimes` table incl. `backend` CHECK at `:58`), `migrations/0002_benchmark_lab.sql` — existing schema; base for the widen-vs-new-table decision (§7).
- `migrations/0003_toolbox_serving_dimension.sql` — new `toolbox_catalog_snapshot`/`serving_runtimes` tables, nullable `experiments.serving_runtime_id`, and the updated `run_summary` view implementing §7/§7a's resolved design.
- `examples/benchmarks/llama-bench.json` — example benchmark data shape; needed to verify whether an "actual tokens consumed" field already exists for TEPR (Open question 6).
- `scripts/test-benchmark-ui.cjs` — existing UI test harness; extension target for spider-chart/TEPR test coverage.
- `src/cli.rs` — existing `toolboxes`/`upgrade toolbox` CLI commands; extension target for multi-backend CLI support and the same-host `enter` guard (§5).
- `src/model_downloads.rs` — model-download job registry, per-backend command builders, completeness/checksum checks (§10/§10a); the PR6 deliverable. Also gains `ResolvedDs4Model`/`resolve_downloaded_ds4_model()` in PR7 (§12a) and `ResolvedHalogenBundle`/`resolve_downloaded_halogen_bundle()` in PR8 (§13a), both reused by `server_mode.rs`.
- `src/escalation/templates/main_dashboard.html` (new `#toolbox-models-view`/"Downloads" nav item, distinct from the pre-existing OMP `#models-view`) — PR6's dashboard UI for catalog browsing + download/cancel/verify job management (§10a). Also gains a new `#server-mode-view`/"Server Mode" nav item in PR7 (§12a), converted in PR8 (§13a) into a backend-tabs bar covering ds4 + halogen, extended in PR9 (§14a) with a third vllm tab plus a separate cache-paths save card.
- `src/server_mode.rs` — ds4 (§12/§12a) + halogen (§13/§13a) + vllm (§14/§14a) Server Mode command builders/lifecycle functions, and the shared `resolve_toolbox_for_server()`/`container_server_status()` helpers all three backends use; the PR7/PR8/PR9 deliverable.
- `src/cockpit_config.rs` — also gains `apply_backend_setting_values()` in PR8 (§13a), a generic per-backend settings merge-writer, reused unmodified by vllm's cache-paths save action in PR9 (§14a) and intended for reuse by PR10 (r9v).
- `src/toolbox_catalog/types.rs` — also gains `Catalog::platform_id_for_toolbox()` in PR8 (§13a), a reverse lookup used to derive halogen's required `platform_id` server-side.
- Upstream (read-only, not vendored as code): `ai_toolbox_cockpit/catalog/schema.py`, `ai_toolbox_cockpit/assets/{models,toolboxes}.json`, `ai_toolbox_cockpit/settings.py`, `ai_toolbox_cockpit/runtime/{images,toolboxes,interactive,engines,server_process}.py`, `ai_toolbox_cockpit/backends/{ds4,halogen}/{server_runner,server,config}.py`, `ai_toolbox_cockpit/backends/halogen/runner.py`, `ai_toolbox_cockpit/halogen_model_manager.py`, `ai_toolbox_cockpit/views_servers.py`, `README.md` — source of the catalog schema/data and the command-sequence patterns this plan replicates natively in Rust (§3, §5, §11, §12, §13); `runtime/interactive.py::build_create_command()` and `backends/ds4/server_runner.py::build_server_cmd()` specifically are what resolved §11's finding; `backends/halogen/runner.py::build_server_cmd()` and `halogen_model_manager.py` are what grounded §13.

## Dory validation record
*(One entry per independent review. All three reviews below were performed against the **v1** document; v2 resolves or re-scopes every blocking finding, as detailed inline in the relevant sections and the Decision log. A bounded round-2 re-verification against v2 has not yet been run — see Open question 8 and the note at the end of this section.)*

**1. Comprehension review**
- Review type: comprehension
- Document version reviewed: v1
- Date/run identifier: 2025-09-16, `task`-tool sub-agent (isolated context, read-only tools)
- Inputs provided: v1 `plan.md` only, instructed to read cited repository/upstream files as needed
- Verdict: **FAIL**
- Blocking findings: 4 unresolved items — (paraphrased) ambiguity in how "engine" relates to the existing `runtimes.backend` column vs. a proposed new column; unclear whether `toolbox_backend` was meant to be closed or open given the "future ones" requirement; the config.json write strategy's failure mode on a concurrent write was not spelled out; the spider chart's TEPR metric did not specify what happens when a cohort has zero passing runs.
- Document changes made: all 4 directly addressed in v2 — engine dimension explicitly identified as the existing `runtimes.backend` column reused, not duplicated (§1, Decision log); backend identity split into open/closed two-layer model (§2); config.json write strategy narrowed and its failure mode made explicit (§4); TEPR v2 explicitly defines "insufficient sample size"/"no passing runs" states (§8).
- Remaining non-blocking notes: none carried forward beyond what's captured in the critic review below (the two reviews overlapped substantially on the enum/config-race items).
- Self-audit question answered post-hoc (v2, applied retroactively since the new rule postdates this review): the verdict relied only on v1's document text and the repository/upstream files it cited; no outside assumptions were identifiable in the sub-agent's returned reasoning.

**2. Readiness review**
- Review type: readiness
- Document version reviewed: v1
- Date/run identifier: 2025-09-16, `task`-tool sub-agent (isolated context, read-only tools)
- Inputs provided: v1 `plan.md` only, instructed to assess whether PR1+PR2 scope (catalog vendoring + generalized Toolboxes tab) was ready to start
- Verdict: **NOT READY**
- Blocking findings: 4 concrete gaps — the closed-enum/future-backend contradiction (same root cause as critic finding #2); no enumerated API route list for PR2 (only prose); no schema-validator module or CI workflow file named despite being described in prose; no explicit statement of what "generalized Toolboxes tab" excludes for PR2 (e.g., whether Server Mode actions are in scope for PR2 or a later PR).
- Document changes made: enum contradiction fixed (§2); API routes enumerated in Detailed implementation (flagged as needing a follow-up contract note, not fully specified inline); schema-validator module and CI workflow file both now named as explicit Detailed-implementation rows and PR1 deliverables; Rollout §PR2 description now explicitly scopes to list/create/update/delete-equivalent only, deferring Server Mode's detached-launch behavior to PR6..N.
- Remaining non-blocking notes: the API route JSON *shapes* (not just names) are still deferred to a pre-PR2 contract note, per the readiness reviewer's own suggestion that this was acceptable as a named follow-up rather than a blocker, provided it's tracked (it is, in Detailed implementation).
- Self-audit question answered post-hoc: relied only on v1's document text and named repository files; no unstated assumptions identified.

**3. Critic review (rubber-duck)**
- Review type: critic
- Document version reviewed: v1
- Date/run identifier: 2025-09-16, `task`-tool sub-agent, backgrounded, read-only tools with repository access
- Inputs provided: v1 `plan.md`, explicit instruction to verify citations against live source rather than trust the document's claims
- Verdict: 5 blocking + 17 important findings (this was the most substantive review; see the full finding list in session history/technical details — summarized inline throughout this document rather than restated in full here to avoid duplication)
- Blocking findings (see §5a, §2, §4, §5b, §5 for the full resolution of each): (1) `--name`-only framing incomplete — real leak is the timeout+`kill_on_drop` path; (2) closed-enum backend model contradicts "future backends"/comfyui-exclusion requirements; (3) config.json atomic-write doesn't solve the RMW race, plus HOME-mismatch risk; (4) detached containers are invisible to the router today, so Sankey/benchmark "real traffic" was unimplementable as designed; (5) `toolbox create` engine-args-forwarding capability is asserted, not verified.
- Document changes made: all 5 resolved or explicitly re-scoped as documented throughout §§1-9 and the Decision log; two of the five most load-bearing factual claims underlying these findings (migration-runner version gate, router provider construction) were independently re-verified directly against live source in this session (not merely trusted from the sub-agent's report) — confirmed true in both cases.
- Remaining non-blocking notes: 17 important findings, all folded into v2 (see Technical details / this document's inline "New in v2" annotations throughout); two pure nits (the `RouteEvent` citation range and Sankey bucket-value ambiguity) were also corrected.
- Self-audit question answered post-hoc: the critic's verdict relied on the v1 document plus direct reads of `src/server.rs`, `src/daemon.rs`, `src/benchmark.rs`, and upstream cockpit source — all either already cited in v1 or added as citations in v2's Referenced files list; no outside assumption was identified that isn't now traceable to a cited file.

**Note on round-2 re-verification:** all three reviews above were run against v1 and are recorded as-is per Dory's never-erase rule; v2's changes are this document's response to them, not independently re-validated by a fresh isolated review yet. Per the Dory bounded-critic-loop rule (max 3 rounds), a round-2 critic + readiness pass focused narrowly on "are the 5 blocking findings actually resolved in v2, and did the rewrite introduce any new blocking issue" is available and recommended before implementation begins, but has not been run — flagged here and in Open question 8 rather than silently skipped or silently presented as done.

## Human approval
**Approved.** v2 was approved via the CLI's plan-mode approval flow on 2025-09-16. A post-approval second-thoughts pass then raised 4 concerns, which the user confirmed reworking ("agreed rework all the ones you mentioned"), producing v3 (Rollout wave reordering, R9V-only spike scoping, decoupled Sankey/benchmark population). Separately, TOP SESSION (the creator session) reviewed v2 and explicitly resolved Open questions 2, 5, 7, and 8 on 2025-09-16, instructing that the remaining open questions (R9V hardware detection, `runtimes` widen-vs-new-table, the R9V `toolbox create` spike, TEPR's actual-tokens field, and the Models-tab download lifecycle) are correctly left for implementation-time resolution via their proposed spikes, with outcomes to be recorded in the Decision log as they land. TOP SESSION's instruction: proceed via autopilot through the Rollout starting with PR 0, stopping only for genuine blockers (a spike result or a design choice materially changing user-facing behavior beyond what's documented here) — not for routine progress updates.
